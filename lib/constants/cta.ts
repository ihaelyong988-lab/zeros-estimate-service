// ─────────────────────────────────────────────────────────────────────────────
// 예상견적 의뢰(request) 탭으로 보내는 CTA 버튼의 단일 문구.
// 렌더 지점이 6곳인데 문구가 5종으로 갈라져 있었다(2026-08-07 B5).
// 6곳 모두 채널 선택 화면(visit·quick 양쪽)을 열므로 "출장"으로 단정하면 오히려 부정확하다.
//
// 값의 출처 = AGENTS §10 확정 문구(app/page.tsx 사업소개 히어로 CTA).
// 조문이 박아둔 리터럴은 그 자리에 두고 여기서 복제하며, 둘의 일치는
// test/ui/pageSource.test.ts 가 기계로 채점한다 — 한쪽만 바뀌면 실패한다.
// ─────────────────────────────────────────────────────────────────────────────
export const REQUEST_CTA_LABEL = '무료 견적 검토 신청';
