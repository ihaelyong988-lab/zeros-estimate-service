# ZEROS 프로젝트 공유 상태 (Shared State)

- **최신 반영(2026-08-08 ②):** **PHASE S-2 — 최적화 ②단계(중복 제거) 완료.** 주인님 지시 "②단계 진행". Agent 11개 · Batch 3개(수정 9 + 적대 검증 2). **소스 14,657 → 14,345줄(−312) · 파일 66 → 76**(중복을 공용 모듈로 뽑아 파일은 늘고 줄은 줄었다) · **테스트 471 → 564(+93)**. ①②합산 **16,635 → 14,345줄(−2,290, −13.8%)**. 통합: `BaseZerosService` 상속 계층 제거 · 마이페이지 상수·유틸 → `lib/requests/timeline.ts` · 관리자 오류 배너 7중 → `AlertBanner.tsx` · 모바일 탭 5개 → `lib/ui/mobileTabs.ts` · `scrollMainPanelToTop` 3중 → `lib/ui/scrollMainPanel.ts` · **OTP 인증 3중 → `lib/otp/`** · a11y 스캔 규칙 이중 구현 → `test/support/sourceScan.ts` · 데이터 적재 → `useMyRequests` · 의뢰폼 필드 8개 · 홈 공종 Record 5종 → `lib/constants/landingTrades.ts`. **⚠ Agent 가 스스로 멈춘 것**: B3(마이페이지 JSX 100줄)는 전수 대조 결과 **렌더 차이가 29개**(폰트·여백·문구·한쪽에만 있는 버튼)라 바이트 동일한 배너만 뽑고 나머지는 두었다 — **미회수가 정답.** B2도 계획서가 "중복 구현"이라 적었으나 실제로는 **도달 불가한 대체 구현**이었다. **⚠ 적대 검증 FAIL 2건 봉합 — R5 8→7 은 해소가 아니라 은폐였다**: 오류 UI 2개가 `components/`→`lib/` 로 옮겨가며 게이트 `SCAN_DIRS` 밖으로 나가 위반이 검출되지 않게 됐다. → ①가드 실제 추가로 R5 진짜 해소 ②`SCAN_DIRS` 에 **`lib` 추가**(베이스라인 12→**32파일** 재스냅샷) ③무변경 채점 테스트 기대값 갱신. 은폐 아님을 기계 증명(가드 제거 시 R5 7↔8). **검증**: tsc 0 · test 564/564 · build 성공 · lint 에러 0 · 게이트 R1 0·회귀 없음 · O-37(704=704·클리핑 0) · O-38(736=736·카드 0.00px). **오류 원장**: 완료시각 3회 지연의 근본 원인 = **동시 실행 상한이 2인 것을 오늘 아침에야 측정**(그전 계획은 4~5 병렬 가정) → **AGENTS §16 신설**(`회전 수 = ceil(Agent수/2)`). **③단계(분해)는 미착수** — 계획서 §5. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-08-08_작업정리.md`.

## ~~🔴 진행 중 인수인계~~ (2026-08-08 08:35 · **해소됨** — ②단계 마감 완료)

> **②단계 중복 제거가 브랜치에서 진행 중이다. 아래를 그대로 이어받는다.**
>
> | | |
> |---|---|
> | 브랜치 | `refactor/stage2-dedupe` (master 미병합) |
> | 마지막 커밋 | `8ac125d` — Batch 1 완료(−210줄 · test 501) |
> | 워킹트리 | **미커밋 28개** — Batch 2가 쓰고 있는 중이므로 **되돌리지 말 것** |
> | 실행 중 | Workflow `w2w339ftk` (Batch 2 · Agent 5개 중 3 완료 · 2 작동) |
> | 근거 문서 | `docs/00_orchestration/최적화-계획서.md` §4 |
>
> **이어서 할 일 (순서 고정)**
> 1. Batch 2 완료 대기 → `npx tsc --noEmit` · `npm test` · `npm run lint` · `ui-quality-gate --check` → 임시 커밋
> 2. **Batch 3 = 적대 검증 Agent 2개**(읽기 전용) — 렌즈 = ①보안·회귀 ②조문·픽셀. FAIL 나오면 봉합
> 3. Playwright 픽셀 게이트 — O-37(홈 1366×768 스크롤 0·클리핑 0) · O-38(사업소개 412×870 스크롤 0·2단 카드 차 0.00px)
> 4. 마감처리 — worklog `docs/_worklog/2026-08-08_작업정리.md`에 ②단계 절 추가 · state 갱신 · 커밋 · master 병합 · 푸시 · `node scripts/prod-probe.mjs`
>
> **⚠ 이 세션에서 확정된 것 (다시 알아내지 말 것)**
> - **동시 실행 상한 = 2** (i7-7500U 물리 2코어). 소요는 `ceil(Agent수/2)` 회전으로 계산 — AGENTS **§16**
> - 용어는 **Batch · Agent** 영어 그대로. `파도`·`기수` 금지 — memory `use-english-batch-agent-terms`
> - Batch 1에서 계획서 오류 1건 발견: B2는 "중복 구현"이 아니라 **도달 불가한 대체 구현**이었다(삭제 판정은 통과)
>
> **주인님 액션 대기 2건** — `docs/03_qa_deploy/prod-env-setup.md` (①Vercel 키 등록 ②`supabase-setup.sql` 실행)

## 📌 현재 단계
- **진행 중인 PHASE:** PHASE J (최종 보고서 작성 및 프로젝트 이양 - 고도화 반영본)
- **현재 담당 역할:** 00_오케스트레이터 (Orchestrator PM)
- **이전 단계 상태:** PHASE A ~ K (랜딩 대시보드, Supabase DB Mock, 요청 위저드, 칸반 보드, CRM, 차트, PWA 모바일, iPhone 시뮬레이터, AI 비전 스캐너) 완료 ➔ 전원 합격 통과
- **최신 고도화 완료:** **PHASE L (최상단 박싱 규격 단일화 및 모바일/데스크톱 템플릿 정립) 100% 반영 완료**
- **최신 반영(2026-08-08):** **PHASE S-1 — 최적화 ①단계(삭제) 완료.** 주인님 지시 "마네킹 지워, ①단계 진행". 수정 5기 + 적대 검증 1기 · 1파도. **소스 16,635 → 14,657줄(−1,978, −12%) · 파일 68 → 66.** 삭제: **마네킹(Mock 폴백) 1,513줄**(`mock-data.ts` 1,406 + `MockZerosService` + `SEED_VERSION` + 삼항분기 + 채점 테스트, `.env.example`·`README` 동기) · `getCategoryVisuals`/`getDynamicMetrics` 죽은 반환 필드 8종 · `MobileSimulator` 131줄(켜는 코드가 0건이라 한 번도 렌더된 적 없음) · `isSupabaseEnabled` 도달불가 분기 2곳 · `zeros_admin_users` 표면 17줄(**DB 테이블은 보존**) · 쓰기 전용 필드·저장 카운터 가산 경로 · 잔재 SVG 5개 · `hasServiceKey`·eslint 제외 보강. **⚠ 기수가 스스로 멈춘 것 2건**: A7(`Estimate.payment_status`)은 계획서가 "확실"로 분류했으나 타입 계약이 소유 밖 3파일(`lib/payments/status.ts` 타입 파생·`RequestWizard` 살아있는 쓰기·`test/fixtures.ts`)에 걸려 **차단** · A10 저장 카운터는 가산 경로만 지우고 타입 필드는 보존(`CustomerList`가 8지점에서 읽는 **파생값 전달 통로**였음). **⚠ 적대 검증 FAIL 1 + 잔재 2 봉합**: ①`tsconfig.json` `resolveJsonModule` 삭제가 성립하지 않는다 — 강제 주체가 tsc 가 아니라 `next build` 라 빌드마다 되돌리며 파일 전체를 재직렬화(축소 0 + 새 부작용) → 복구·사유 주석·빌드 후 재작성 0 확인 ②`dataRoute.test.ts:72` 죽은 `hasServiceKey` mock ③**관리자 화면에 "로컬 Mock DB에 반영됩니다" 거짓 문구** 잔존 → 교정. **검증**: tsc 0 · test 471/471 · build 성공 · lint 에러 0 · 게이트 R1 0·베이스라인 회귀 없음 · O-37(704=704·클리핑 0) · O-38(736=736·카드 0.00px) · 콘솔 에러 0. **이월 3건**(A7·`SendResult.sent`·`FileMeta.file_type` — 전부 다중 파일 타입 계약이라 기수 묶음 필요). **②단계(중복 700줄)·③단계(분해) 대기** = 계획서 §4·§5. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-08-08_작업정리.md`.
- **최신 반영(2026-08-07 ②):** **PHASE R-3 — 4차 잔여 PLAN B 완주(26건).** 주인님 확정 ①A(CTA 통일) ②A(신뢰지표 라벨 분리) ③가(모바일 메인=홈 랜딩만 화이트) 반영. 수정 14기 + 적대 검증 3기 · 5파도. **주요**: C2 모바일 홈 다크→화이트 24지점(+`layout.tsx` themeColor·상태바) · C10 헤더 터치 44px 5곳(총높이 64/68px 불변) · B5 CTA 5종→1종 · B4 공개 실적 집계 모집단 통일 · N5 공종 표기 3종 통일(원시 렌더 잔존 0건, 공개 실적·고객 견적서 xlsx 포함) · B8 품목표 초안 저장 · C7 종료팝업 · C8 URL 병렬 기록 · D1 게이트 3룰→**7룰+전수 스윕+베이스라인** · D2·D7·N6·N4·N1·N2·B9 4건·N3·C4·C5(19지점)·C6·C9·D6. **⚠ 적대 검증이 FAIL 2건을 배포 전 봉합**: ①D7 `allowed_mime_types`는 방어값 0(MIME은 클라이언트 신고값 + `octet-stream` 허용 필수)인데 AutoCAD·한컴이 보내는 값을 거부해 **정상 첨부만 막는다** → 절 삭제, 용량 한도만 유지 ②N6 삭제 op를 만들고 **호출부가 0건**이라 실제 결함은 남고 공격 표면만 늘었다 → 관리자 UI에 삭제 버튼+확인 단계 신설. **기수가 계획서 오류 2건도 교정**: 3초 인터벌은 죽은 코드가 아니었고(데스크톱 견적검토가 소비 — 지웠으면 회귀), C5는 12지점이 아니라 19지점(주입 CSS·인라인 style 누락). **검증**: tsc 0 · **test 166→476** · build 성공 · lint 에러 0 · 게이트 R1 0·베이스라인 회귀 없음 · O-37(704=704·클리핑 0) · O-38(736=736·카드 0.00px) · C2 셸 `rgb(255,255,255)` · B5 실측 1종 수렴. **주인님 판단 대기 3건**(하단탭 활성 오렌지 대비 3.23:1 · 헤더 로그인 버튼 색 · '배관+장비설치'·'기타' 표시명)과 **신규 4건**은 결함대장 부록 C. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-08-07_2차_잔여작업정리.md`.
- **최신 반영(2026-08-07):** **PHASE R-2 — 4차 PLAN A 실행(치명 4 + 높음 11).** 주인님 위임("전문가답게 모든 작업을 해결하고 완료") → 헌법 제11조대로 PLAN A/B 보고 → **PLAN A 선택** → 실행. 소요 2시간 20분(07:05~09:25), 수정 9기 + 적대 검증 4기 · 3파도 · 파일 배타 소유. **보안**: ①관리자 로그인 시도 제한 신설(IP 15분 5회 — 배치를 400→503→429→401로 잡아 `prod-probe`의 env 미설정 진단이 가려지지 않게) ②**파일 서명 소유권 우회 차단**(업로드 경로 규약 + 남의 견적 경로 거부) ③접수 레이트리밋·입력 허용목록 ④관리자 토큰 무염 SHA-256 → 서버 SECRET HMAC(배포 시 기존 토큰 전부 무효·재로그인 필요) ⑤서버 오류 원문 비노출 ⑥접수번호 채번 재시도 + SQL 유니크 인덱스(**주인님 실행 대기**). **관리자**: 세션 만료 시 접수목록 `TypeError` 크래시(8시간마다 재현) 제거 · 매출·미수금 `₩0` 단정 표시 → `—` + `role="alert"` · **실재 기업명으로 랜덤 가짜 견적을 라이브 DB에 쓰던 '대리 접수' 버튼 제거** · 결제 행 수정 신설(삭제는 서버 op 부재로 미구현) · 칸반 수주성공 이탈 시 계약금액 되돌림(멱등) · 조회 실패를 '데이터 없음'으로 안내하던 3화면 분기. **고객 여정**: **의뢰 폼에 공사 종류·현장 유형 선택 신설**(기본 선택 없음·미선택 차단, 구 localStorage 초안의 기본값도 폐기) · 접수 완료 CTA 무반응 → 로그인 모달 경유 · 오지 않는 문자 안내 문구 삭제 · 고객 모달 2종 ESC·포커스 트랩 · **개인정보처리방침 페이지 신설**(수집 항목은 코드에서 확인한 것만) + 죽은 링크 복구 + 저작권 연도 파생 + 고아 라우트 `app/request/complete/` 삭제. **⚠ 적대 검증이 배포 전 4건 봉합**: ①A2가 `file_url` 필드로 그대로 우회(검증은 `file_path`만, 저장은 클라이언트 객체 통째) ②번호 레이트리밋이 400 요청도 소모 → 정상 고객 1시간 차단·제3자가 남의 번호 봉쇄 가능 ③B1이 STEP1을 796→1,299px로 늘려 오류 배너가 뷰포트 밖 렌더(= '다음' 무반응 화면) ④C1의 span→링크가 93×20px 터치 타깃 생성 — ①②는 수정 기수가 "완료"로 보고한 항목이었다. **검증 실측**: tsc 0 · **test 166→274** · build 성공(`/privacy` 생성·`/request/complete` 제거) · lint 에러 0 · 게이트 R1 0건 · O-38(736=736·카드 83.6/83.6·가로 0) · O-37(704=704·클리핑 0) · 봉합 배너 top 242(뷰포트 안)·터치 93×44. **✅ 이제 Vercel env 4종 등록해도 안전**(A단계가 선행 조건을 닫음). 잔여 = 4차 24건 + 신규 7건(결함대장 부록 B) — **B4(공개 실적 검토비율 100% 초과)는 B1 때문에 잠재→활성으로 바뀌어 다음 차수 최우선.** AGENTS §15-2 개정·§15-6~8 신설. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-08-07_작업정리.md` · 계획서 §8-8.
- **최신 반영(2026-08-06):** **PHASE R-1 (조사·계획만 — 코드 변경 0건)** — 미보호 영역 4차 전수조사. 베이스라인 실측 전부 초록(`tsc 0`·`lint 에러 0`·`test 166/166`·`build 성공`·라이브 200·콘솔 에러 0) → 조사 범위를 §14-2대로 **테스트 미보호 영역**(컴포넌트·API 라우트·미검증 유틸)으로 축소. 서브에이전트 **5기 병렬 정독**(계획 6기 중 5기 완료 시점에 주인님 지시로 종료, **적대 검증 2파도 미실시**) → **결함 39건**(치명 4·높음 11·보통 20·낮음 4), 그중 **13건은 본체가 코드 정독·라이브 실측으로 직접 확인**. **라이브 확증 5건**: ①의뢰 폼이 공사 종류를 한 번도 묻지 않음(`RequestWizard.tsx:120` 정의·448 제출 두 곳뿐 — 프로덕션 실제 접수 2건 전부 `배관공사/공장`) ②공개 실적 101건 중 **99건이 `est-test-*` 시드**인데 사업소개는 `246건`(`trust.ts:20` 상수) 표시 = 탭 두 번에 모순 노출 ③**env 4종 등록이 보안 결함 2건의 방아쇠**(관리자 로그인 무제한 대입 `admin/login/route.ts:34` · `createEstimate`의 클라이언트 `submitted_files` 무검증 저장 → `files/sign` 소유권 우회) — 지난 차수 P2-2와 동일 구조, **A단계 선행 필수** ④관리자 세션 8시간 만료 시 접수목록 `TypeError` 크래시(`EstimateList.tsx:218`, 매일 재현) ⑤모바일 홈 셸이 §10-A가 금지한 `#041B33` 사용(라이브 `rgb(4,27,51)`). **회귀 0 확인**: O-37·O-38 픽셀 게이트 5종 통과 · §13 익명 PII 차단 정상(고객/결제 401·익명 쓰기 403) · OTP 코드 응답 미노출 · 3단계 번들 분리 유지(고객 홈 `/api/data` 0회). 첫 로드 JS **338KB gzip / 1,174KB raw**. 산출물: 결함대장 `docs/_worklog/2026-08-06_전수조사_결함대장.md`(39건 전문 + 부록 A 라이브 실측) · 계획서 **§8 신설**(결정 3건 + A~D 4단계, 4h40m~6h10m) · **AGENTS §15 신설**(조사 축 분할·ETA 선보고·측정도구 의심·도구 함정·게이트 사각지대 = 오류 원장 8건). 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-08-06_작업정리.md`.
- **최신 반영(2026-08-01):** **1~3단계 개선 완료** — 안전망(vitest+CI)·돈 정합성·거짓정보 제거·방문자 이탈 차단·체감/구조 최적화. 테스트 0 → **166개**. 통합 기록 = `docs/00_orchestration/2026-07-30~08-01_개선작업_통합기록.md`(판단 근거·재발 방지 교훈·잔여 항목). 커밋 `e2aaa8e`.
- **최신 반영(2026-07-30 ③):** **PHASE Q-3** — 잔여 전량 완료(2차에서 "범위 밖"으로 남긴 것이 대부분 자체 제약 때문이었음 → 제약 해제 후 처리). ①**Pretendard 실제 로드 적용**(`npm i pretendard` + globals.css에 **dynamic-subset variable** CSS import) — **Playwright 픽셀 게이트 4종 실측 통과**: O-38 사업소개 412×870 `736=736`(오버플로 0)·가로 0 / O-37 홈 1366×768 카테고리 bottom **640**(조문 실측값과 일치)·`704=704`. 런타임 폰트 교체 비교로 scrollHeight 델타 0 확인(메트릭 유사) ②**ESLint 에러 6→0**(setState-in-effect 4곳을 파생 상태로 전환·렌더 중 ref 접근·`as any`·죽은 `SectionHeading` 23줄 삭제) ③**게이트 R1 강화**(`*error*` 임의 식별자 탐지, 기존 패턴·차단등급 불변 = 약화 0) → 강화 즉시 `MyRequestsView` role=alert 누락을 **새로 차단**해 그 자리에서 수정, 규칙만 있고 게이트가 비어 있던 상태 해소 증명. `role="alert"` 3곳 적용 ④**취약점 7→4건(HIGH 5→2)** — `overrides`로 postcss·sharp 강제(2차에서 "해소 불가"로 보고했던 건) ⑤죽은 코드 정리(manuals.ts 미사용 필드·RightSidebar export 해제·LeftSidebar 미렌더 label, 화면 문자열 무변경). tsc 0·build 0·lint 에러 0·게이트 차단룰 0(`--pass`). **잔여**: Vercel env 4종 등록(주인님 액션, 자격증명이라 AI 불가)·`noUnusedLocals`(죽은 코드 추가 정리 후). 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-30_작업정리.md` 3차.
- **최신 반영(2026-07-30 ②):** **PHASE Q-2** — 2시간 강화(Next CVE 선행 + 4기 병렬 + 적대 검증 3기). ①**Next 16.2.7→16.2.12**(eslint-config-next 동반) — Next 자체 HIGH 5건 직접 어드바이저리 0건화. **정정: postcss·sharp는 미해소**(next가 여전히 물고 있음, overrides 필요) ②**에러 바운더리 신설**(`app/error.tsx`·`global-error.tsx`) — 예외 1건에 네이비 백지로 굳던 것 차단 ③**화면 확대 차단 해제**(viewport `maximumScale`·`userScalable` 제거, 중장년 접근성) ④**정적자산 캐시 헤더**(`/images/*` immutable, 라이브 실측 `max-age=0`이었음) + **보안 헤더 3종** + manifest 중복 제거 ⑤**세션 만료 401 구분**·`loadTable` 오류 전파·**관리자 토큰 30일→8시간+비밀번호 바인딩**·XFF 마지막 홉·`unsign` byteLength·**KST 채번** ⑥관리자 UI: `toUpperCase` 무가드 2곳·방문 완료 **거짓 안내** 교정·취소 방문 분기·알림 status 정직 표시 ⑦**`.gitignore`에 고객자료 폴더 등재**(`견본/`·`견적서 사본/`·`estimate-ops/` 등 — `git add -A` 1회로 공개 원격 유출되던 경로, 추적 0건 확인). **⚠ 적대 검증이 치명적 회귀 차단**: `adminSessionInvalid` 401이 익명 분기보다 앞서 관리자 토큰 잔존 브라우저의 공개 홈·실적이 통째로 비는 문제 → 익명 견적 분기를 앞으로 이동 + 401 시 스테일 토큰 폐기. **로컬 실측**(무효 토큰+견적=200 / 무효 토큰+고객테이블=401)로 확인. **미실행**: 위생·툴체인 기수는 게이트 훅 약화 지시가 포함돼 **안전 분류기에 차단**(타당) → R1 정규식 보강·`noUnusedLocals`·lint 게이트 편입은 이월. **Pretendard 실로드는 폰트 파일 부재로 미적용**(적용 시 O-37/O-38 픽셀 게이트 재실측 필요 — 별도 PHASE). tsc 0·build 0·게이트 차단룰 0·lint 9건(증가 없음). 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-30_작업정리.md` 2차.
- **최신 반영(2026-07-30):** **PHASE Q-1** — 코드베이스 **전수 조사(5축 병렬 + 기계 채점)** 후 **위중 3건만 축소 조치**(주인님 지시 "1시간 안에 위중한 것만"). 조사 결과 = 신규 73건 + 기존 재확인 27건(P0 3·P1 12·P2 31·P3 54), 문서 `docs/_worklog/2026-07-30_전수조사_개선계획.md`. 계획을 **독립 검증**에 태워 착수 전 치명 결함 3건 차단(무인 자동배포로 라이브 로그인 중단 위험·홈 폰트 16px가 O-37 세로예산과 조문 모순·Wave 커밋 부재로 롤백 불능). 조치 3건(서브에이전트 3기 병렬, 파일 배타 소유): ①**인증번호 화면 노출 차단** — `otp/send` 응답 `devCode` 제거 + 표시 UI 3곳 삭제(타인 번호만 알면 남의 견적·도면·견적서 열람 가능하던 P0. `verify` fail-closed는 키 미등록 상태라 **미적용**) ②**익명 API 영업정보 유출 차단** — `stripEstimate`를 차단목록→**허용목록(`PublicEstimate`)** 전환(품목 단가·계약금액·실주사유 노출 차단, 실사용 필드 대조로 `estimate_sent_at` 포함) ③**방문 이력이 수주 실적을 삭감하던 버그** — `VISIT_SYNCABLE_STATUSES` 화이트리스트 + '취소' 분기 + 멱등 가드로 수주성공 건 상태 보존(`total_won`·`total_revenue` 차감 차단). tsc 0·build 0·게이트 차단룰 0(`--pass`). **ESLint 6에러는 범위 밖이라 잔존.** 나머지 97건·판정보류 8건 미실행. 파일: `api/data/route.ts`·`api/otp/send/route.ts`·`PhoneVerifyGate`·`CustomerLoginModal`·`MyRequestsView`·`lib/supabase/client.ts`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-30_작업정리.md`.
- **최신 반영(2026-07-17):** **PHASE O-37** — 캡쳐 3장 전수(계획 승인 후 위임 실행). ①홈 히어로 3카드 설명 = **3문장 업무용 카피**(판독→대조→효익, "부풀려진" 단정 표현 교정) + 1366×768 한 화면 세로 예산 재배분(배지 top 72·카테고리 bottom 640 좌표 실측) ②공종 실사 16장 **전면 재선정**(Pexels, 파일 교체만·코드 무수정) — 사후 교정 2건 반영: **외국인 얼굴·손 노출 0건(무인물 시설물 우선)·아크용접 0건·TIG 2건**, ID 매핑=worklog 표 ③사업소개 2단 카드 하단 **3색 시그니처 언더라인**(블루 155EEF·틸 1E7A72·앰버 B5762A, 3:2:1, 오렌지=CTA 전용 유지). AGENTS §10-A 조문 3건 신설(홈 항 신설·사진 기준·언더라인)+아카이브 연대기 4행. 게이트 `--pass`·빌드 0에러·Playwright 실측(홈 2해상도·business·review 바인딩). 파일: `app/page.tsx`·`public/images/trades/`(16)·`AGENTS.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-17_작업정리.md`.
- **최신 반영(2026-07-16 ④):** **PHASE O-35** — **AGENTS.md 지침 현행화**("md 지침 기록 정리" 지시, AskUserQuestion으로 범위 1회 확정). §10-A 시간순 누적 17불릿 → **화면별 그룹**(공통/실적/견적검토/사업소개/의뢰/SOP) 재편 + 최종 확정값만 유지(모순 조문 병합: 실적 헤더 "실적 분포" 단독·코너 제목 23px·카드 상단보더 철회 등), §10 기본 조문 현행화(히어로 clamp 38px·KPI 무채색), **O-30 사업소개 구조 조문 편입**(AGENTS 누락분), 섹션 번호 §0~13 불변. 구 조문·개정 이력 = **`docs/00_orchestration/agents-archive.md` 신설**(연대기 표 18행 + 원문 전문 스냅샷, 무손실 기준 git `e32c6c9`). 1:1 재대조 누락 0·빌드 0에러. 운영 규칙: 이후 §10-A 개정 = 본문 최종값 갱신 + 아카이브 연대기 1행(개정 서사 본문 누적 금지). 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-16_작업정리.md` O-35.
- **최신 반영(2026-07-16 ③):** **PHASE O-34** — 웹 기본 규칙 검토(홈탭 첫화면·종료 가드·휴대폰 인증) 후 보완 실행. **라이브 실측 발견 2건**: ①`/api/otp/status`=`enabled:false` — SOLAPI 3종 Vercel·로컬 모두 미등록(문자 미발송, 게이트 자동 생략) ②`/api/admin/login` 오답 프로브=**503** — `ZEROS_ADMIN_PASSWORD` Vercel 미설정(프로덕션 관리자 로그인 불가, fail-closed 정상). 보완: ⓐ**`scripts/prod-probe.mjs` 신설**(사이트 200·OTP 활성·관리자 env 3종 기계 판정, `--strict` 옵션) ⓑ마감처리 절차 **9단계 "배포 후 프로브" 필수 편입**(`.claude/commands/마감처리.md`) ⓒ`AGENTS.md §13` 실측 결과+프로브 의무 조문 ⓓ주인님 등록 가이드 `docs/03_qa_deploy/prod-env-setup.md`(Solapi 가입→발신번호 심사→키 발급→Vercel 등록→재배포→프로브 검증) ⓔ memory `prod-env-gaps-2026-07-16`. 홈탭 첫화면(manifest `start_url:"/"`+URL 무파라미터→home)·종료 가드(O-33 Playwright 실측)는 정상 재확인. 키 등록은 보안 규칙상 사용자 전용 액션. 빌드 0에러. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-16_작업정리.md` O-34.
- **최신 반영(2026-07-16 ②):** **PHASE O-33** — ①좌측 메뉴 라벨 **X표시 접미어만 삭제**(캡쳐 지시, 이유=필요 이상 설명·오해 소지): `CAPEX개선,증설`·`배관 SPOOL Module`·`SKID 제작설치`·`Structure제작,설치`(각 ~~검토~~)·`공사규모·금액`(~~미정~~) — 단일 소스 `menu.ts` + 하드코딩 4곳(LeftSidebar·AppShell 드로어·홈카드·실적 라벨) 전수, 잔존 0건, 데이터 키 불변. ②모바일 **앱 닫기 팝업(종료 가드) 전 시나리오 Playwright 실측 통과**(가드 적재→뒤로가기 팝업→계속 사용 재적재→재차 팝업→닫기 close, 수정 불필요). 게이트 `--pass`·빌드 0에러. 파일: `menu.ts`·`LeftSidebar.tsx`·`AppShell.tsx`·`page.tsx`·`PerformanceInsights.tsx`·`AGENTS.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-16_작업정리.md` O-33.
- **최신 반영(2026-07-16):** **PHASE O-32** — 예상견적 의뢰 채널 화면 레이아웃 정돈(캡쳐+후속 지시). ①채널 2카드 = 공용 박스 제거 + **좌우 독립 패널**(`rounded border p-6`, 내부 버튼 이중선 제거) + `gap-6 md:gap-8` ②절차 방법론 = `13.5px leading-snug`+**설명 핵심 키워드 압축**(폭만으론 협소 중앙패널서 2줄 남던 것 → 전 단계 한 줄, 방법론 용어 유지) ③하단 Footer = **브랜드·핵심영역 삭제, © 저작권 바 단독** ④Footer `mt-auto` 하단 앵커 복원(오버라인 213px 뜬 오류 교정, 좌측 사이드바 하단과 정렬) + 채널·footer 폭을 **AI Native(SOP) 탭과 동일한 `max-w-3xl lg:max-w-4xl`로 통일** ⑤우측 '예상 견적범위' 패널 자동 숨김 조건에 **`request` 추가**(의뢰 탭서 직전 상태 남아 열려보이던 문제, 엣지 탭 클릭 펼침 유지). 데스크톱·모바일 브라우저 실측 확인. 게이트 `--pass`·빌드 0에러·`graphify update`. 파일: `RequestWizard.tsx`·`app/page.tsx`·`AppShell.tsx`·`AGENTS.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-16_작업정리.md`.
- **최신 반영(2026-07-15):** **PHASE O-31** — 예상견적 의뢰 탭 채널 선택 2카드 하단에 **견적 절차 방법론** 신설(캡쳐 지시): 좌 visit=**Crisp-DM 7단계** / 우 quick=**KDD 6단계**, 각 카드 아래 구분 바+제목+번호 목록(`CHANNEL_METHODOLOGY` 데이터 맵 + `MethodologyList` 공용 렌더, 버튼 바깥 비인터랙티브). `AskUserQuestion` 3건 확정(우측 제목=채널명 교정 "무료 견적 신청 - 견적 절차 KDD 방법론" / `#` 제거 / 바로 마감). 캡쳐 이미지 2(사업소개 지시)는 O-30 기배포로 판정(라이브 마커 grep 검증). 게이트 `--pass`·빌드 0에러. 파일: `components/forms/RequestWizard.tsx`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-15_작업정리.md`.
- **최신 반영(2026-07-13):** **PHASE O-30** — 사업 소개 탭을 **사업모델 중심으로 재구성**(캡쳐 지시 "웹사이트 얼굴·간결·직관·시각 이동선"). 기존 문제→분석→안심 3단계 타임라인 폐지 → ①목적 서브카피 ②**무료(소액=자동화 툴 실적기반 무료)/소정 수수료(초과분=신뢰 예산 책정) 2단 모델** ③지금(전문가 컨설팅·AI DB 축적)→목표(AI Native 무료출장견적·의사결정서비스 ±5%) 성장 축 ④**신뢰 지표 클러스터(30년·246건·98.4%) → 단일 CTA**. `ui-ux-pro-max`(Social Proof-Focused) 선호출·권고(사회적 증거를 CTA 직전에) 반영, 2단 카드 편집형 인덱스+호버, 오렌지는 CTA 1곳만. 헤드라인 현행 유지. `AskUserQuestion` 3건(구조=재구성/요금=2단 명시/헤드라인=유지) 확정. 게이트 `--pass`·`graphify update`·빌드 0에러. 파일: `app/page.tsx`(`renderBusinessTab`). 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-13_작업정리.md`.
- **최신 반영(2026-07-11):** **PHASE P-1** — 전수 디버깅 감사(4축 병렬: 버그·보안·성능·UI) 후 **기본 데이터 보안 계층** 구축. 최우선 중대 결함 **C-1(테이블 RLS 전면 개방 → 공개 anon 키로 전 고객 PII 조회·수정·삭제 가능)** 차단: ①서버 데이터 게이트웨이 `app/api/data/route.ts`(service_role+신원검증: 관리자=전체/고객=본인건/익명=PII제거 분석행/쓰기=관리자/공개접수=OTP토큰검증 후 서버 단건채번) ②`lib/supabase/supabaseServer.ts` 신규 ③`client.ts` loadTable/persistTable를 게이트 호출로 우회(anon 직접쿼리 제거)+createEstimate 서버화 ④시크릿 fail-closed(OTP 랜덤폴백·관리자PW 미설정 503) ⑤RLS 잠금 SQL(`supabase-setup.sql §3` anon 정책 DROP) ⑥H-2 동반수정(sessionToken 저장·방문정보 createEstimate 통합). 빌드 0에러·`/api/data`(ƒ) 등록. **이어서 위임("작업을 완료하세요")으로 후속 5건도 완결**: C-2 서버 삭제 op(실 DB 반영)·C-3 CRM 멱등화(수주 전이 시에만)·H-5 OTP 레이트리밋(`rateLimit.ts`, 429)·월별 차트 동적화·KST 날짜 유틸(`lib/utils/date.ts`, 칸반 크래시 가드). **마감처리 완료 → master 배포(`5651ee9`).** ✅ **RLS 잠금 SQL 실제 실행·검증 완료(2026-07-11)** — Management API로 anon 정책 6→0 제거, RLS 6테이블 활성 확인, anon 조회 3행→0행 차단, 라이브 `/api/data` 200·PII 제거(customer_name/phone 공란)·분석필드 유지 확인 = Vercel service_role env 정상. 누출 닫힘. 상세: `docs/_worklog/2026-07-11_작업정리.md` PHASE P-1 · 불변식 `AGENTS.md §13`.
- **최신 반영(2026-07-09):** **PHASE O-25** — 견적검토 히어로(데스크톱) 캡쳐 4항목 + 리듬 보정. ①FLOW 헤더 부제 삭제 ②"견적 작업 FLOW" 21→**26px**(CAPEX 타이틀과 동일)+오렌지 바 h-5→h-6 ③우측 "사전 견적 후 결과 지표" `pt-2` 제거→좌측 CAPEX와 상단선 수평 정렬 ④benefit 불릿 15→16px + 리듬 보정(FLOW 제목→탭 `mt-3.5→mt-4`, benefit `ul mt-3.5→mt-4`로 좌우 대칭). `AGENTS.md` §10-A 조문을 새 확정값으로 갱신(2026-07-04 "21px·pt-2 하향" 반전 명문화). 게이트 `--pass`·빌드 0에러. 파일: `EstimateFlow.tsx`·`app/page.tsx`·`AGENTS.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-09_작업정리.md`.
- **최신 반영(2026-06-21):** **PHASE M** — 하네스 엔지니어링(폴더 정리·`SessionStart`/`Stop` 훅·슬래시커맨드 4종) + 고객화면 UI 정리(사이드바 미니멀 프로화·사업소개 CTA 2개 삭제·CAPEX 신뢰칩 도형→언더라인). 빌드 0에러 → master 배포 완료(`22c9e94`). 상세: `docs/_worklog/2026-06-21_작업정리.md`
- **최신 반영(2026-07-11):** **PHASE O-26** — 모바일 실적 상단 밴드의 `실적 분포` 제목과 KPI 4종을 히트맵처럼 한 줄 가로 스와이프로 전환해 세로 줄바꿈을 제거. 차트·히트맵·세부 실적·데스크톱 KPI 정렬은 유지하고, `AGENTS.md` 불변식·작업 기록을 동기화. 그래프 갱신·변경 파일 ESLint·프로덕션 빌드 통과. 커밋 `<해시 예정>` → master 배포. 상세: `docs/_worklog/2026-07-11_작업정리.md`.
- **최신 반영(2026-06-23):** **PHASE N** — 견적검토 히어로 공종별 시그니처 색 차별화(`LANDING_SIGNATURE_HEX`) + 사업소개 헤드라인 "BIZ모델_ 최소 범주대역(±5%)" 교체·오렌지 eyebrow 삭제 + 공종상세 "주요 견적공종" 인라인 + 좌측메뉴 "예상 견적총액별 방안"·"온라인 컨설팅" + 탭 상단 수평정렬 통일(py-3). 빌드 0에러 → master 배포(`86a1186`).
- **최신 반영(2026-06-27):** **PHASE O** — UI 전면 리디자인 반복(사업소개 히어로=문제→해결 + 단일 CTA + 헤드라인 품위 "흔들리는 견적을 데이터로 바로잡아, 최적의 예산을 책정합니다" / 실적 KPI=상하 헤어라인·누적건수만 오렌지 / 막대=공종 시그니처색 / 의뢰폼 3분할 / 견적검토 히어로 4분할) + **스킬 강제 하네스**(AGENTS.md §0 + 훅) + **실적 테스트표본 99건 Supabase 직접 시드**(est-test-*). 빌드 0에러 → master 배포(최종 `6ee126d`). **핵심 교훈을 AGENTS.md §8(캐시 검증)·§9(데이터=Supabase)·§10(확정 디자인 패턴)에 영구화.** 상세: `docs/_worklog/2026-06-27_작업정리.md`
- **최신 반영(2026-06-27, 세션 연장):** **PHASE O-2** — 공종 상세 "주요 견적공종" 밴드 폰트 상향(라벨=키워드 동일 22/24px, 한 단계 ↑)·가운데 간격 균형(gap-8) + 키워드 밴드 내용 정리(8공종 `TRADE_KEYWORDS` 수정·삭제) + 외주제작 메뉴 라벨 수정(SKID 제작설치 검토 / Structure제작,설치 검토 — menu.ts·LeftSidebar·홈카드). 빌드 0에러 → master 배포(`bebeb75`). 상세: `docs/_worklog/2026-06-27_작업정리.md` §5.
- **하네스 보강(2026-06-27):** "마감처리"에 **작업 기록 자동 갱신 단계** 신설(`.claude/commands/마감처리.md` step4 + AGENTS.md §6) — 이제 마감처리 시 worklog·state가 같은 커밋에 자동 포함(사용자가 매번 안 시켜도 됨).
- **최신 반영(2026-06-27):** 공종 상세 밴드 라벨 "주요 견적공종" → **WORK_ITEM**, 색 오렌지 → `#155EEF`(홈 "AI 분석"과 동일). 상세: worklog §5-4.
- **최신 반영(2026-06-28):** **PHASE O-3** — 사업소개 히어로 오렌지 강조 3곳("데이터로"·"안심 예산 대역 ±5%" 텍스트·막대·밑줄·푸터 "최적합 견적 검증")을 청색 `#155EEF`로 통일(CTA만 오렌지 유지) + 공종 상세 밴드 라벨 `WORK_ITEM` → **"주요 공종"** + 견적총액별 방안(small/medium/large `TRADE_KEYWORDS` 추가)에 동일 밴드 확장. `ui-ux-pro-max` 사후검증(대비 5.4:1 통과)·빌드 0에러. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-06-28_작업정리.md`.
- **최신 반영(2026-06-28):** **PHASE O-4** — 모바일 상단 헤더 정리 + 실적 KPI 압축. ①상단 "마이페이지" 칩 삭제(하단 탭 중복) ②AI NATIVE 배지 → 'AI Native 검증' SOP 링크화(`setActiveTab('sop')`) ③옆 녹색 닷 삭제 ④실적 집계표 설명 단락 삭제 ⑤KPI 보조 3종 라벨 단축(검토 비율/평균 소요/공종 수)+포지션 압축(숫자 30→23px, gap-x-9→6, 누적건수만 유지). 캡처 X표시를 "삭제"로 1차 오독→사용자 정정으로 ⑤는 "글자 단축·축소"로 재처리. 빌드 0에러. 파일: `AppShell.tsx`·`PerformanceInsights.tsx`. 커밋 `<해시>` → master 배포. 상세: worklog §2.
- **최신 반영(2026-06-28):** **PHASE O-5** — 서비스 소개(모바일) 레이아웃 3단계 흐름 개편 + 문장 함축. ①시안 3종(`show_widget`) 제시→사용자가 **개편안 B(문제→분석→안심 수직 타임라인)** 선택, `renderBusinessTab` 본문을 `flowSteps` 데이터 기반 타임라인으로 교체(아이콘 노드+연결선, ±5%는 '안심' 단계 1곳 지표) ②긴 설명 문장 6곳을 신뢰성·정직성 있게 함축(과장 어휘 제거·사실 위주). 컨셉(청색 데이터강조+오렌지 단일 CTA+푸터) 유지. `ui-ux-pro-max` 선호출·빌드 0에러. 파일: `app/page.tsx`. 커밋 `<해시>` → master 배포. 상세: worklog §3.
- **최신 반영(2026-06-28):** **PHASE O-6** — 서비스 소개(모바일) 히어로 압축. 캡처 X표시 지시대로 ①헤드라인 "바로잡아," 삭제→**2줄 고정**("흔들리는 견적을 데이터로"/"최적의 예산을 책정합니다") ②보조문장 "현장 30년 경험과 실거래 데이터로 검증합니다." 한 줄 삭제 ③컨테이너·타임라인 간격 축소(`gap-6 py-6`→`gap-5 py-5` / 타임라인 `gap-5 py-6`→`gap-4 py-5`)로 푸터 수평선이 한 화면에 노출. `ui-ux-pro-max` 선호출·빌드 0에러·시안 확인. 파일: `app/page.tsx`. 커밋 `<해시>` → master 배포. 상세: worklog §4.
- **최신 반영(2026-07-01):** **PHASE O-13** — 실적 대시보드 세련화. ①공종 색을 **작업 특성 기반 의미 팔레트**로(배관=블루·기계실=틸·장비=그래파이트·공장증설=앰버·노후교체=러스트·연결=인디고·CAPEX=네이비, 딥·뮤트 통일) ②막대·히트맵·세부카드 **3곳 동일 색 코디**(히트맵 각 행=공종 색 농도, 밝기 기반 글자색으로 대비 확보) ③막대 폴리시(격자·축 제거 + 끝 값 라벨 `LabelList`) ④세부 카드 상단보더·닷·%칩 공종색. `ui-ux-pro-max`(color·Executive Dashboard) 선호출·게이트 `--pass`·빌드 0에러·`show_widget` 3종. 스크린샷 옛 박스형 실적 히어로는 O-7에서 제거된 캐시 화면 확인. 파일: `components/PerformanceInsights.tsx`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-01_작업정리.md` §O-13. (히어로 KPI 추가 개선은 사용자 지시 대기)
- **최신 반영(2026-07-01):** **PHASE O-12** — 모바일 화이트 리디자인 + 정렬 통일 + SOP 데이터 파이프라인(검증단계 반영). ①실적 막대↔히트맵 색 통일(스틸블루 `#1E4D8C`) ②우측 예상견적 패널 자동열림 중지(공종/규모 선택 시 접힘, 검토 엣지탭으로만 — `AppShell`) ③모바일 메인 3섹션 다크네이비→화이트+네온 아이콘→스틸블루 단색+오렌지 CTA 하나("AI 도구 느낌" 제거, 템플릿 유지) ④히어로 정렬 언밸런싱 교정(좌측 단일축+eyebrow 파리티) ⑤**SOP를 표준 데이터 분석 6단계 파이프라인으로 재구축** — 수집→**데이터 검증·정제**→AI 판독·정형→모델링→실거래 벤치마킹·**이상치 검증**→**결과 검증**·검토서(방법론 칩+데이터 변환 노출, 오렌지=핵심 벤치마킹). `ui-ux-pro-max`(Trust&Authority) 선호출·`show_widget` 시안 다수·`AskUserQuestion` 방향 확정·게이트 `--pass`·빌드 0에러. memory `sop-infographic-must-reflect-real-data-pipeline` 신설(검증단계 필수). 파일: `app/page.tsx`·`PerformanceInsights.tsx`·`AppShell.tsx`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-01_작업정리.md` §O-12.
- **최신 반영(2026-07-01):** **PHASE O-11** — AI Native 검증 SOP를 **진짜 내림차순 깔때기 인포그래픽**으로 최적화. O-10에서 영어·엔진은 제거됐으나 funnel 폭 수렴(97→85%)·균일 폰트로 "그냥 6줄 리스트"였던 것을 ①폭 수렴 또렷(모바일 100→82%·데스크톱 100→60%, `items-center` 중앙정렬) ②폰트 리듬(핵심 단계 거품탐지·검토서만 22px) ③하이라이트 2곳(거품탐지 오렌지·검토서 네이비) ④보조설명 6~8자 함축·결과칩 제거 ⑤순번색 gray-light→gray(대비 4.5:1)로 재설계. 견적문의(캡쳐①)는 O-10 충족분 유지(범위 규율). `ui-ux-pro-max`(Funnel·Trust&Authority) 선호출·게이트 `--pass`·빌드 0에러·`show_widget` 시안 확인. 파일: `app/page.tsx`(`renderSopTab`). 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-01_작업정리.md`.
- **최신 반영(2026-06-30):** **PHASE O-10** — 캡쳐 2종 전수. ①AI NATIVE 검증 SOP를 **내림차순(funnel) 6단계 인포그래픽**으로 리디자인: 영어·DS역량/활용엔진(OpenCV·PyTorch)·다크 JSON 콘솔 전부 삭제, 한글 동사 키워드 단일화, 오렌지 핵심 3곳만, 주제·절차·결과지표 3블록 재정립(중학생 눈높이), 아이콘 절제. ②견적문의 자료등록을 **선택형 단계 탭 폼**으로 재구성: 헤더 제목·부제·하단문구 제거, 채널카드 버튼형 정리, visit/quick 각 3입력단계(①사업·현장+업종 신규 ②방문·연락/예상금액·연락 ③참조·자료) + **등록완료 탭→관리 페이지 이동**(`setShowMyRequests`). 가입정보 자동입력·진행바·`validateStep`·OTP/업로드/createSiteVisit 보존. `ui-ux-pro-max` 선호출·`show_widget` 시안 2종·`AskUserQuestion` 방향 2건 확정. 빌드 0에러·게이트 `--pass`. 파일: `app/page.tsx`·`components/forms/RequestWizard.tsx`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-06-30_작업정리.md` PHASE O-10.
- **최신 반영(2026-06-30):** **PHASE O-9** — 하네스·Loop 엔지니어링(검증 루프) 구축 + 견적문의 접근성 보강. ①Stop 훅 차단형 검증 루프 `.claude/hooks/ui-quality-gate.mjs`(변경 .tsx 채점, 차단룰 R1=에러 role=alert, 경고 R2 gray-light·R3 터치<44px, `--check`/`--pass`, 미통과 시 마감 차단·세션 3회 cap) + settings.json Stop 등록 ②기준파일 `design-system/zeros/MASTER.md`(ui-ux-pro-max --persist + AGENTS §10 오버라이드) ③규칙 명문화 3곳(전역 `~/.claude/CLAUDE.md`·`AGENTS.md §11`·바탕화면 `Hermer Workspace/`) ④견적문의 접근성 1~5 전수(role=alert/aria-live·뒤로버튼 44px+focus-visible·gray-light 4곳→gray·오전오후 min-h-44·reduced-motion). 게이트 0건·빌드 0에러·--pass. 파일: `RequestWizard.tsx`·`AGENTS.md`·`.claude/`·`design-system/`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-06-30_작업정리.md`.
- **최신 반영(2026-06-29):** **PHASE O-8** — 견적문의 탭 재설계(캡처 7개 항목 전수). ①상단 검은 설명 박스 삭제 ②로그인된 사용자는 바로 2채널 자료등록 화면 ③처음 사용자 OTP 인증 시 로그인 영속화(`handleVerified→setCustomerAuth`) ④출장요청 채널=자료+예약방문(날짜·시간) → `createSiteVisit` 이력 기록 ⑤무료견적 채널=100만↓ small/≤1,000만 자동분류 ⑥견적문의 탭=로그인 게이팅+2채널 허브 ⑦접수=zeros_estimates·예약=zeros_site_visits 동시 저장. `ui-ux-pro-max` 선호출·`show_widget` 시안 3화면·`AskUserQuestion`(2채널/예약폼 신규/기존 OTP 확정). 빌드 0에러. 파일: `RequestWizard.tsx`·`app/page.tsx`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-06-29_작업정리.md`.
- **최신 반영(2026-06-28):** **PHASE O-7** — 실적 집계표(모바일) 상단 압축. 캡처 상단 X표 2개 지시대로 ①제목 헤더("ZEROS 실적 집계표")+상단 헤어라인(X표시선) 삭제 → 그래프를 첫 화면 최상단으로 ②101 수치 폰트 절반(`clamp 52~72px`→`26~36px`, 건/밑줄 비례축소) ③컨테이너·KPI 패딩/간격 압축(`gap-5 py-3`→`gap-4 py-2`, 섹션 `border-t·border-b py-7`→`border-b pb-5`)로 막대그래프(8공종) 하단까지 첫 화면 노출. `ui-ux-pro-max` 선호출·빌드 0에러·모바일 시안 확인. 파일: `components/PerformanceInsights.tsx`. 커밋 `<해시>` → master 배포. 상세: worklog §5.
- **최신 반영(2026-07-04):** **PHASE O-19** — 캡쳐 4장 전수. ①사업소개 헤드라인 데스크톱 **한 줄**(`clamp 30~38px`+nowrap, 모바일 2줄 유지)+3단계 확대(제목 19px·설명 15.5px·±5% 52px·gap-7) ②견적검토 FLOW 폰트=하단 밴드 동기(21px/탭 15.5px)+우측 열 좌측 대칭(제목 26px·bullet 15px·세부 13/17.5px·세로폭 확장·하향) ③실적 누적 KPI **지배지표 복원**(라벨 15px·숫자 30px 한 줄 — O-18 23px 통일안 재지적으로 폐기, AGENTS §10-A 수정) ④SOP 카드 간격 8→14px. 게이트 `--pass`·빌드 0에러. 파일: `app/page.tsx`·`EstimateFlow.tsx`·`PerformanceInsights.tsx`·`AGENTS.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-04_작업정리.md` §O-19.
- **최신 반영(2026-07-04):** **PHASE O-18** — 실적 대시보드(데스크톱) 캡쳐 지시 2건 전수. ①타원(누적 진단 건수) = 라벨+숫자 **한 줄 가로배치**·보조 KPI와 동일 폰트(라벨 12px·숫자 23px, 밑줄 삭제·오렌지는 숫자에만) → 절약된 높이만큼 페이지 상향 ②막대 분포 차트 행 pitch를 **히트맵과 동기화**(`h-[224px]`→`h-[296px]`=8행×≈37px, 구 224px 규격 대체). AskUserQuestion 1회 확정(①보조 KPI 동일 ②그래프를 히트맵에 맞춤) 후 반영, AGENTS §10-A "실적 KPI 밴드·행간" 조문 신설 + 헌법 제7조(정밀 판독) 전 폴더 배포분 포함. `ui-ux-pro-max` 선호출·게이트 `--pass`·빌드 0에러·`show_widget` before→after. 파일: `components/PerformanceInsights.tsx`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-04_작업정리.md` §O-18.
- **최신 반영(2026-07-04):** **PHASE O-17** — 견적 검토 FLOW 폰트 상향 + 히어로 좌우 균형(우측 3단 구조) + 공종 실사 이미지 16장(Pexels, `public/images/trades/`) 연결. master 배포(`cc7f60f`). 상세: `docs/_worklog/2026-07-04_작업정리.md` §O-17.
- **최신 반영(2026-07-03):** **PHASE O-15** — 견적 검토(데스크톱) 히어로 재설계: 캡쳐 지시 "비직관적·나열적" 교정. 우측 `작업과정 01·02·03` 3줄 리스트 삭제 → **−34.2% 54px 대형 주인공** + `검토 전 100→최적합 65.8` before→after 바 그래프(지시③ benefit 그래프), 작업과정은 하단 한 줄 절차 캡션으로 압축. 좌측 공종 타이틀 26px 승격·키워드 `|`나열→한 줄 절제·실사 이미지 자리 크게 확보(지시①②, 플레이스홀더). 색은 공종 시그니처색 하나만. 하단 밴드 박스그리드→헤어라인 인라인 지표. `ui-ux-pro-max`(Trust&Authority)·`show_widget` A/B 시안·`AskUserQuestion`(A안 확정)·게이트 `--pass`·빌드 0에러. 파일: `app/page.tsx`(`renderReviewDesktop`). 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-03_작업정리.md`.

## 📈 전체 로드맵 및 상태
| PHASE | 단계 명칭 | 담당 역할 | 상태 |
| :--- | :--- | :--- | :--- |
| **PHASE 0** | 에이전트/워크스페이스 부트스트랩 | 오케스트레이터 | **[완료]** |
| **PHASE A** | 프로젝트 골격 및 패키지 설치 | 프론트/디자인 | **[완료]** |
| **PHASE B** | 랜딩 대시보드 셸 고급화 | 프론트/디자인 | **[완료]** |
| **PHASE C** | Supabase & 시드 데이터 설정 | 백엔드/DB | **[완료]** |
| **PHASE D** | 고객 플로우 (요청 폼 및 결과) | 프론트/디자인 | **[완료]** |
| **PHASE E** | 관리자 플로우 (접수 & 칸반 보드) | 프론트/백엔드 | **[완료]** |
| **PHASE F** | 실적관리 & 의사결정 위젯 | 백엔드/DB | **[완료]** |
| **PHASE G** | 고객관리 | 프론트/디자인 | **[완료]** |
| **PHASE H** | 통합 시나리오 테스트 & 검증 | QA/배포 | **[완료]** |
| **PHASE I** | 배포 가이드 및 패키징 | QA/배포 | **[완료]** |
| **PHASE K** | 모바일 시뮬레이터 및 예상견적 브랜딩 고도화 | 프론트/디자인/DB | **[완료]** |
| **PHASE L** | 최상단 박싱 규격 단일화 및 템플릿 정립 | 프론트/디자인 | **[완료]** |
| **PHASE J** | 최종 보고서 작성 및 프로젝트 이양 | 오케스트레이터 | **[완료]** |
| **PHASE M** | 하네스 엔지니어링 & 고객 UI 정밀 정리 | 오케스트레이터/프론트 | **[완료]** |
| **PHASE N** | 견적검토 시그니처색 + 사업소개/공종상세 정리 | 프론트/디자인 | **[완료]** |
| **PHASE O** | UI 전면 리디자인 + 스킬 강제 하네스 + 실적 Supabase 시드 | 오케스트레이터/프론트 | **[완료]** |

- **최신 반영(2026-07-02):** **PHASE P** — ①하네스 고도화: 성장 게이트 `.claude/hooks/growth-gate.mjs`(Stop: 기록 없는 마감 차단, PMIS·꽁아코스에도 이식) + `docs/OPTIMIZE.md`(표준 루프+개선 프롬프트) + AGENTS §11-2 영구화 ②**자동 견적서 1단계(반자동)**: `EstimateLineItem`/`line_items` 스키마 + 규칙 기반 초안 엔진(`lib/quote/quoteDraft.ts`, 공종 10종 표준 구성비) + exceljs 견적서 생성(살아있는 수식·±5% 대역, `lib/quote/quoteXlsx.ts`) + 관리자 모달 "AI 견적 초안·발송" 카드(편집 표+승인 게이트, `EstimateDetailModal`) + 발송 시 `quotes/` 업로드·`estimate_pdf_url`·상태 '견적서 송부완료' + 고객 마이페이지 "견적서 다운로드" 버튼(`MyRequestsView`). ui-ux-pro-max 선호출·시안 확정·빌드 0에러·게이트 `--pass`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-02_작업정리.md`.

- **마감(2026-07-02):** PHASE P E2E 검정(Playwright 전 단계·엑셀 수식 재검증) + 전 작업 9항목 검정 스윕 통과 + 4폴더 마감 완료(PMIS PR #7 머지 `ca502f0` · 꽁아코스 하네스 커밋 `2f09589` · Hermes 파일 기록). 상세: worklog §3~§4.

- **최신 반영(2026-07-02, 세션 연장):** **PHASE O-14** — 모바일 간결화(캡처 2장 전수). ①홈 랜딩 eyebrow 3곳 전부 삭제("AI NATIVE 견적 검증"·"AI NATIVE 정량 분석"·"AI 실시간 정량 분석" — 필요 이상 라벨 금지) ②프로세스 9단계: P1~P3 범례 한 줄 인라인 압축 + 중간 "고객 여정 타임라인" 라벨 삭제 + 단계 카드를 `번호+제목+주체칩 → 한 줄 설명 → 산출물 칩`으로 재구성(아이콘 박스·영문 이중 표기·Q&A 문장 삭제, desc 1문장 함축). AGENTS §10-A에 "eyebrow 금지"·"프로세스=간결·직관" 확정 패턴 신설. 빌드 0에러·게이트 `--pass`. 파일: `app/page.tsx`. 커밋 `<해시>` → master 배포. 상세: worklog §5.

- **최신 반영(2026-07-03, 세션 연장):** **PHASE O-16** — 캡쳐 3장 전수. ①SOP(데스크톱) eyebrow 삭제 + 상단 밀착 + 폭 `lg:max-w-4xl`·간격 압축으로 **한 화면 수납**(스크롤 흔들림 제거) ②견적 검토 히어로 좌측 실사 사진 **상·하 2분할**(현장 전경/작업 상세 플레이스홀더) + 우측 **"사전 견적 후 결과 지표"** 제목·benefit 3줄 신설(−N% 주인공 지표와 헤어라인 구분) ③실적 분포 차트 recharts → **레일형 에디토리얼 막대**(`N건 · N%` 직접 표기, 224px 규격 유지, recharts 의존 제거) + 히트맵 헤더·범례 대비 상향. 게이트 `--pass`·빌드 0에러. 파일: `app/page.tsx`·`PerformanceInsights.tsx`. 커밋 `<해시>` → master 배포. 상세: worklog PHASE O-16.

- **최신 반영(2026-07-04):** **PHASE O-17** — 캡쳐 전수 + 실사 이미지. ①견적 작업 FLOW 박스 내부 폰트 위계 상향(탭 14px·제목 18px·설명 14.5px·안내문 대비 교정) ②히어로 좌측 eyebrow·키워드 줄 삭제(X 지시) ③우측 패널 3단 구조(상단 제목+benefit / 중앙 −N% 주인공 / 하단 신뢰·절차 앵커)로 **좌·우 세로 균형** 확보 ④**공종 실사 이미지 16장**(Pexels 무료 라이선스) 리서치→전수 육안검증(부적합 4장 교체)→`public/images/trades/` 자체 호스팅→`TRADE_PHOTOS` 맵 연결(자사 사진 확보 시 파일 교체만). 게이트 `--pass`·빌드 0에러. 파일: `EstimateFlow.tsx`·`app/page.tsx`·`public/images/trades/`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-04_작업정리.md`.

- **최신 반영(2026-07-05):** **PHASE O-20** — 실적 화면 캡쳐 마크업 5건 전수. ①KPI "누적 진단 건수"→**"누적 견적 건수"**(도형·오렌지 숫자 유지) ②분리선 4곳에 **공종 8색 미니 스트립**(`TradeStrip`, 각 섹션 등장 순서 색 = 미니 범례)으로 다음 섹션 예고 ③히트맵 제목 행 삭제→**표 좌상단 코너 셀 내장**(2줄+캡션, 코너 132px) → 세로폭 절약으로 **하단 합계 행 스크롤 없이 노출** ④"셀 = 견적 건수" ⑤막대차트 제목 "공종별 견적 실적 분포"(화면 3곳 용어 통일, "프로젝트 사전진단"은 유지). AskUserQuestion 3건 사전 확정·게이트 `--pass`·빌드 0에러·`show_widget` 재대조. AGENTS §10-A "실적 화면 용어·히트맵 제목·분리선" 조문 신설. 파일: `components/PerformanceInsights.tsx`·`AGENTS.md`. 커밋 `7e667cf`(머지 `2ed7a36`) → master 배포. 상세: `docs/_worklog/2026-07-05_작업정리.md`.

- **최신 반영(2026-07-05):** **PHASE O-22** — 실적 상단 밴드 개편(캡쳐 4건). ①좌측 지배 KPI 블록 폐지 → 우측 **KPI 4종 균일 클러스터**(견적 건수·검토 비율·평균 소요·공종 수, 12/23px 네이비) ②"공종별 견적 실적 분포" 헤더 **최상단 행 이동**(좌 헤더+우 KPI 한 행, 세로폭 확보) ③히트맵 코너 = **"실적 히트맵" 단독 14px**(부제·캡션 삭제) ④표 행 pitch 37→30px 압축(+막대 296→240px 동기) → **합계 행 첫 화면 노출**. AGENTS §10-A 조문 2건 갱신. 게이트 --pass·빌드 0에러. 파일: `PerformanceInsights.tsx`. 커밋 `<해시>` → master 배포. 상세: worklog §O-22.
- **최신 반영(2026-07-05, 세션 연장):** **PHASE O-21** — **첨부파일 보안 잠금**(사용자: "아무나 열어보면 안 됨, 이력만 노출·관리자만 열람"). ①버킷 비공개 SQL(`supabase-setup.sql` §5) ②서명 URL API `/api/files/sign`(관리자 토큰=전체 / 고객 세션 토큰=본인 건만, 10분 만료, 레거시 공개 URL 호환) ③관리자 비밀번호 서버 이전(`/api/admin/login`, env `ZEROS_ADMIN_PASSWORD`) ④업로드는 `file_path`만 저장(공개 URL 발급 중단) ⑤열람 UI 전부 `openSecureFile()` 경유(관리자 모달·마이페이지). 빌드 0에러·게이트 --pass. **발효 조건(사용자 3단계): Vercel `SUPABASE_SERVICE_ROLE_KEY` 등록 → 배포 → SQL §5 실행.** 커밋 `37f7f9e`. 상세: `docs/_worklog/2026-07-05_작업정리.md` PHASE O-21.
- **최신 반영(2026-07-07):** **PHASE O-23** — 실적 화면 캡쳐 마크업 4건 전수. ①상단 밴드 헤더 = **"실적 분포" 단독**("공종별 견적"·"(건수·비중)" X표시 삭제) ②"실적 분포"·"실적 히트맵" **두 섹션 제목 폰트 = KPI 수치와 동일 23px**(견적 건수 "101"과 같게, 아이콘 20px 동반) ③우측 **"예상 견적범위" 패널 = 실적 탭 기본 숨김**(`AppShell` 자동숨김에 `performance` 추가, "검토" 엣지 탭 클릭 시에만 펼침) ④세부 실적 구간 **이중선 제거**(헤더 `TradeStrip`·카드 상단 `border-t-2` 컬러선 삭제, 상단·히트맵 스트립은 유지). ①·④ AskUserQuestion 1회 사전 확정·게이트 `--pass`·빌드 0에러. AGENTS §10-A 조문 신설(§151·154 대체 부분 명시). 파일: `PerformanceInsights.tsx`·`AppShell.tsx`·`AGENTS.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-07_작업정리.md`.
- **최신 반영(2026-07-07, 세션 연장):** **PHASE O-24** — 실적 분포·히트맵 공종 8색 스트립 코디(캡쳐 마크업 4건). ①막대차트 좌측에 **끊김 없는 하나의 수직 색선**(히트맵 좌측처럼, 행 전체 높이 `self-stretch`+행 균등분할 `flex-1`, 공종 8색 분절) ②막대 하단 가로 스트립(상단 헤더와 대칭) ③히트맵 하단 가로 스트립(히트맵 상단과 대칭) ④히트맵 밀도 범례("셀 색=적음→많음") **완전 삭제**(건수는 셀 툴팁 유지). 시안·`AskUserQuestion` 확정·게이트 `--pass`·빌드 0에러. **반복 교정 방지**: "팩트보다 지시 맥락 이해" 지시 → `memory/intent-context-over-literal-facts.md` 신설. 파일: `PerformanceInsights.tsx`. 커밋 `<해시>` → master 배포. 상세: worklog PHASE O-24.

- **최신 반영(2026-07-28):** **PHASE O-38** — 사업소개(서비스 소개) **모바일 한 화면 수납 + 문단 정돈**(캡쳐 지시 "무료 옆에 소액 예산 공사 배치해 세로폭 축소 / 이렇게 제공합니다 위·아래 안 맞음 / 내용 절반·여백 절반, 미술품 아님" + 후속 "한 화면에 나올 만큼만" · "문단을 깔끔하게 정돈"). 착수 전 실측 = 여백 50.3%·제목 상하 3.6:1·카드 169/171px·1.73화면·한글 글자 단위 절단. 조치 = ①배지+제목 한 줄 인라인(성장축 `지금`/`목표` 라벨도 동일) ②`break-keep`+`text-pretty`로 어절 단위 줄바꿈 ③`auto-rows-fr`+`mt-auto`로 두 카드 높이 동일·언더라인 하단 정렬 ④2열 전환 `sm:`→`md:`(640px 제목 깨짐) ⑤모바일 세로 예산 압축(`sm:` 이상 데스크톱 구값 명시 유지) ⑥셸 `pb-28`(112px 죽은 공간) → **business 탭만** `px-4 py-3`. 결과 = **412×870 scrollHeight 736 = clientHeight 736(스크롤 0)** · 카드 104.3/104.3 · 제목 상하 1.13:1·1.00:1 · 가로 스크롤 0(360~1366) · 데스크톱 1366×768 구값 유지·CTA 750.9≤768. 검증 = **Playwright DOM 실측**(프리뷰 탭은 rAF 정지로 사용 불가 — §8 오류 원장 조문화). AGENTS §10-A 조문 7건 신설 + 아카이브 폐지 3행. 게이트 `--pass`·빌드 0에러·tsc 0에러. 파일: `app/page.tsx`·`AppShell.tsx`·`AGENTS.md`·`agents-archive.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-28_작업정리.md`.
- **최신 반영(2026-07-12):** **PHASE O-28 + O-29** — ①(O-28) 실적 분포·히트맵 **공유 세로 그리드**(캡쳐 녹색선): 분포 공종 순서=히트맵(WORK_TYPES) 순서 고정·라벨 좌측 정렬(176px, 3px 색보더+pl-3)·건수/합계 열 104px 동기·두 섹션 폰트 13px 계열 한 단계 업. ②(O-29) 다항목 7건: FOOTER 소개란 사업소개→**예상견적 의뢰 탭 하단 이동**(`renderBrandFooter()` 공용화, 사업소개는 타임라인 flex-1 justify-center로 여백 균형 재배분) · SOP 헤드라인 **"DATA가 도면으로, 검증된 예산안이 되기까지"** 교체 + 최소 폰트 11→12px · `TRADE_KEYWORDS['unknown']` 추가(공사규모·금액 미정 상세 헤드라인 밴드 표시) · **휴대폰 뒤로가기 앱 닫기 확인 팝업**(AppShell 종료 가드 `zerosExitGuard`, 닫기/계속 48px 버튼) · OTP SMS 자동화는 코드 완비 확인(Solapi) — **로컬·라이브 모두 키 미설정(`enabled:false`)이 유일 잔여**(0-C 갱신). AGENTS §10-A 조문 2건 신설. 게이트 `--pass`·빌드 0에러. 파일: `PerformanceInsights.tsx`·`app/page.tsx`·`AppShell.tsx`·`AGENTS.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-12_작업정리.md`.
- **최신 반영(2026-07-11, 세션 연장):** **PHASE O-27** — 실적 분포 막대 세로폭 확대(캡쳐 지시 "히트맵 가로셀 폭과 균형, 좌우·상하 균형 원칙"). 막대 컨테이너 `h-[240px]`→**`h-[296px]`**(행 pitch 30→37px 복원) + 레일 `h-[14px]`→**`h-[18px]`** + 래퍼 `gap-2.5`→`gap-2`(합계 행 밀림 보상). AGENTS §10-A ② 조문을 새 확정값으로 대체(구 "합계 행 노출 우선 압축" 반전). 게이트 `--pass`·빌드 0에러. 파일: `PerformanceInsights.tsx`·`AGENTS.md`. 커밋 `<해시>` → master 배포. 상세: `docs/_worklog/2026-07-11_작업정리.md` PHASE O-27.

## 📝 다음 이어서 할 일 (Next Steps)
0-⭐. **(2026-08-08 · 최우선 · 코드 작업 아님 — 주인님 액션 2건) 지금 서비스가 못 하고 있는 것.**
> **Vercel 키 등록과 SQL 실행 5분. 지금 고객이 마이페이지에 못 들어가고, 관리자 로그인이 막혀 있다. 이건 코드 문제가 아니라 키가 없어서다.**

절차 전문 = **`docs/03_qa_deploy/prod-env-setup.md`**.
 - **① ~~`supabase-setup.sql` 실행~~ → ✅ 완료 (2026-08-08 11:35 · 주인님 실행 + AI 독립 실측 확인).** 재실행 불필요.
   - **독립 검증 10/11 PASS**: `file_size_limit = 52428800` ✅ · 접수번호 유니크 인덱스 존재 ✅ · 버킷 비공개 ✅ · 익명 업로드 200 ✅ · 익명 파일 읽기 400 ✅ · 익명 테이블 5종 전부 0행(service_role 대조군은 행 있음) ✅. 1건 FAIL 은 **점검 항목 자체의 결함** — AGENTS §15-11 참조.
   - **1차 실행분은 조용히 롤백됐었다**(11:10 실측 `file_size_limit = null`). 원인 = SQL Editor 가 전체를 한 트랜잭션으로 돌리는데 파일 마지막이 `update` 라 성공과 롤백이 화면상 구분되지 않았다 → **§8 자기검증 절**(마지막을 `select` 로) + `test/supabase/setupSql.test.ts` 기계 채점으로 봉인. AGENTS §15-9.
   - **Supabase 접속 계정 = GitHub `ihaelyong988-lab`**(비밀번호 로그인 불가 계정 — 소셜 버튼으로만). 프로젝트 `xtljznrfmythnnpeorgz`. SQL Editor 직링크: `https://supabase.com/dashboard/project/xtljznrfmythnnpeorgz/sql/new`
 - **② Vercel 키 등록 (심사 1~2일 + 등록 20분)** — solapi.com 가입 → **발신번호 사전등록(여기서 1~2일)** → API Key/Secret 발급 → `.env.local` 에 붙여넣고 `scripts\push-env-vercel.ps1` 실행(값이 채팅·화면을 거치지 않는다) → AI 에게 "재배포하고 검증해". `ZEROS_ADMIN_PASSWORD` 는 주인님이 정하시면 **지금 바로 가능**.
 - **✅ 보안 선행 조건은 전부 해소됐다** — 2026-08-07 A1(관리자 로그인 레이트리밋)·A2(파일 서명 소유권)·A3(접수 레이트리밋)로 "키 등록 순간 활성화되던 위험"이 닫혔다. **지금 등록해도 안전하다.**
 - ⚠ **키를 등록해도 상태 알림 문자는 나가지 않는다** — `sendSms` 호출부가 본인인증 1곳뿐이다. 화면 문구는 그 사실에 맞게 이미 교정됐고, 발송 경로 구축은 별도 작업.

0-◇. **(2026-08-08) 코드 최적화 ①②단계 완료 · ③단계 중단.** 소스 **16,635 → 14,345줄(−2,290, −13.8%)** · 테스트 **166 → 564개**. ③단계(분해)는 읽기 전용 정찰 Agent 3개 결과 **중단** — 화면을 쪼개면 얻는 건 "파일이 짧아짐" 하나인데, `pageSource.test.ts` 단언 2개가 **공허화**되고(초록불인데 아무것도 안 지킴) props 30개 배선이 필요하며 **숨은 교차 소비 3건**(특히 `HOME_STATS` 를 견적검토가 몰래 소비)이 있다. 판정·근거·"그래도 한다면"의 안전 절단선 = `docs/00_orchestration/최적화-계획서.md` **§5-A**. **재조사 금지 — 그 문서에 다 있다.**

0-◎. **(2026-08-07 · 착수 대기 — "작업 시작" 한 마디면 즉시 진행) 잔여작업 실행준비 완료.** 단일 착수 문서 = **`docs/00_orchestration/잔여작업-실행준비.md`**. 읽기 전용 재검증 6기가 잔여를 코드와 1:1 대조해 **32건 판정 → 중복 제거 26건 전부 「실재」**(해소·재현불가 0건). 실측 난이도 **L 2 · M 13 · S 11**, 픽셀 게이트 4건, **조문 개정 필요 2건**(B5 CTA 명칭 · 신뢰지표 246 — §10-A에 리터럴로 박혀 있음), **범위 해석 확정 1건**(C2 "모바일 메인화면"이 홈 랜딩만인지 전 탭인지). 추정이 틀렸던 것: C2 M→**L**(AppShell 23지점 + 연쇄 18) · C10 3→**5건** · C5 9→**12지점**(정규식이 임의값·주입 CSS를 놓침) · C6 "9파일"→**72곳 중 본문 30곳**(게이트 R2가 파일 단위라 오탐) · D1 M→**L**. **PLAN A** = 확정 3건 불필요분 23건 · 12기 · 3파도 · **2시간 10분** / **PLAN B** = 26건 전량 · 15기 · 4파도 · **2시간 50분**. D3(단건 쓰기 전환, L)은 별도 차수 권고. 실행 스크립트는 **`.claude/workflows/round4-residual.mjs`** 에 작성 완료 — 착수 시 `Workflow({scriptPath, args:{plan,wave}})` 한 번으로 기동되며 프롬프트에 실측 file:line 이 박혀 있어 재조사 불필요.
0-☆. **(2026-08-07 갱신) 4차 잔여 24건 + 신규 7건** — 단일 기준 문서 = `docs/00_orchestration/개선-작업계획서.md` **§8**(실행 기록은 §8-8). **PLAN A(치명 4 + 높음 11)는 2026-08-07 완료.** 착수 순서: **(1) §8-1 결정 ①②** 답변(공개 실적 지표 정책 · 모바일 홈 `#041B33` 조문 이행 — ③ 개인정보처리방침은 처리 완료) → **(2) B4 최우선**(B1이 `work_type`에 8종 밖 값을 저장하게 만들어 '검토 비율 100% 초과'가 잠재→활성) → (3) 나머지 B·C·D단계 + 결함대장 부록 B 7건. **✅ 아래 0-C(Vercel env 4종 등록)의 보안 선행 조건은 해소됐다** — 이제 등록해도 안전하다. **주의: 2026-08-06 조사분 중 「실측확인」 13건 외에는 여전히 적대 검증 미실시** — 착수 전 §14-1대로 실패 테스트로 먼저 고정해 재확인한다.
0-★★. **(2026-08-07 신설 · 주인님 액션) `supabase/supabase-setup.sql` §6 실행** — 접수번호 유니크 인덱스(`zeros_estimates_no_uniq`). 실행 전에도 코드는 정상 동작한다(재시도 로직이 무해). 중복 확인 쿼리를 SQL 주석에 동봉했다.
0-★. **(2026-07-24) 프로젝트 폴더가 OneDrive 밖 `C:\dev\zerospipe.co.kr`로 이전됨** — 이 state.md를 여기서 읽고 있다면 이전이 반영된 것. 배경: OneDrive가 재생성물(.next·node_modules) 3.6만 파일을 동기화해 빌드 지연·EPERM 위험. A안(.next 삭제+git gc, 608MB 회수)→B안(junction, **Turbopack 비호환으로 폐기**)→**C안(폴더 이전, 신위치 빌드 검증 완료)**. 상세: `docs/_worklog/2026-07-24_작업정리.md`. **남은 handoff**: 구 OneDrive 원본 폴더는 신위치 실동작 확인 후 삭제(소스=GitHub·개인파일=신위치 복제됨). **미결 스레드**: 방문자 관점 MECE 정찰 = **완료**(42에이전트·발견 93건·적대검증). 계획 초안 저장됨 → `docs/_worklog/2026-07-24_방문자정찰_계획초안.md`(원본 `..._원본결과.json`). **재개 지점: 이 초안을 주인님께 보고→승인/우선순위 조정→승인범위만 Workflow 병렬 실행→검증→마감.** 가장 큰 구멍 3: ①B-1 테스트 인증번호 화면노출로 타인 PII·도면·견적서 유출(P0 최우선) ②견적검토·실적 탭 신청 CTA 부재+선택 URL 미기록으로 의뢰 도달 차단(P1) ③SSR 스플래시+에러바운더리 전무로 백지화·Pretendard 미로드(P0/P1).
0-A. **파일 보안 잠금 = 발효 완료(2026-07-05)** — Vercel `SUPABASE_SERVICE_ROLE_KEY` 등록 확인(실서버 sign API 403 응답으로 증명) + 버킷 비공개 전환을 Storage API로 원격 실행(`public: false` 확인) + 실서버 관리자 로그인→서명 파이프라인 검증 통과·공개 URL 400 차단 확인. **잔여(선택) 1건**: 구 `anon_modify_estimate_files`(익명 update 허용) 정책 제거 — Supabase SQL Editor에서 `drop policy if exists "anon_modify_estimate_files" on storage.objects;` 1줄 실행 권장(REST로 불가, 정책 변경은 SQL만 가능).
0-B. (구) **(발효 대기) 파일 보안 잠금 활성화** — 사용자: ①Supabase service_role 키를 Vercel+`.env.local`의 `SUPABASE_SERVICE_ROLE_KEY`에 등록 ②배포 확인 ③Supabase SQL Editor에서 `supabase-setup.sql` §5 실행(버킷 비공개). 실행 전까지 기존 공개 URL은 계속 열림. 잠금 후 관리자 재로그인 1회 필요.
0. **(보류 중) 휴대폰 앱 — 긴 프로젝트 제목 2줄 분리 이슈**: 제목 길이 무관 최상단 1줄 노출 요청. 대상 화면(검토 패널/공종 상세 본문/관리자 상세 모달/마이페이지 카드)과 동작 방식(말줄임 vs 상단고정 2줄허용) 사용자 확인 대기.
0-C. **(주인님 1순위 액션) 프로덕션 env 4종 등록 — 코드 완비, 키만 남음(2026-07-16 재실측·가이드 완비)**: ①SOLAPI 3종(`SOLAPI_API_KEY/SECRET/SENDER`) — 문자 인증 실발송(현재 테스트 모드·게이트 생략) ②**`ZEROS_ADMIN_PASSWORD` — 현재 Vercel 미설정으로 프로덕션 관리자 로그인 503 불가(2026-07-16 프로브 발견)** ③`OTP_SERVER_SECRET` Vercel 등록 확인(로컬엔 있음, 미등록 시 30일 세션 인스턴스별 무효). 절차·검증법 = **`docs/03_qa_deploy/prod-env-setup.md`**(Solapi 가입→발신번호 심사 1~2일→키 발급→Vercel 등록→재배포). **등록 자동화(2026-07-16 O-36)**: 값을 `.env.local`에 붙여넣고 `scripts/push-env-vercel.ps1` 실행(최초 1회 `vercel login`+`vercel link`) — 값이 채팅·AI를 거치지 않음. 등록 후 판정: `node scripts/prod-probe.mjs` 3/3 PASS + 실기기 문자 수신 1회.
1. **(우선) 사용자 기기 캐시 해소 확인** — **2026-07-05 원격 판정 완료: 배포 정상**(라이브 청크에 O-20 신규 문구 존재·구 문구 0건, AGENTS §8 원격 판정법). "변화 없음"의 원인 = 기기 캐시. 조치: 시크릿 창(Ctrl+Shift+R) 또는 설치형 PWA **완전 종료 후 2회 실행**(1회차 kill-switch SW가 캐시 정리 → 2회차 새 화면). 그래도 구화면이면 보이는 헤드라인 문구 1줄+주소 확인(§8). 그 외 **PHASE O-8 견적문의 새 동선**(2채널 선택→예약방문) 실기기 확인 + OTP 서버 설정 여부(③ 로그인 게이트 작동 조건) 점검.
2. est-test-* 테스트행 실런칭 전 일괄 삭제: `delete from zeros_estimates where id like 'est-test-%'`.
3. ~~견적검토 실사 사진 연결~~ → **완료(2026-07-04 O-17 최초 연결 → 2026-07-17 O-37 전면 재선정: 무인물·TIG 기준)**. 남은 후속: 자사 현장 사진 확보 시 `public/images/trades/` 파일 교체(경로 유지, 무인물·TIG 기준 준수).
4. 2차 Toss Payments 실서버 연동 / 알림톡 SMTP 실서버 연동.
5. AI 도면 비전 스캐너 모델 고도화.
6. ~~자동 견적서 1단계(반자동)~~ → **완료(2026-07-02 PHASE P)**. 남은 후속: ⓐ배포 후 관리자 화면에서 "AI 초안 생성→승인·발송" 실사용 1회 테스트 ⓑ2단계 Edge Function + Claude API 도면 판독(`docs/OPTIMIZE.md` P2).

