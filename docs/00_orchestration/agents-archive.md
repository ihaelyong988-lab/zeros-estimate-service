# AGENTS.md 아카이브 — 구 조문·개정 이력 (2026-07-16 현행화 시 분리)

> **목적**: `AGENTS.md`가 누적 개정으로 비대해져 2026-07-16 "md 지침 기록 정리" 지시로 **현행 규칙만 본문에 남기고**, 폐지·대체된 구 조문과 개정 서사를 이 파일로 이동했다.
> **사용법**: 어떤 UI 패턴을 "복원"하고 싶어질 때 — 먼저 여기서 그 패턴이 **의도적으로 폐지된 것인지** 확인한다(폐지 조문 복원 = 반복금지 헌법 위반). 현행 규칙은 항상 `AGENTS.md` §10-A가 정본.
> **무손실 보증**: 현행화 직전 전체 원문 = git `e32c6c9` (master, 2026-07-16). 아래 스냅샷은 재작성된 조문의 원문 전문이다.

## 1. 개정 연대기 (무엇이 언제 폐지·대체되었나)

| 폐지·대체된 구 조문 | 시점 | 최종 확정값 (현행 §10-A) |
|---|---|---|
| 사업소개 히어로 오버사이즈 헤드라인 `clamp ~50px` | 2026-07-09 O-25 | 데스크톱 한 줄 `clamp(30px,3vw,38px)`+nowrap, 모바일 2줄 |
| 사업소개 Footer(`mt-auto`+`min-h-[calc(100vh-128px)]` 하단 앵커+한 화면) | 2026-07-12 | 사업소개 탭 Footer 삭제 — request 탭 전용 |
| 사업소개 "문제→분석→안심" 3단계 타임라인(+O-25 확대: 제목 19px·설명 15.5px·±5% 52px·gap-7 / 2026-07-12 여백 배분: `flex-1 justify-center`·gap-9·py-8·루트 gap-6·py-6·CTA pb-5) | 2026-07-13 O-30 | 타임라인 자체 폐지 → 사업모델 중심 구조(2단 모델·성장 축·신뢰 지표→CTA) |
| 실적 KPI 누적 건수만 오렌지(펄스 점+숫자+짧은 룰) | 2026-07-05 | KPI 전부 무채색 — 오렌지·펄스 닷 금지 |
| 실적 좌측 지배 KPI 블록(누적 견적 건수 크게) | 2026-07-05 | 폐지 — 한 행 헤더+KPI 4종 클러스터 |
| 실적 상단 헤더 문구 "공종별 견적 실적 분포 (건수·비중)" | 2026-07-07 O-23 | "실적 분포" 단독 |
| 히트맵 코너 문구 "공종 × 견적규모" 부제·"(셀 = 견적 건수)" 캡션 → "실적 히트맵" 단독 14px | 2026-07-05 → 2026-07-07 | "실적 히트맵" 단독 + 섹션 제목 `text-[23px]`·아이콘 w-5 h-5 |
| 막대 컨테이너 h-[240px]/레일 14px(합계 행 첫 화면 노출 우선 압축) | 2026-07-11 | h-[296px](행 pitch 37px)/레일 h-[18px] — 히트맵 셀과 균형 우선 |
| 분포 차트 건수 내림차순 정렬 | 2026-07-12 | WORK_TYPES(히트맵) 순서 고정 |
| 실적 두 섹션 기본 폰트 12/11px | 2026-07-12 | 13px 계열(라벨 13·건수 14·캡션 12) |
| 세부 실적 카드 상단보더 = 공종색(`border-t-2`) + 세부 구간 TradeStrip | 2026-07-07 O-23 | 이중선 → 삭제, 색 코딩은 닷·%칩만(상단 분포·히트맵 TradeStrip은 유지) |
| FLOW 제목 "하단 다크 밴드 동기 21px" + 부제 "단계를 누르면…" | 2026-07-09 O-25 | 26px(CAPEX 타이틀 동일)+오렌지 바 h-6, 부제 삭제 |
| 히어로 우측 열 제목 `pt-2` 살짝 하향 / benefit 불릿 15px | 2026-07-09 O-25 | pt 제거(상단선 정렬) / 16px |
| AI NATIVE 헤드라인 "도면 한 장이…" | 2026-07-12 | "DATA가 도면으로, 검증된 예산안이 되기까지" |
| Footer 브랜드 소개(ZEROS 워드마크+태그라인+설명문)·"핵심 영역" 3줄 블록 / 폭 max-w-5xl·max-w-3xl / 고정 `mt-10` | 2026-07-16 O-32 | © 저작권 바 단독 · `max-w-3xl lg:max-w-4xl` · `mt-auto pt-6` 하단 앵커 |
| 채널 카드 공용 박스(위저드 루트 border/shadow) · 방법론 긴 문장 설명 | 2026-07-16 O-32 | 카드별 독립 패널 · 설명 핵심 키워드 압축(≈11~14자 한 줄) |
| 좌측 메뉴 라벨 접미어 "검토"(4종)·"미정"(1종) | 2026-07-16 O-33 | 접미어 삭제(표시명만, 데이터 키 불변) |
| 모바일 랜딩 eyebrow 라벨("AI NATIVE 견적 검증"·"AI NATIVE 정량 분석"·"AI 실시간 정량 분석") | 2026-07-02 | 전부 삭제 — eyebrow 금지 조문으로 일반화 |

## 2. 원문 스냅샷 (현행화로 재작성된 조문의 이동 전 전문)

### 2-1. §7 구 제목
```
## 7. 오늘 확정된 구조 (2026-06-20)
```
(내용 4개 불릿은 전부 현행 유지 — 제목만 "확정 화면 구조 — 견적검토"로 개칭, 우측 패널 불릿에 §10-A 확장 각주 추가.)

### 2-2. §10 구 조문 (사업소개 히어로 · 실적 KPI)
```
- **사업소개 히어로**: eyebrow 없음 · **오버사이즈 헤드라인**(`clamp ~50px`, `font-extrabold`=800; 900은 과해서 품위↓) · **accent(오렌지)는 키워드 한 곳만** · 본문 `max-w-[560px]`로 줄길이 균형(가독 65~75자) · **단일 CTA "무료 견적 검토 신청"** · Footer는 `mt-auto`+`min-h-[calc(100vh-128px)]`로 **하단 앵커+한 화면**(안전여백 둬 미세 스크롤 방지) · 헤드라인 카피는 **격식 어미("…책정합니다")**, 구어체("들쑥날쑥/남깁니다") 지양.
- **실적 KPI**: 박스 없이 **상·하 헤어라인만** · 누적 건수만 오렌지(펄스 점+숫자+짧은 룰) · 나머지 무채색(navy/gray) · 막대그래프=**공종 시그니처 색**(`TRADE_COLORS`, `fillOpacity 0.9`).
```

### 2-3. §10-A 이동 전 전문 (개정 서사 포함 원문 — 2026-07-16 현행화 직전)
```
### 10-A. 확정 패턴 추가 (2026-07-01 · PHASE O-12/O-13 — 여러 번 반복 교정받아 영구화)
> 근거: `ui-ux-pro-max`(Trust & Authority · Executive/Data-Dense Dashboard). 아래는 **반복 교정 방지용 확정 규칙**이니 다음 작업 때 재추론·재질문 없이 따른다.

- **모바일 메인화면 = 화이트 배경(고정).** 다크 네이비 배경(`#041B33` 등)·네온 아이콘색(sky/indigo/emerald)은 **"AI 도구 느낌" → 금지.** 배경 `#FFFFFF` · 헤드라인/본문 네이비 `#0F1E35`/그레이 `#5B6573` · 아이콘 **스틸블루 `#1E4D8C` 단색** · 카드 화이트+헤어라인 `#E4EAF2` · 주 CTA **오렌지 솔리드 `#E0701A` 하나만**. (템플릿·레이아웃은 유지, 컬러만 교체.)
- **정렬 = 좌측 단일 축(고정).** 한 섹션 안에서 헤드라인만 가운데+본문 좌측 같은 **혼합 축 금지.** 모든 섹션을 동일 좌측 기준선으로. 세로는 중앙(`justify-center`) 허용.
- **필요 이상 라벨(eyebrow) 금지(2026-07-02 사용자 지시 — "이런 필요 이상의 것 삭제").** 모바일 랜딩 제목 위 소문자 트래킹 라벨("AI NATIVE 견적 검증"·"AI NATIVE 정량 분석"·"AI 실시간 정량 분석") 전부 삭제됨. **제목과 의미가 중복되는 장식 라벨을 새로 추가하지 않는다** — 섹션은 eyebrow 없이 제목부터 시작(좌측 축은 유지).
- **서비스 프로세스 9단계 = 간결·직관(2026-07-02 확정 — 사용자 "나의 컨셉").** ①P1/P2/P3 국면 범례는 **한 줄 인라인**(세로폭 낭비 금지) ②단계 카드는 `번호+제목+주체칩 한 줄 → 한 줄 설명 → 산출물 칩`만 — **아이콘 박스·영문 이중 표기(REQUEST INTAKE 등)·Q&A 문장 금지** ③설명은 1문장 함축. 단계형 안내 화면 전반에 동일 기준 적용.
- **차트/실적 공종 색 = "작업 특성 기반" 의미 팔레트(고정).** 단색으로 flatten 금지, 무지개(고채도 8색)도 금지 — **딥·뮤트 톤**으로 통일하되 공종 성격에 맞춘 의미색: 배관/유체=블루 `#1D6E93` · 유틸리티=스틸블루 `#3B7CA8` · 기계실/HVAC=틸 `#1E7A72` · 금속 장비=그래파이트 `#566270` · 건설/증설=앰버 `#B5762A` · 노후/부식=러스트 `#9E4B2C` · 라인 연결=인디고 `#4A56A6` · 자본/검토=딥네이비 `#1E3A5F`(`TRADE_COLORS` in `PerformanceInsights.tsx`). **막대·히트맵·세부카드 3곳이 같은 색 체계로 코디**(히트맵 각 행=자기 공종 색 농도, 카드 상단보더·닷·%칩=공종 색). 히트맵 셀 글자색은 **밝기(luminance) 기반**으로 산출해 어떤 공종 색이든 대비 확보. 막대는 격자·축 제거 + **끝 값 라벨(`LabelList`)** 로 정리.
- **실적 상단 밴드·행간(2026-07-05 최종 — 구 지배지표 조문 대체).** ①좌측 지배 KPI 블록(누적 견적 건수 크게) = **폐지**(사용자 X 마크업·도형 샘플 확정). 최상단 한 행 = **좌 "공종별 견적 실적 분포 (건수·비중)" 헤더 + 우 KPI 4종 균일 클러스터**(견적 건수·검토 비율·평균 소요·공종 수, 라벨 12px·숫자 23px 네이비 — 오렌지·펄스 닷 없음). ②막대 차트 세로폭(2026-07-11 갱신 — 캡쳐 지시 "히트맵 가로셀 폭과 균형"): 막대 컨테이너 **`h-[296px]`(행 pitch 37px)·레일 `h-[18px]`** — 히트맵 가로셀(폭 ≈190px)의 덩어리감과 좌우·상하 균형이 우선(구 2026-07-05 "합계 행 첫 화면 노출 우선 h-[240px]/레일 14px" 압축 대체, 밀림은 섹션 gap 소폭 회수로 보상). 히트맵 셀 `py-1.5`는 유지.
- **견적검토 히어로·FLOW 상하 균형(2026-07-04 확정 · 2026-07-09 PHASE O-25 갱신).** ①FLOW 스트립 제목 = **좌측 CAPEX 타이틀과 동일 26px**(오렌지 세로바 h-6 동반) + **부제 삭제**(구 "하단 다크 밴드 동기 21px·부제 '단계를 누르면…'" 대체 — 2026-07-09 캡쳐 지시), 탭은 15.5px 유지 ②히어로 우측 열 제목 "사전 견적 후 결과 지표" = **좌측 CAPEX 타이틀과 상단선 정렬**(pt 제거 — 구 "pt-2 살짝 하향" 철회) · 동일 26px, benefit 불릿 = **16px**(구 15px, 본문 ≥16px 규칙 충족), 그래프 라벨·세부 지표는 13~17.5px로 좌우 대칭 + 세로 여백 넓게(pt-5·mt-4) — 우측만 잘게 두지 않는다. ③사업소개 헤드라인 = 데스크톱 **한 줄**(`clamp(30px,3vw,38px)`+nowrap, 모바일 2줄 유지), 절약분은 3단계 타임라인 확대(제목 19px·설명 15.5px·±5% 52px·gap-7)에 배분.
- **실적 화면 용어·히트맵 제목·분리선(2026-07-05 확정, PHASE O-20).** ①실적 화면 표기 = **"견적"**("진단" 금지 — 단 견적규모 카테고리명 "프로젝트 사전진단"은 서비스 명칭이라 유지): 누적 견적 건수 · 공종별 견적 실적 분포 · 셀 = 견적 건수. ②히트맵 제목 = **표 좌상단 빈 코너 셀 내장**(별도 제목 행 금지 — 세로폭 절약으로 하단 합계 행이 스크롤 없이 보이게 하는 것이 의도). **2026-07-05 재확정: 코너 문구 = "실적 히트맵" 단독 14px**("공종 × 견적규모" 부제·"(셀 = 견적 건수)" 캡션 삭제 — 사용자 취소선 마크업). ③섹션 분리선 = 회색 헤어라인 좌측에 **공종 8색 미니 스트립**(`TradeStrip`, 각 섹션 등장 순서 색)으로 다음 섹션을 예고 — 전부 같은 회색 분리선 금지.
- **실적 화면 제목·우측패널·이중선(2026-07-07 확정, PHASE O-23 — 사용자 캡쳐 마크업).** ①상단 밴드 헤더 = **"실적 분포" 단독**("공종별 견적"·"(건수·비중)" 삭제 — §152 구 문구 "공종별 견적 실적 분포 (건수·비중)" 대체). ②"실적 분포"·"실적 히트맵" **두 섹션 제목 폰트 = KPI 수치와 동일 `text-[23px]`**(견적 건수 "101"과 같게, 아이콘도 `w-5 h-5` 동반 — §154 "코너 문구 단독 14px" 대체). ③**우측 "예상 견적범위" 패널 = 실적(`performance`)·예상견적 의뢰(`request`) 화면 기본 숨김** — `AppShell` 자동숨김 useEffect 조건에 performance·**request**(2026-07-16 추가) 포함, 우측 "검토" 세로 엣지 탭 클릭 시에만 펼침(문맥에 안 맞아 기본 접힘). **의뢰 탭 누락 시 이전 탭 상태가 남아 패널이 열린 채 보임 — 조건에서 빼지 말 것.** ④**세부 실적 구간 "이중선" 제거** — "공종별 세부 실적" 헤더 컬러 스트립(`TradeStrip`)·각 카드 상단 2px 컬러선(`border-t-2`) 삭제(회색 헤어라인 위 컬러선 = 눈 피로), 카드 색 코딩은 닷·%칩으로 유지(§151 "카드 상단보더=공종색" 부분 철회). **상단 분포·히트맵의 `TradeStrip`은 유지**(§154 ③ 잔존, 세부 실적 구간만 예외).
- **AI Native / SOP = 실제 데이터 분석 파이프라인(고정).** 얕은 6단어 키워드로 단순화 금지 — 표준 데이터 분석 생애주기(수집 → **데이터 검증·정제** → 전처리 → 모델링 → 분석·**이상치 검증** → **결과 검증** → 전달)를 반영하고, 각 단계에 방법론 칩(Vision AI·Feature Engineering·Z-score 이상치·신뢰구간 ±5%)과 데이터 변환(io)을 노출. **데이터 검증 단계 누락 = 실패**(재지적받음). 코드/다크콘솔이 아닌 깔끔한 인포그래픽으로. 상세 memory: `sop-infographic-must-reflect-real-data-pipeline`.
- **실적 분포·히트맵 = 공유 세로 그리드 + 13px(2026-07-12 확정 — 캡쳐 녹색선 마크업).** ①분포 차트 공종 순서 = **히트맵(WORK_TYPES) 순서 고정**(건수 내림차순 폐지 — "행렬을 직관적으로 맞춤"; 막대 최대값은 `Math.max(...values)`로 별도 계산, `distribution[0]` 최대값 가정 금지). ②두 섹션이 같은 세로선을 탄다: **라벨 열 176px**(3px 공종색 보더 + `pl-3` **좌측 정렬** — 우측 정렬 금지, 히트맵 코너 제목 실폭 기준) · 레일 시작 = 히트맵 셀 시작 · **건수/합계 열 104px** — 분포 행 grid `[176px_1fr_104px]`, 히트맵 코너 th `w-[176px] min-w-[176px]`·합계 th `w-[104px]`. ③실적 두 섹션 기본 폰트 = **13px 계열로 한 단계 업**(분포 라벨 13·건수 14·'건'/% 12, 히트맵 표 전체 13 — 구 12/11 폐지). 세부 실적 카드의 내림차순·폰트는 범위 밖(유지).
- **Footer 위치·사업소개 균형·SOP 카피·종료 가드(2026-07-12 확정 — 다항목 지시).** ①FOOTER = **예상견적 의뢰(request) 탭 중앙패널 하단**이 유일 위치(`renderBrandFooter()` 공용 렌더) — **사업소개 탭에서는 삭제됨, 재추가 금지.** **2026-07-16 PHASE O-32 갱신:** 브랜드 소개(ZEROS 워드마크+"최적합 견적 검증" 태그라인+설명문)·"핵심 영역" 3줄 블록은 **삭제** → footer = **© 저작권 바 단독**(저작권·개인정보처리방침·문의하기, `border-t-2 border-navy pt-4`). 위치 = **`mt-auto pt-6` 하단 앵커**로 좌측 사이드바 하단 레벨에 정렬(초기 `mt-10` 고정은 footer가 중앙에 떠 213px 위로 뜨는 오류 → 하단 앵커 복원). 폭 = **`max-w-3xl lg:max-w-4xl`**(AI Native/SOP 탭 `renderSopTab` 컨테이너와 동일 — 탭 간 패널 폭 일치, 구 max-w-5xl·max-w-3xl 대체). 채널 카드 컨테이너(`RequestWizard` 루트 channelView)도 **동일하게 `max-w-3xl lg:max-w-4xl`**. **브랜드/핵심영역 재추가 금지.** ②사업소개는 Footer 여백을 언더라인 이하에 배분: 3단계 타임라인 섹션 = `flex-1 justify-center` + 스텝 gap-9·py-8, 루트 gap-6·py-6, CTA pb-5. ③AI NATIVE 검증 헤드라인 = **"DATA가 도면으로, 검증된 예산안이 되기까지"**(navy + accent span "검증된 예산안" — 컬러 체계 유지, 구 "도면 한 장이…" 폐지) · SOP 화면 최소 폰트 = **12px**(방법론 칩·io 라벨, 11px 금지). ④`TRADE_KEYWORDS`에 `'unknown'` 포함(규모 산정·예산 방향·전문가 상담) — 공사규모·금액 미정 상세도 상단 헤드라인 밴드 표시, **견적규모 4종 모두 키 누락 금지.** ⑤휴대폰 뒤로가기 = AppShell **종료 가드**(진입 엔트리를 `zerosExitGuard`로 치환+동일 URL 한 층 push) → 스택 바닥 popstate 시 "앱을 닫으시겠습니까?" 팝업(닫기/계속 사용, 48px 버튼) — popstate 핸들러에서 가드 state는 applyUrlState 태우지 말 것.
- **모바일 실적 상단 밴드 수평 스와이프(2026-07-11 확정).** 휴대폰에서는 `실적 분포` 제목과 KPI 4종(견적 건수·검토 비율·평균 소요·공종 수)을 줄바꿈하지 않는다. 히트맵과 동일하게 내부 `overflow-x-auto no-scrollbar`로 한 줄 가로 이동만 허용해 세로폭을 늘리지 않으며, 차트·히트맵·스트립·데스크톱 KPI 우측 정렬은 변경하지 않는다.
- **좌측 메뉴 라벨 = 접미어 없이 간결(2026-07-16 확정, PHASE O-33 — 캡쳐 X 마크).** 메뉴 표시명에서 필요 이상 접미어 삭제(이유: 설명문이 길고 오해 소지): `CAPEX개선,증설`(~~검토~~) · `배관 SPOOL Module`(~~검토~~) · `SKID 제작설치`(~~검토~~) · `Structure제작,설치`(~~검토~~) · `공사규모·금액`(~~미정~~). 단일 소스 = `lib/constants/menu.ts` `MENU_DISPLAY_NAMES` + 하드코딩 3곳(LeftSidebar 로컬 label·AppShell 모바일 드로어·page.tsx 홈카드·PerformanceInsights) 동기. **데이터 키(`'CAPEX 개·증설 검토'` WorkType/menu value)는 DB 저장 키라 불변** — 표시 문자열만 수정. '공장증설 검토'·'온라인 간편검토'는 X 없음 → 유지(임의 확대 금지). 접미어를 되살리지 말 것.
- **의뢰 탭 채널 카드 하단 = 견적 절차 방법론(2026-07-15 확정, PHASE O-31).** 예상견적 의뢰 채널 선택 화면의 각 카드 아래에 구분 바+방법론 번호 목록을 상시 노출: 좌 visit=**Crisp-DM 7단계**(작업 이해→데이터 이해→데이터 준비→AI모델링→평가→전개→고객 제공) / 우 quick=**KDD 6단계**(데이터→전처리→변환→마이닝→평가→고객 제공). 콘텐츠는 `CHANNEL_METHODOLOGY` 데이터 맵(`RequestWizard.tsx`)으로 분리, 렌더는 `MethodologyList` 공용 1곳(중복 금지)·**카드 `<button>` 바깥** 비인터랙티브. 제목은 카드 제목과 동기("견적·출장요청 자료등록 - …Crisp-DM…" / "무료 견적 신청 - …KDD…"), `#` 접두 금지. 캡쳐의 검은 굵은 테두리류 = 마크업 프레임이지 렌더 대상 아님. **2026-07-16 PHASE O-32 레이아웃 갱신:** 채널 선택 화면은 **공용 박스(위저드 루트 border/shadow) 제거** → 루트 `channelView`일 때만 `max-w-3xl lg:max-w-4xl mx-auto`(테두리 없음, AI Native/SOP 패널과 동일 폭), 각 카드 열 = **독립 패널**(`rounded-custom border border-border bg-bg p-5 md:p-6`, 내부 `<button>`은 자체 테두리·패딩 제거해 이중선 방지) · 그리드 `md:grid-cols-2 gap-6 md:gap-8`로 좌우 넓게 분리 · 방법론 `<li>` = `text-[13.5px] leading-snug`, `<ol>` gap-1.5. **각 단계 설명은 핵심 키워드만(≈11~14자)로 압축** — 3-Pane 중앙 패널이 우측 검토 패널 탓에 좁아 긴 문장은 2줄로 넘어가므로, 폭 확대 + 설명 압축을 함께 적용해 **모든 단계가 한 줄**로 보이게(핵심 방법론 용어 신뢰구간·교차검증 등은 유지, `whitespace-nowrap` 강제 금지, 긴 문장으로 되돌리지 말 것).
```
