# ZEROS 4차 잔여작업 — 실행준비서 (2026-08-07) · ⛔ 이관됨

> # ⛔ 이 문서의 잔여 목록은 낡았다 — **`잔여작업-마스터.md`** 를 볼 것
>
> 2026-08-08 전량 재검증 결과 **여기 적힌 26건 중 22건이 이미 해소**돼 있었다(코드 실측).
> 08-07 이후 커밋 23개가 처리했는데 그 사실이 여기 역반영되지 않았다.
> 특히 아래는 **사실이 아니다** — D1(게이트 개편, 실제 383줄·7룰·4모드 완료) · B4(완료) ·
> C2(`041B33` 0곳) · D7(SQL 실행 완료) · 확정②(오늘 소멸) · `app/page.tsx` 2,166줄(실제 1,852줄).
>
> **이 문서는 2026-08-07 시점의 기록으로만 남긴다. 착수 판단에 쓰지 말 것.**

> 주인님 지시: "작업준비만 해 두세요. 작업 시작하면 바로 진행할 수 있도록."
> **이 문서 하나로 착수한다.** 재검증 6기(읽기 전용)가 잔여 항목을 코드와 1:1 대조해 만든 실측 기반이다.
> 근거: 결함대장 `docs/_worklog/2026-08-06_전수조사_결함대장.md` · 계획서 `개선-작업계획서.md` §8 · 실행기록 §8-8

---

## 0. 착수 신호와 절차

주인님이 **"작업 시작"** 이라고 하면 아래 순서로 즉시 진행한다. 계획 재수립·프롬프트 작성 시간은 0이다.

1. PRE-FLIGHT 6줄 출력 (`~/.claude/실행-결함-분석.md` §4)
2. 브랜치 생성 → 파도 1 기동 (스크립트: `.claude/workflows/round4-residual.mjs`)
3. 파도마다 임시 커밋 → 다음 파도
4. 적대 검증 파도 → FAIL 봉합
5. Playwright 픽셀 게이트 실측(O-37 · O-38)
6. CI 5단계 → 마감처리(커밋·머지·푸시·프로브)

---

## 1. 재검증 결과 — 숫자가 바뀌었다

읽기 전용 6기가 **32건을 판정**했고(중복 ID 제거 시 **26건**), **전부 「실재」**로 나왔다. 「이미 해소됨」·「재현 불가」는 0건이다.

| 항목 | 재검증 전(추정) | **재검증 후(실측)** |
|---|---|---|
| 난이도 | S 위주로 가정 | **L 2 · M 13 · S 11** |
| 픽셀 게이트 필요 | 1건 | **4건** |
| 조문 개정 필요 | 0건 | **2건** (B5 · 결정①) |
| 범위 해석 확정 필요 | 0건 | **1건** (C2) |
| 소요 | 2시간 | **2시간 10분 ~ 2시간 50분** |

### 추정이 틀렸던 것

| 항목 | 내가 잡았던 것 | 실측 |
|---|---|---|
| C2 모바일 홈 리컬러 | M | **L** — `AppShell.tsx` **23~24지점**. 배경 5개를 바꾸면 그 위 글자·아이콘·테두리 18개가 연쇄로 뒤집힌다. `app/layout.tsx`의 `themeColor`·상태바까지 이어진다 |
| C10 터치 타깃 | 3건 | **5건** — 라이브 실측이 못 본 메뉴 열기 36px 등 2건 추가 |
| C5 `motion-reduce` | 9곳 | **12지점** — 정규식이 놓친 `animate-[spin_4s...]`·`animate-in`·인라인 주입 CSS 포함. **게이트 룰 자체에 구멍이 있다는 증거** |
| C6 본문 대비 | "9개 파일" | **총 72곳 중 본문 30곳** — 나머지는 장식이라 손대면 안 된다. 게이트 R2가 가리키는 곳과 실제 결함 위치가 **서로 다르다**(오탐) |
| D1 게이트 개편 | M | **L** — 파일이 123줄이고 룰 4종 신설 + 모드 파싱 재작성 + 베이스라인 스냅샷 + `.claude/settings.json` 연동 |
| B5 CTA 명칭 | "5종" | **6곳 5종.** 그중 `app/page.tsx:526`은 §10 확정 문구라 **불변(기준값)** |

---

## 2. ⛔ 착수 전 주인님 확정 3건

### 확정 ①. B5 — CTA 명칭 통일 범위 (조문 개정 동반)

`app/page.tsx`에 CTA 6곳·5종이 있다. 기준값은 `L526 무료 견적 검토 신청`(§10 확정 문구).

| 안 | 조치 |
|---|---|
| **A** (권고) | 5곳(644·806·1569·1764·2033)을 기준값으로 통일 + 단일 상수로 추출. **탭 라벨(`예상견적 의뢰하기`)·채널 카드 제목(`무료 견적 신청`)은 제외**하고 그 제외를 §10에 1줄 명문화 |
| B | 이번엔 보류 — 5종 혼재 유지 |

> §10-A에 `예상견적 의뢰(request) 탭`이 화면 명칭으로 박혀 있어, 탭 라벨까지 통일하려면 조문 개정이 선행된다.

### 확정 ②. 신뢰지표 246 — 조문에 리터럴로 박혀 있다

`lib/constants/trust.ts:20 cumulativeReviews: 246`. 화면 렌더 5지점. **§10-A 사업소개 조문에 "신뢰 지표 클러스터(30년·246건·98.4%)"로 값이 문자열째 확정**돼 있다.
한편 실적 탭은 DB 실측(101건, 그중 99건이 `est-test-*` 시드)을 렌더한다 — 방문자가 탭 두 번에 246과 101을 본다.

| 안 | 조치 |
|---|---|
| **A** (권고) | 라벨 분리 — 246은 `누적 검증(30년 실적)`, 실적 탭은 `온라인 접수 현황`. 숫자는 유지하되 출처를 명시 → 조문에 라벨 1줄 추가 |
| B | `est-test-*` 삭제 + DB 실측 하나로 통일 → 공개 실적이 **2건**으로 떨어진다 |
| C | 보류 |

### 확정 ③. C2 — "모바일 메인화면"의 범위

§10-A 공통 1항 "모바일 메인화면 = 화이트 배경(고정)"의 적용 범위.

| 안 | 조치 | 작업량 |
|---|---|---|
| **A** (권고) | 홈 랜딩(`isMobileLanding`)만 화이트 | L · 23지점 |
| B | 전 탭 셸(헤더·하단탭·드로어 포함) 화이트 | L+ · 지점 2배 · 픽셀 게이트 전면 재측정 |
| C | 보류 — 조문 개정(현행 다크 유지) |

---

## 3. 기수 배분 (파일 배타 소유 — 이 표가 충돌 방지의 전부다)

같은 파일은 **반드시 한 기수**다. `AppShell.tsx`는 항목이 5개(C2·C7·C8·C10·D6)라 한 기수에 다 넣으면 비대해지므로 **파도를 나눠 시간대를 분리**한다.

### 파도 1 — 수정 6기

| 기 | 배타 소유 파일 | 항목 | 난이도 |
|---|---|---|---|
| 1 | `app/page.tsx` (2,166줄) | B5 · C4(2108) · C5(12지점) · C6(1551) | M |
| 2 | `AppShell.tsx` + `app/layout.tsx` | **C2**(23지점) · C10(5지점) | **L** |
| 3 | `RequestWizard.tsx` + `lib/forms/requestForm.ts` | B9-a 이메일 · B9-b 방문일 · B9-c 초기화 · B9-d 시간대 · N3 `customer_type` | M |
| 4 | `PerformanceInsights.tsx` + `lib/constants/trust.ts` | **B4** · 확정② | M |
| 5 | `admin/CustomerList.tsx` + `VisitList.tsx` | N1 저장실패 무음 · N2 alert | S |
| 6 | `.claude/hooks/ui-quality-gate.mjs` + `settings.json` | **D1** 룰 4종 + `--all` 스윕 + 베이스라인 | **L** |

### 파도 2 — 수정 5기

| 기 | 배타 소유 파일 | 항목 | 난이도 |
|---|---|---|---|
| 7 | `AppShell.tsx` + `LeftSidebar.tsx` + `ShellContext.tsx` | C7 종료팝업 · C8 URL 동기화 · D6 죽은 인터벌 · C9 플레이스홀더 | M |
| 8 | `admin/EstimateDetailModal.tsx` | B8 품목표 초안 저장 · C4·C5·C6(admin 몫) | M |
| 9 | `lib/supabase/client.ts` + `api/data/route.ts` + `supabase-setup.sql` | D2 mock 동적 import · D7 버킷 한도 · N6 결제 삭제 op | M |
| 10 | `RequestWizard.tsx` + `lib/a11y/` | N4 roving tabindex(공용 컴포넌트 신설) | M |
| 11 | `RightSidebar.tsx` + `MobileSimulator.tsx` | C4·C5·C6(우측 패널 몫 9지점) | S |

### 파도 3 — 수정 1기 (단독)

| 기 | 배타 소유 파일 | 항목 |
|---|---|---|
| 12 | `EstimateList` `KanbanBoard` `MyRequestsView` `MyRequestsModal` `PrintableScopeSheet` `PerformanceDashboard` `menu.ts` | **N5** 공종 표기 3종 통일 (8파일 14지점) |

> N5는 파도 1·2의 거의 모든 파일과 겹친다. **반드시 단독 파도**로 마지막에 돌린다. 선례: memory `Workflow 단일 편집 누락`.

### 파도 4 — 적대 검증 3기

| 렌즈 | 무엇을 반증하는가 |
|---|---|
| 보안·회귀 | D7 버킷 한도가 정상 업로드를 막지 않는가 · N6 삭제가 미수금 파생과 어긋나지 않는가 · §13 불변식 유지 |
| 여정·접근성 | B9 4건이 정상 접수를 막지 않는가 · N4 roving이 선택 상태를 깨지 않는가 · role=alert·44px·대비 |
| 조문·픽셀 | §10/§10-A 확정 조문 위반 여부 · O-37·O-38 재측정 · C2 연쇄가 세로 예산을 깨지 않는가 |

---

## 4. 소요·기수 (실측 기반)

| | **PLAN A — 결정 없이 즉시** | **PLAN B — 전량** |
|---|---|---|
| 범위 | **23건** (확정 ①②③ 필요분 제외) | **26건 + 확정 3건 반영** |
| 제외 | B5 · 확정①(246) · C2 | 없음 (D3만 별도 차수) |
| 수정 기수 | **9기** (2기에서 C2 빠짐, 1기·4기 축소) | **12기** |
| 검증 기수 | 3기 | 3기 |
| **총 기수** | **12기** | **15기** |
| 파도 | **3파도** | **4파도** |
| 소요 | **2시간 10분** | **2시간 50분** |
| 픽셀 실측 | C10만 | **C2 포함 전수** |

**산정 근거** — 2026-08-07 PLAN A 실측(15건 · 수정 9기 · 3파도 = 2시간 20분). 이번은 L 2건(C2·D1)이 병목이고 파도가 하나 더 붙는다.

**중단 가능 지점**: 매 파도 끝(임시 커밋). 적대 검증 직후가 가장 안전하다.

**제외 — D3(전 테이블 재업로드 → 단건 쓰기, L)**: 추상 계약 변경 + 서브클래스 2개 + 호출부 13곳 + 서버 op 4개 신설 + 캐시 규칙 재정의. **별도 차수를 권한다.** 이번에 넣으면 소요가 2배가 된다.

---

## 5. 실행 중 지킬 것

1. **파일 배타 소유** — §3 표를 벗어난 파일은 읽기만. 어기면 변경이 조용히 사라진다(선례 있음).
2. **§14-1 테스트 우선** — 결함마다 실패 테스트를 먼저. 안 써지면 그 사실을 보고한다.
3. **§10-A 확정 조문 우선** — 충돌하면 고치지 말고 보고한다.
4. **적대 검증 필수** — 2026-08-07에 검증이 **배포 전 4건을 막았고, 그중 2건은 수정 기수가 "완료"로 보고한 것**이었다.
5. **치명·높음은 오케스트레이터가 직접 diff를 읽는다** — 적대 검증은 2차 방어다(`실행-결함-분석.md` D5).
6. **약속 시각 = 체크포인트** — 끝났든 아니든 그 시각에 보고한다.

---

## 6. 착수 시 첫 응답에 채울 PRE-FLIGHT

```
[PRE-FLIGHT]
1. 규칙 조회 : 이 문서 · AGENTS §10-A·§14·§15 · 실행-결함-분석.md
2. 범위     : PLAN _ · __건 · 대상 __파일
3. 충돌 지도 : §3 표 — 수정 __기 · 충돌 0 · 파도 __개
4. 소요     : __분 → 완료 __:__ KST
5. 승인     : PLAN _ 승인 완료(주인님 __월 __일 지시)
6. 검증 계획 : 적대 검증 3기(전체의 __%) · 픽셀 게이트 O-37·O-38 · 내가 직접 읽을 diff __개
```
