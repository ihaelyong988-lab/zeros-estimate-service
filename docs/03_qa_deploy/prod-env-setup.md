# 프로덕션 마무리 가이드 — 주인님이 직접 하실 일

> ## ▶ 현행 상태 (2026-08-08 11:35 실측)
>
> | # | 무엇 | 상태 |
> |---|---|---|
> | **①** | **`supabase-setup.sql` 실행** (§5) | ✅ **완료** — 주인님 실행 + AI 독립 실측 확인 |
> | **②** | **Vercel 키 등록** (§1~§4) | ⬜ **남음** — 고객이 마이페이지에 못 들어감 · 관리자 로그인 불가 |
>
> **남은 것은 ② 하나뿐입니다.** 문자 발신번호 심사(1~2일) 때문에 신청만 먼저 하시면 됩니다.
>
> ### 접속 계정 (2026-08-08 확정 — 다시 찾지 말 것)
>
> | 서비스 | 계정 | 로그인 방법 | 바로가기 |
> |---|---|---|---|
> | Supabase | **GitHub `ihaelyong988-lab`** | **소셜 버튼만** — 비밀번호 로그인 불가 계정("does not support password sign-in") | [SQL Editor](https://supabase.com/dashboard/project/xtljznrfmythnnpeorgz/sql/new) |
> | GitHub | `ihaelyong988-lab` | — | [저장소](https://github.com/ihaelyong988-lab/zeros-estimate-service) |
> | 라이브 사이트 | — | — | [zerospipe.co.kr](https://zerospipe.co.kr) |
>
> ### 키 상태 (2026-08-08 실측 · 값은 확인하지 않음)
>
> | 키 | `.env.local` | Vercel | 필요한 일 |
> |---|---|---|---|
> | `SOLAPI_API_KEY` | 비어있음 | 미등록 | **Solapi 발급** |
> | `SOLAPI_API_SECRET` | 비어있음 | 미등록 | **Solapi 발급** |
> | `SOLAPI_SENDER` | 비어있음 | 미등록 | **발신번호 등록·심사(1~2일)** |
> | `ZEROS_ADMIN_PASSWORD` | 비어있음 | 미등록 | **주인님이 정하면 됨** |
> | `OTP_SERVER_SECRET` | 설정됨(64자) | 미확인 | 로컬 값 복사 등록 |
> | `SUPABASE_SERVICE_ROLE_KEY` | 설정됨(41자) | 등록됨 | — |
>
> `vercel` CLI는 설치돼 있으나 **프로젝트 미연결**(`.vercel/project.json` 없음) → §3의 `vercel login` + `vercel link`가 선행.
>
> ### ✅ 보안 선행 조건은 전부 해소됐습니다
>
> 이전에는 키를 등록하는 순간 위험이 활성화되는 상태였습니다. 지금은 아닙니다.
>
> | 과거 위험 | 해소 |
> |---|---|
> | 인증 토큰이 서버로 안 감 → 등록 시 모든 접수가 403 | 2026-08-01 2단계 |
> | 관리자 로그인에 시도 제한 없음 → 무제한 대입 | 2026-08-07 A1 (IP 15분 5회) |
> | 파일 서명 소유권 우회 → 남의 도면 열람 | 2026-08-07 A2 |
> | 접수 무제한 생성 | 2026-08-07 A3 (번호 1시간 3회) |
>
> **지금 등록해도 안전합니다.**
>
> ### 등록 전까지 알고 계실 것
>
> 지금도 접수는 작동합니다(인증 게이트를 건너뛰는 상태). 다만 **아무나 남의 번호로 접수할 수 있고**, 고객은 마이페이지에서 자기 접수 현황을 볼 수 없습니다.

> 키·비밀번호는 **사람이 직접** 등록한다(보안 원칙상 AI에게 값을 전달하거나 채팅·코드·커밋에 붙여넣지 않는다). 코드 수정은 필요 없다 — 등록만 하면 자동 활성.

## 1. 휴대폰 인증 문자 (SOLAPI 3종)

| 단계 | 내용 |
|---|---|
| 1 | [solapi.com](https://solapi.com) 가입 |
| 2 | 콘솔에서 **발신번호 사전등록**(통신사 서류 심사, 통상 1~2일) — 등록된 번호만 발신 가능 |
| 3 | 콘솔 → API Key 메뉴에서 **API Key / API Secret** 발급 |
| 4 | Vercel 등록(아래 §3) — `SOLAPI_API_KEY` · `SOLAPI_API_SECRET` · `SOLAPI_SENDER`(발신번호, 숫자만) |
| 5 | 로컬 `.env.local`의 같은 키에도 채움(로컬 테스트용, 선택) |

- 문자 단가는 건당 과금(SMS 기준 십수 원대) — 남용 완화는 코드에 이미 있음(번호당 1시간 5회, IP당 20회. `lib/otp/rateLimit.ts`).

## 2. 관리자 로그인 (ZEROS_ADMIN_PASSWORD) + 토큰 서명키 (OTP_SERVER_SECRET)

| 키 | 값 | 비고 |
|---|---|---|
| `ZEROS_ADMIN_PASSWORD` | 직접 정한 강한 비밀번호 | 미설정 시 관리자 로그인 503(현재 상태). 로컬 `.env.local`에도 동일하게 |
| `OTP_SERVER_SECRET` | 로컬 `.env.local`에 이미 있는 값을 복사해 등록 | 미설정 시 로그인 세션(30일)·인증 토큰이 서버 인스턴스마다 무효화됨 |

- 새 시크릿이 필요하면 생성: `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`
- `SUPABASE_SERVICE_ROLE_KEY`는 이미 라이브에서 동작 확인됨(2026-07-11 §13) — 재등록 불필요.

## 3. Vercel 등록 절차 (둘 중 하나)

**방법 A — 원클릭 스크립트(권장, 2026-07-16 추가)**: 키 값을 `.env.local`에 붙여넣은 뒤 아래 실행 — 값이 화면·채팅을 거치지 않고 로컬에서 Vercel로 바로 전송된다.
1. 최초 1회: `npm i -g vercel`(설치됨, v56) → `vercel login`(브라우저 인증) → 프로젝트 폴더에서 `vercel link`(프로젝트 선택)
2. `.\scripts\push-env-vercel.ps1` 실행 → 5개 키 중 값이 있는 것만 Production에 등록(빈 키는 건너뜀 표시)
3. Claude에게 "재배포하고 검증해" 지시 → 빈 커밋 푸시로 재배포 + `prod-probe` 3/3 PASS 확인

**방법 B — 대시보드 수동**:
1. [vercel.com](https://vercel.com) 대시보드 → 해당 프로젝트 → **Settings → Environment Variables**
2. 키 이름을 위 표와 **철자 그대로** 입력, Environment는 **Production**(Preview까지 체크해도 무방)
3. 저장 후 **Redeploy**(Deployments → 최신 배포 → Redeploy) — env는 재배포 시점에 반영된다.

---

## 5. ② Supabase SQL 실행 (5분 · 오늘 바로 가능)

**무엇이 바뀌는가**

| 항목 | 지금 | 실행 후 |
|---|---|---|
| 접수번호 | 동시에 2건이 들어오면 **같은 번호**가 발급될 수 있다 | DB가 중복을 막는다. 서버는 번호를 다시 매겨 최대 5회 재시도 |
| 파일 업로드 | 개당 **용량 제한 없음** — 공개 키만 있으면 누구나 대용량 적재 가능 | 개당 **50MB** 상한 |

**코드는 이미 준비돼 있습니다.** 실행 전에도 앱은 정상 동작합니다(코드가 이 한도를 읽지 않음). 실행하면 그때부터 적용됩니다.

### 절차

**1단계 — 중복 접수번호가 있는지 먼저 확인**

**▶ [SQL Editor 바로가기](https://supabase.com/dashboard/project/xtljznrfmythnnpeorgz/sql/new)** — 이 링크가 바로 새 쿼리 창입니다(로그인만 되어 있으면 됨). 아래 붙여넣고 **Run**.

> 절차 안내에는 **항상 클릭 링크를 준다.** "대시보드 → 프로젝트 → 왼쪽 메뉴" 식 길안내는 쓰지 않는다(2026-08-08 주인님 지시).

```sql
select data->>'estimate_no' as no, count(*)
  from zeros_estimates
 group by 1 having count(*) > 1;
```

- **"No rows returned"** 가 뜨면 → 2단계로 진행
- **행이 나오면** → 중복이 이미 있다는 뜻이라 2단계가 실패합니다. 그 결과를 저에게 알려주십시오(중복 정리 후 재시도)

**2단계 — 전체 SQL 실행**

프로젝트 폴더의 **`supabase/supabase-setup.sql`** 파일을 열어 **전체 복사** → SQL Editor 에 붙여넣기 → **Run**

- 이 파일은 **몇 번 실행해도 결과가 같습니다**(멱등). 이전에 실행한 적이 있어도 안전합니다

**3단계 — 확인 (파일이 스스로 채점합니다)**

Run 하면 마지막에 **결과 표 5줄**이 나옵니다. **전부 `OK` 여야 끝난 것입니다.**

| 항목 | 결과 |
|---|---|
| 1. 접수번호 중복방지 | OK |
| 2. 업로드 용량 50MB | OK |
| 3. 버킷 비공개 | OK |
| 4. 익명 업로드 허용 | OK |
| 5. 익명 테이블 차단 | OK |

한 줄이라도 `실패` 면 **그 화면을 그대로** 알려주십시오.

> **왜 표가 나오게 바꿨나 (2026-08-08).** 예전에는 마지막이 `update` 라 **"Success. No rows returned"** 만 떴습니다. 그런데 SQL Editor 는 전체를 **한 트랜잭션**으로 돌려서, 중간 한 줄이 실패하면 **앞뒤가 통째로 취소**됩니다 — 그때도 화면은 비슷해 보입니다. 실제로 2026-08-08 실행분이 이렇게 조용히 취소됐고(업로드 한도 미적용), 사람 눈으로는 구분할 수 없었습니다. 그래서 **결과 표 자체가 판정**이 되도록 파일 끝에 검증 절을 넣었습니다.

### 주의

- **파일 전체를 복사**하십시오. 일부만 실행하면 앞부분(테이블 생성·보안 정책)이 빠집니다
- 이 SQL 은 **데이터를 지우지 않습니다.** 테이블 구조와 정책만 다룹니다
- 실행 중 빨간 오류가 뜨면 **그 문구를 그대로** 알려주십시오. 대부분 1단계의 중복 때문입니다

---

## 6. 두 작업을 마치면

```bash
node scripts/prod-probe.mjs
```

- **3/3 PASS** 가 나오면 완료입니다(현재는 사이트 200 PASS · 문자 WARN · 관리자 WARN)
- 마지막으로 실기기에서 1회: 예상견적 의뢰 → 휴대폰 인증 → **문자 수신** → 인증번호 입력 → 접수 → 관리자 화면에서 확인

### 그 뒤에 달라지는 것

| | 지금 | 완료 후 |
|---|---|---|
| 고객 마이페이지 | 로그인 불가("문자 인증 준비 중입니다") | 휴대폰 번호로 로그인해 접수 현황 확인 |
| 관리자 화면 | 로그인 503 | 비밀번호로 로그인 |
| 장난 접수 | 아무나 남의 번호로 가능 | 본인 인증한 사람만 |
| 접수번호 | 동시 접수 시 중복 가능 | 중복 불가 |
| 파일 업로드 | 용량 무제한 | 개당 50MB |

### 아직 남는 것 (키를 등록해도 해결되지 않음)

**상태 알림 문자는 나가지 않습니다.** 문자 발송 코드가 본인인증 1곳에만 있고, "견적서 송부완료" 같은 상태 알림에는 발송 호출 자체가 없습니다. 이력만 쌓이고 고객에게는 아무것도 가지 않습니다.

지금 화면 문구는 그 사실에 맞춰 정직하게 고쳐 뒀습니다("상태 변경은 이 화면에서 확인합니다"). 실제 발송 경로 구축은 별도 작업입니다.
