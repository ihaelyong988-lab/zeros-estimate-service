# 2026-07-09 작업정리

## PHASE O-25 — 견적검토 히어로 FLOW·제목 폰트 위계·수평 정렬·리듬 균형 (캡쳐 마크업 4항목)

### 배경
주인님 캡쳐(데스크톱 견적검토 히어로) 손글씨 주석 4건 + "폰트는 언어를 통한 감성의 리듬 — 균형감 있게 완료" 지시. 상단 FLOW 헤더와 좌·우 두 컬럼 제목의 폰트 위계·수평 정렬을 다듬고, 커진 폰트에 맞춰 인접 여백을 함께 조정해 세로 리듬을 완성한다. 대상은 데스크톱 전용 블록(`hidden lg:grid`) 1곳(모바일 무영향 — grep 확인).

### 반영 (캡쳐 4항목 + 리듬 보정 2건)
1. **① FLOW 헤더 부제 삭제** (`components/EstimateFlow.tsx`): "단계를 누르면 내용이 펼쳐집니다" 인라인 부제 span 제거(캡쳐 X표시).
2. **② "견적 작업 FLOW" 폰트 상향** (`EstimateFlow.tsx`): `text-[21px]` → `text-[26px]`(좌측 CAPEX 타이틀과 동일) + 오렌지 세로바 `h-5`→`h-6` 동반 상향.
3. **③ 우측 "사전 견적 후 결과 지표" 상단선 정렬** (`app/page.tsx:1747`): `pt-2` 제거 → 좌측 CAPEX `<h1>`(상단 패딩 없음)과 상단선 수평 정렬(8px 상향). 2026-07-04 "pt-2 살짝 하향" 철회.
4. **④ benefit 불릿 폰트 상향** (`app/page.tsx:1756`): `text-[15px]` → `text-[16px]`(본문 가독성 ≥16px 규칙 충족). 3줄: 공사 이해도 향상 / 안심 예산 책정 / 공사비 버블 불안감 해소.
5. **리듬 보정 A** (`EstimateFlow.tsx`): 제목 26px에 맞춰 제목→탭 레일 간격 `mt-3.5`→`mt-4`(아래 호흡 확대).
6. **리듬 보정 B** (`app/page.tsx`): 우측 benefit `ul` `mt-3.5`→`mt-4` — 좌측 CAPEX 제목 아래 사진(`pt-4`)과 좌·우 제목→첫요소 간격 대칭.

### 규칙 동기화 (반복금지 제4조)
- `AGENTS.md` §10-A "견적검토 히어로·FLOW 상하 균형" 조문을 2026-07-09 확정값으로 갱신: FLOW 제목=CAPEX와 동일 26px·부제 삭제(구 "하단 밴드 동기 21px" 대체), 우측 제목=pt 제거 상단선 정렬(구 "pt-2 하향" 철회), benefit 불릿 16px. → 다음 세션 되돌림 방지.

### 검증
- `npm run build` 0에러(2회) · `ui-quality-gate --check`→`--pass`(차단룰 0, R2는 기존 장식 요소로 무관) · 코드 1:1 재대조(4항목+보정2 누락 0).
- 헤드리스 프리뷰는 스플래시 정지로 신뢰 불가(§8) → build + 코드 재대조로 검증.

### 파일
- `components/EstimateFlow.tsx` · `app/page.tsx`(견적검토 히어로 데스크톱 블록) · `AGENTS.md` §10-A
