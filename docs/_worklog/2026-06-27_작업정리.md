# 2026-06-27 작업정리

> 주제: 사업소개/실적/견적검토/의뢰폼 UI 리디자인 반복 + 스킬 강제 하네스 + 실적 테스트데이터 시드. 반복 마찰점(캐시·데이터소스)을 지침에 영구 박음.

## 1. 반영·배포한 것 (master 머지 순서)

| 영역 | 내용 | 비고 |
|---|---|---|
| 사업소개 패널 | 진입 시 우측 예상견적 패널 접힘(검토 엣지탭으로 펼침) | `AppShell` `setShowDecisionPanel(false)` to `business` |
| 사업소개 히어로 | ±5% 게이지 단일 하이라이트 + 리스크→차단 원장(중복 제거) → 이후 **오버사이즈 에디토리얼** → **문제→해결 전면 리디자인 + 단일 CTA** → **헤드라인 품위 변환(C1)** | `app/page.tsx` `renderBusinessTab` |
| 견적검토 히어로 | 4분할 재구성: 우측 CTA·AI Native 링크 삭제(상단탭 중복), 우상=작업과정/우하=결과지표, 좌하=관련 이미지 자리 | `renderReviewDesktop` |
| 견적검토 밴드 | 헤드라인 22px·1행 컴팩트(높이 1/2), 지표 4개 유지 | |
| 의뢰폼 | 2분할→프레임 통일·폰트 확대 → **3분할(기본/현장/자료) + 하단 등록 풀블리드 바** | `RequestWizard` |
| 실적 KPI | 박스 제거·상하 헤어라인·누적건수만 오렌지(펄스+룰)·무채색 보조 → 누적 72px | `PerformanceInsights` |
| 실적 막대 | 공종별 시그니처 색(`TRADE_COLORS`, fillOpacity 0.9) | |
| 실적 데이터 | **Supabase에 테스트표본 99건 직접 시드**(공종별 10~15, est-test-* id) | 아래 §3 참고 |
| 하네스 | AGENTS.md §0(스킬 우선) + UserPromptSubmit 훅 자동주입(UI/업무문서) | |

최종 헤드라인: **"흔들리는 견적을 데이터로 바로잡아, 최적의 예산을 책정합니다"** (font-extrabold 800, accent="데이터로", 본문 max-w-[560px]). ±5%는 헤드라인보다 작게(clamp 46px).

## 2. 스킬 강제 하네스 (반복 지적 → 영구 고정)
- AGENTS.md **§0 스킬 우선 적용** 신설 + `.claude/hooks/prompt-checklist.mjs` 확장 → 매 지시에 "UI=ui-ux-pro-max/frontend-design, 엑셀=xlsx/excel-report, PPT=pptx…" 자동 주입.
- 메모리 `always-use-uiux-skill-first` 기록(전 작업 적용).

## 3. 큰 교훈 (다시 안 틀리려고 기록 — AGENTS.md §8·§9에 영구화)
- **데이터 소스는 Supabase**(Mock 아님). `supabaseBrowser.ts` DEFAULT 키 + `.env.local` → `isSupabaseEnabled=true` 항상. `mock-data.ts` 시드는 안 보인다 → `SupabaseZerosService`에 멱등 시드해야 함.
- **배포 후 "똑같다" = 거의 PWA/브라우저 캐시.** 헤드리스 프리뷰는 스플래시(rAF)에서 멈춰 스크린샷 불가 → 검증은 `npm run build`+`show_widget` 시안. 사용자는 **시크릿 창**으로 확인.
- **마감처리 git은 단계별 출력 확인.** 한 줄 다중명령이 깨져 push가 안 나간 적 있음 → `xxx..yyy master -> master` 눈으로 확인.

## 5. 세션 연장 반영 — 공종 상세 밴드 + 키워드/라벨 (master `bebeb75`)

### 5-1. 공종 상세 "주요 견적공종" 밴드 (page.tsx `renderManualDetail`)
- "주요 견적공종" 라벨 `12px → 22/24px`(키워드와 동일 크기, accent 색 유지) + 키워드 `20/22 → 22/24px`(한 단계 ↑, 모듈러 스케일 …20·22·24). 구분선 `h-16→20`.
- 가운데 간격: 컨테이너 `gap-6 → gap-8`·`py-3 → py-4`, 밴드 `pb-5 → pb-6` (상단 공백과 균형).
- ※ 검증 방법: dev 서버(`npm run dev`) HMR로 `localhost:3000/?menu=배관공사`에서 즉시 확인(헤드리스 프리뷰는 스플래시로 불가 — AGENTS.md §8). "반영 안 됨" 원인은 옛 탭/홈페이지(공종상세 아닌 곳)를 본 것이었음.

### 5-2. 키워드 밴드 내용 정리 (`TRADE_KEYWORDS`, page.tsx)
- Utility: 질소→**온수 배관** / 공장증설: 라인증설→**생산라인 증설**·무중단분기→**무단수 작업**·유량검토 삭제 / 노후배관교체: 부식배관→**배관교체**·무중단교체·잔존수명 삭제 / 생산설비: 설비연결→**장비 연결**·유틸리티 탭·정밀정렬 삭제 / CAPEX: 예산상한→**예산 견적**·LCC 삭제 / spool: 스풀분할 삭제 / structure: 하중검토 삭제.

### 5-3. 외주제작 메뉴 라벨 (menu.ts + LeftSidebar + page.tsx 홈카드)
- `SKID . SPOOL제작 견적` → **SKID 제작설치 검토** (3곳) / `Structure제작,설치` → **Structure제작,설치 검토** (2곳).
- 주의: LeftSidebar는 자체 `label`을 렌더하므로 menu.ts만 고치면 안 되고 **3곳/2곳 모두** 수정해야 함.

## 6. 하네스 보강 — "마감처리"에 작업 기록 자동화
- 문제: 매 마감처리 후 사용자가 "md 정리해 둬"를 매번 시켜야 했음(반복 마찰).
- 해결: **마감처리 절차에 "작업 기록 자동 갱신" 단계 신설** — 커밋 직전 worklog 추가 + state.md 갱신을 **같은 커밋에 포함**. 다신 안 시켜도 자동.
- 반영: `.claude/commands/마감처리.md`(step 4) + AGENTS.md §6 + 메모리 `margam-auto-records-worklog`.

## 7. 다음 할 일
- est-test-* 테스트행은 실 런칭 전 일괄 삭제(`delete from zeros_estimates where id like 'est-test-%'`).
- 견적검토 좌하 "관련 이미지 자리"에 실제 공종 이미지 연결.
- 2차 Toss Payments 실서버 연동 / 알림톡 SMTP 실서버.
