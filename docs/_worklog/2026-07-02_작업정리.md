# 2026-07-02 작업정리 — PHASE P: 하네스 고도화 + 자동 견적서 1단계(반자동)

## §1. 하네스 고도화 (4폴더 최적화 지시 — 이 저장소 반영분)
- **무엇**: ①성장 게이트 Stop 훅 신설 ②표준 절차·개선 프롬프트 문서 신설.
- **왜**: "작업 후 자동 업데이트(기록)"를 사람 의지가 아니라 기계 게이트로 강제(반복금지 헌법 제1조). 개선 지시를 매번 재설명하지 않기 위함.
- **어디**:
  - `.claude/hooks/growth-gate.mjs` (신규) — 오늘 커밋이 있는데 worklog/결정로그/state 기록이 갱신 안 됐으면 마감 `decision:block`(세션 2회 cap, `--check` 모드). 같은 파일을 개인용 PMIS·꽁아코스 `scripts/`에도 이식.
  - `.claude/settings.json` — Stop 배열에 growth-gate 등록(다음 세션부터 활성).
  - `docs/OPTIMIZE.md` (신규) — 표준 작업 루프 + P1(자동 견적서)·P2(Edge Function) 복붙 프롬프트.
  - `AGENTS.md` §11-2 — 성장 게이트 규칙 영구화.
- **검증**: 통과 경로(오늘 커밋 0 → 비차단) 3폴더 실측 + 차단 경로 스크래치 git 저장소에서 block JSON 발동·기록 후 통과 실증.

## §2. 자동 견적서 1단계 — 반자동 파이프라인 (OPTIMIZE.md P1)
- **무엇**: 고객 업로드 자료 기반 AI 견적 초안 → 관리자 편집·승인 → 엑셀 견적서 생성·버킷 업로드 → 고객 마이페이지 다운로드.
- **왜**: 기존에는 견적서 자동 생성이 없었음(브라우저 인쇄 + 금액 수동 입력뿐). `estimate_pdf_url` 예약 필드를 실제로 활용.
- **어디**:
  - `types/estimate.ts` — `EstimateLineItem`(품명·규격·수량·단위·단가) + `Estimate.line_items` 추가.
  - `lib/quote/quoteDraft.ts` (신규) — **규칙 기반 초안 엔진**: 공종(WorkType) 10종별 표준 품목 구성비 템플릿 + 예산 근거 총액(관리자 산출액 > 고객 예산대역 > 규모 분류) + 공수 단가 45만 스케일링. 2단계(Claude API 도면 판독)로 교체 시 이 모듈만 대체.
  - `lib/quote/quoteXlsx.ts` (신규) — exceljs **동적 import**로 견적서 엑셀 생성. 금액=수량*단가·소계 SUM·VAT ROUND·±5% 대역 전부 **살아있는 수식**. 하단 고지("예상 원가 검토서·현장 실측 후 확정" — 단정 표현 금지 §10).
  - `lib/supabase/storage.ts` — `'견적서' → quotes/` 버킷 폴더 매핑 추가.
  - `components/admin/EstimateDetailModal.tsx` — "AI 견적 초안 · 견적서 발송" 카드: AI 초안 생성 버튼 → 편집 가능한 품목표(입력 aria-label 전수) → 소계/VAT/합계/±5% 요약 → [엑셀 미리보기] + [승인·견적서 발송](오렌지 CTA 1개). **승인 게이트**: 발송 시에만 `quotes/` 업로드 + `estimate_pdf_url`·`estimate_sent_at` 기록 + 상태 '견적서 송부완료'. 오류는 `role="alert"`.
  - `components/MyRequestsView.tsx` — 접수 카드에 `estimate_pdf_url` 있으면 "견적서 다운로드 (엑셀)" 버튼(min-h-11) 노출. 라벨 1곳 text-gray-light→text-gray 교정.
  - `package.json` — `exceljs` 의존성 추가.
- **디자인 근거**: ui-ux-pro-max 선호출 + `design-system/zeros/MASTER.md`(§10 오버라이드) + `show_widget` 시안 → 사용자 "이대로 진행" 확정.
- **검증**: `npm run build` 0에러 · `ui-quality-gate --check` 차단룰 0건(→ `--pass` 마커) · graphify 188노드 갱신.
- **남은 것**: 2단계(Supabase Edge Function + Claude API 실제 도면 판독) = OPTIMIZE.md P2.

## §3. E2E 실사용 검정 (Playwright — 배포 커밋 후 사용자 요청)
- **방법**: 로컬 prod 서버(`npm run start`) + Playwright MCP. 테스트 행 `ZR-20260618-161`(est-test, 노후배관교체) 사용 — 실고객 데이터 무손상.
- **결과 (전 단계 통과)**: ①관리자 로그인 ②접수관리 카드→상세 모달→"AI 견적 초안" 카드 렌더 ③AI 초안 생성 → 노후배관교체 템플릿 7품목·공수 88×45만·합계 132,000,000(VAT 포함)·±5% 대역 125.4M~138.6M 자동 산출 ④승인·발송 → Supabase `quotes/`에 xlsx 실업로드 + `estimate_pdf_url`·`estimate_sent_at` 기록 + "발송 완료·발송본 열기" 표시 ⑤업로드 파일 다운로드 재검증: 유효 xlsx(PK)·살아있는 수식(`D9*F9`·`SUM`·VAT `*0.1`)·제목·품목 텍스트 전부 확인.
- **비고**: 테스트 행 상태가 '견적서 송부완료'로 변경됨 — est-test 일괄 삭제 시 함께 정리됨. 고객측 다운로드 버튼은 `estimate_pdf_url` 존재 시 렌더(코드 검증), OTP 실기기 확인은 사용자 몫.

## §4. 전 작업 검정 스윕 + 4폴더 마감 (사용자 지시)
- **검정 9항목 전수 통과**: ①Playwright MCP(Connected·cmd /c npx) ②성장 게이트 3폴더 실데이터 판정 정확 ③OPTIMIZE.md 4종 ④Hermes placeholder 0(오탐 1건 판명) ⑤견적서 E2E(§3) ⑥배포 동기화+라이브 200 ⑦PMIS Playwright+PR ⑧꽁아코스 validate ⑨memory 2건.
- **검정 중 교정 1건**: 꽁아코스 `validate.mjs` allowlist에 Font Awesome CDN 누락(기존 공백, 오차단 유발) → 1줄 교정 후 PASS.
- **4폴더 마감**: PMIS PR #7 머지(master `ca502f0`, Vercel 자동 배포) · 꽁아코스 하네스 4파일 작업 브랜치 커밋·푸시(`2f09589`) · Hermes는 비-git(파일=기록, OneDrive 동기화) · zerospipe 본 커밋.

## §5. PHASE O-14 — 모바일 간결화 (캡처 2장 지시 전수)
- **지시 전수 (①~⑤)**: ①홈 히어로 위 "AI NATIVE 견적 검증" eyebrow 삭제(필요 이상 요소) ②프로세스 P1/P2/P3 스트립 세로폭 압축 ③단계 카드 아이콘 박스·영문 이중 표기(REQUEST INTAKE 등) 삭제 ④Q&A 문장(faq) 삭제 ⑤전체 설명 1문장 함축("간결·직관 = 나의 컨셉").
- **무엇/어디** (`app/page.tsx`):
  - `renderMobileLanding` — 히어로 eyebrow 삭제 + 같은 류인 2·3페이지 eyebrow("AI NATIVE 정량 분석"·"AI 실시간 정량 분석")도 동일 기준 삭제(제목과 의미 중복). 좌측 축 유지.
  - `renderServiceProcessTab` — ⓐ헤더: 부제 1문장 함축 + P1~P3 범례를 **한 줄 인라인**(2행 그리드→dot+P번호+단축 라벨, "접수·요건 정의/모델링·AI 검증/시뮬레이션·결과 전달") ⓑ"고객 여정 타임라인 (9단계)" 중간 라벨 삭제(제목과 중복) ⓒ단계 카드 = `번호+제목+주체칩 한 줄 → 한 줄 설명 → 산출물 칩`으로 재구성(아이콘 박스·en·faq 필드 삭제) ⓓ9단계 desc 전부 1문장 함축 ⓔ미사용 아이콘 import 3종(MessageCircle·Send·RefreshCw) 정리.
- **규칙화**: AGENTS.md §10-A에 "필요 이상 라벨(eyebrow) 금지" + "프로세스 9단계 = 간결·직관 확정 패턴" 신설.
- **검증**: `ui-ux-pro-max` 선호출(기준파일 MASTER.md 준수) · `npm run build` 0에러 · `ui-quality-gate --check` 차단룰 0건 → `--pass` 마커(R2 경고는 기존 장식 라벨, 본문 아님) · `show_widget` 시안(before→after) 제시 · graphify 188노드 갱신.
