# 2026-06-28 작업정리

## §1. 고객 화면 색 위계 청색화 + 공종 밴드 통일 (PHASE O-3)

사용자 캡처 주석 3장 기준. 메인화면 "AI 분석"과 동일한 청색(`#155EEF`)으로 강조색을 통일하고, 공종 상세 밴드를 견적총액별 방안에도 확장.

### 변경 내역 (항목별)
1. **사업소개 히어로 오렌지 → 청색 `#155EEF`** (`app/page.tsx` renderBusinessTab)
   - 헤드라인 "데이터로" (`text-accent` → `text-[#155EEF]`)
   - "안심 예산 대역 ±5%" — 텍스트·세로막대(`bg-accent`)·밑줄(`bg-accent`) 3요소 모두 청색
   - 푸터 "최적합 견적 검증" 라벨 청색
   - **CTA "무료 견적 검토 신청" 버튼은 오렌지 유지** (행동 유도 1곳만 강조 — AGENTS.md §10)
2. **공종 상세 밴드 라벨 `WORK_ITEM` → `주요 공종`** (`app/page.tsx` renderManualDetail, 약 1534행)
   - 색은 기존 청색 `#155EEF` 유지. 공용 컴포넌트라 전 공종 페이지에 자동 반영.
   - 영어 내부 토큰 → 한글 라벨(AGENTS.md §4 영어 절제 부합).
3. **견적총액별 방안에 동일 밴드 추가** (`app/page.tsx` TRADE_KEYWORDS, 약 91~103행)
   - `small`(온라인 간편검토)=소규모 분기·펌프·밸브 교체·단가 검토
   - `medium`(출장견적)=현장 실측·중규모 배관·계통 검토
   - `large`(프로젝트 사전진단)=대규모 설비·CAPEX·공법·예산
   - `renderManualDetail(selectedBudget)`가 small/medium/large로 호출되므로 키워드만 추가하면 동일 밴드가 자동 생성(밋밋함 해소).

### 의미축(색) 규칙 — 유지 필수
- **파랑 `#155EEF`** = 데이터·AI·검증 / **오렌지** = 행동(CTA)·주의(최대 N% 견적 차이)·비용(수수료 2%) / **네이비** = 본문.

### 검증
- `ui-ux-pro-max` 사후 검증: 권장 팔레트(Trust & Authority = Navy + Blue CTA)에 부합. `#155EEF` on 흰배경 대비 ≈ **5.4:1** → 작은 글씨(푸터 12.5px)도 4.5:1 통과.
- `npm run build` 0에러. `graphify update .` 완료(172 nodes / 41 communities).

### 교훈
- UI 작업은 "단순해 보여도" 착수 **전** `ui-ux-pro-max` 호출이 §0 필수 — 이번에 색·라벨 변경을 단순작업으로 보고 건너뛴 뒤 사용자 지적으로 사후 검증함. 다음부터 선(先)스킬.

---

## §2. 모바일 상단 헤더 정리 + 실적 집계표 KPI 압축 (PHASE O-4)

사용자 캡처 1장(빨간 주석 + X표시) 기준. 모바일 앱 화면의 군더더기 제거 + AI NATIVE 배지 링크화 + 실적 KPI 레이아웃 압축. "깔끔·직관·단조" 추구.

### 변경 내역 (캡처 6항목 전수)
1. **상단 "마이페이지" 칩 삭제** (`components/layout/AppShell.tsx` 약 428행)
   - 로그인 후 표시되던 상단 우측 `마이페이지` 칩 제거 — 하단 탭 `마이페이지`와 중복(AGENTS.md §4 중복 금지). 비로그인 시 `간편 로그인/등록` 버튼은 진입점이라 유지.
2. **AI NATIVE 배지 → 설명 링크 연결** (`AppShell.tsx` 약 451행)
   - 기존 비클릭 `<span>` → `<button>`. 클릭 시 `setActiveTab('sop')`(= 'AI Native 검증' 표준작업절차)로 이동. 모바일 탭은 '서비스 소개'에 매핑(page.tsx 258~260행 동기화 로직).
3. **AI NATIVE 옆 점(녹색 닷) 아이콘 삭제** (동상)
   - `bg-success animate-pulse` 닷 제거(단조·직관).
4. **실적 집계표 설명 단락 삭제** (`components/PerformanceInsights.tsx` 약 158행)
   - "홍보용 과장 수치 대신…" `<p>` 제거, 제목 `ZEROS 실적 집계표`만 유지.
5. **KPI 보조 3종 라벨 단축 + 포지션 압축** (`PerformanceInsights.tsx` 약 166행)
   - 1차에 통째 삭제했다가 사용자 정정("X는 글자 단축·포지션 축소 의미")으로 **복구**.
   - 라벨: `검토 완료율→검토 비율` · `평균 검토 소요→평균 소요` · `진단 공종 수→공종 수`.
   - 크기 축소: 숫자 30→23px, 단위 16→13px, 간격 `gap-x-9→gap-x-6`. 누적 진단 건수(지배지표·유일 컬러)는 유지.

### 검증
- `npm run build` 0에러. `graphify update .` 완료(172 nodes / 41 communities).
- 시각 검증은 시안(`show_widget`)으로 대체(헤드리스 프리뷰 스플래시 정지 — AGENTS.md §8).

### 교훈
- **캡처의 X표시 ≠ 항상 "삭제"**. 이번 KPI는 X가 "글자 단축·포지션 축소"를 의미했는데 통째 삭제로 1차 오독 → 재작업. 다항목 캡처는 애매하면 "이 X가 삭제인지 축소인지" **먼저 확인**(§1-A). 라벨처럼 보여도, 동작 의미도 임의 단정 금지.

---

## §3. 서비스 소개(모바일) 레이아웃 3단계 흐름 개편 + 문장 함축 (PHASE O-5)

휴대폰 앱 "서비스 소개" 화면을 직관·가독·간결 컨셉 유지하면서 레이아웃 개편 → 이어서 긴 문장을 신뢰성·정직성 있게 함축. `ui-ux-pro-max` 선(先)호출(§0 준수).

### 변경 내역 (`app/page.tsx` renderBusinessTab, 약 292행)
1. **레이아웃 개편 — 개편안 B(3단계 흐름)** 시안 3종(`show_widget`) 제시 → 사용자가 B 선택.
   - 기존 `±5% 헤어라인 단일 지표` 섹션 → **`문제(01)→분석(02)→안심(03)` 수직 타임라인**으로 교체(스킬 권고 Scroll-Storytelling 패턴).
   - 단계별 아이콘 노드(AlertTriangle/Database/ShieldCheck) + 연결선 `gray-light→steel→#155EEF`(흐름이 '안심'으로 수렴).
   - `flowSteps` 배열로 콘텐츠 데이터 분리(추후 교체 용이 — §5).
   - `±5%`는 마지막 '안심' 단계 1곳에만 큰 시각화 지표(중복 제거 — §4). 히어로/오렌지 단일 CTA/신뢰 한 줄/푸터는 컨셉 유지.
2. **긴 문장 신뢰성·정직성 함축**(과장 어휘 제거·사실 위주·짧게 — §4 단정적 표현 금지)
   - 히어로 보조문: "…대표님의 견적에 확신을 더합니다." → **"현장 30년 경험과 실거래 데이터로 검증합니다."**
   - 01 설명: "어디까지가 적정가인지…분쟁 위험에 노출됩니다." → **"적정가를 가늠하기 어렵고 분쟁 위험이 큽니다."**
   - 02 설명: "표준 품셈과 1군…단가 거품을 걷어냅니다." → **"표준 품셈과 실거래 데이터로 대조 검증합니다."**
   - 03 제목/설명: "최적합 안심 예산 대역 확정" → **"안심 예산 대역 확정"**(중복 '최적합' 제거) / "…확신을 더합니다." → **"과다·과소 없는 예산으로 결정을 돕습니다."**
   - 푸터 소개문: "현장 실무 경험과 데이터 분석으로 과다·과소 없는 최적합 예산을 산출·검증하는 서비스입니다…" → **"현장 경험과 데이터로 적정 예산을 검증합니다. 시공이 아닌, 결정을 돕는 견적 파트너입니다."**
   - 데이터 지표(30년·246건·98.4%)는 정직한 사실이라 그대로 유지.

### 검증
- `npm run build` 0에러(2회). `graphify update .` 완료(171 nodes / 41 communities).
- 시각 검증은 시안(`show_widget`)으로 대체(헤드리스 프리뷰 스플래시 정지 — AGENTS.md §8).

### 교훈
- 레이아웃 개편은 §2(시안 먼저)대로 3종 시안 제시 후 사용자 선택 → 재작업 없이 1회 반영. 카피는 "과장 어휘 제거 = 신뢰성"으로 직결(ZEROS는 견적 검증 서비스, 단정 금지 — §4).

---

## §4. 서비스 소개(모바일) 히어로 압축 — 헤드라인 2줄화·보조문장 삭제·세로폭 조정 (PHASE O-6)

캡처 주석(빨간 X·지시문) 지시대로 히어로 영역을 압축해 푸터 수평선이 한 화면에 보이도록 정리. `ui-ux-pro-max` 선(先)호출(§0 준수, design-system 일치 확인).

### 지시 전수 처리 (§1-A)
- ① 헤드라인: X표시된 "바로잡아," 삭제 → **2줄 고정**("흔들리는 견적을 데이터로" / "최적의 예산을 책정합니다")
- ② 보조문장 삭제: "현장 30년 경험과 실거래 데이터로 검증합니다." 한 줄 통째 제거
- ③ 세로폭/간격: ①②로 줄어든 만큼 컨테이너·타임라인 간격을 좁혀 **"무료 견적 검토 신청" CTA 밑 푸터 수평선(border-t-2)이 한 화면에 노출**되도록 조정

### 변경 내역 (`app/page.tsx` renderBusinessTab, 약 314행)
1. 히어로 `<h1>`에서 `데이터로 바로잡아,<br />` → `데이터로<br />` (오렌지 강조 '데이터로'는 유지). 보조 `<p>` 블록 삭제. 히어로 `<section>` `gap-4` → `flex flex-col`(단일 자식이라 gap 불필요).
2. 컨테이너 `gap-6 py-6` → **`gap-5 py-5`**. 3단계 타임라인 `<section>` `gap-5 py-6` → **`gap-4 py-5`**. `min-h-[calc(100vh-128px)]`+푸터 `mt-auto` 구조 유지 → 내용 총높이가 가시영역 이내로 줄어 푸터가 하단 앵커에 노출.

### 검증
- `npm run build` 0에러. `graphify update .` 완료(171 nodes / 41 communities).
- 시각 검증은 시안(`show_widget`)으로 대체(헤드리스 프리뷰 스플래시 정지 — §8). 시안에서 2줄 헤드라인·보조문장 없음·푸터 수평선 한 화면 노출 확인.

### 교훈
- 캡처의 X표시는 "삭제" 지시로 일관 처리(O-4의 오독 재발 방지). 세로폭 압축은 새 박스 추가 없이 기존 gap/padding 단계 축소 + `mt-auto` 앵커 활용으로 해결(도형 남발 금지 — §4).
