# 2026-07-07 작업정리

## PHASE O-23 — 실적 화면 캡쳐 마크업 4건 전수

사용자가 실적(performance) 화면 캡쳐에 남긴 4개 마크업 지시를 정밀 판독(헌법 제7조)·전수 처리. ①·④는 해석이 둘 이상이라 만들기 전 `AskUserQuestion` 1회로 확정.

### 지시 → 반영 (1:1 재대조)
| # | 지시(캡쳐) | 반영 | 위치 |
|---|---|---|---|
| ① | 상단 제목 X표시 삭제 → 나머지 "실적 분포" | `공종별 견적 실적 분포 (건수·비중)` → **`실적 분포`** | `PerformanceInsights.tsx` 상단 밴드 h3 |
| ② | "실적 분포"·하단 "실적 히트맵" 폰트 = 견적 건수 수치와 같게 | 두 제목 `text-[14px]` → **`text-[23px]`**(KPI "101"과 동일), 아이콘 16→20px 동반, 세로 리듬 균형 | 상단 밴드 h3 · 히트맵 코너 셀 |
| ③ | 우측 "예상 견적범위" 패널 숨김 고정 + 탭 클릭 시에만 | `AppShell` 자동숨김 useEffect 조건에 **`activeTab==='performance'` 추가** → 실적 화면 기본 접힘, "검토" 세로 엣지 탭 클릭 시에만 펼침(기존 메커니즘 활용, 신규 UI 없음) | `AppShell.tsx` |
| ④ | 세부 실적 "괜한 이중선" 삭제(세부 실적 구간만) | 헤더 컬러 스트립(`TradeStrip`)·각 카드 상단 2px 컬러선(`border-t-2`) 제거 → 회색 헤어라인만 / 카드 무선(간격 구분). 공종 색은 닷·%칩 유지. **상단 분포·히트맵 스트립은 유지** | `PerformanceInsights.tsx` 세부 실적 구간 |

### 사전 확정 (AskUserQuestion)
- ① 최종 제목 = **"실적 분포"**(vs "공종별 실적 분포") → "실적 분포" 확정.
- ④ 이중선 삭제 범위 = **세부 실적 구간만**(vs 3곳 전부) → 세부 실적 구간만 확정.

### 정리 부수
- 스트립 제거로 미사용된 `cardColors` 변수 삭제(lint/TS 경고 방지).
- AGENTS.md §10-A에 "실적 화면 제목·우측패널·이중선(2026-07-07, PHASE O-23)" 조문 신설 — 구 §151("카드 상단보더=공종색")·§154("코너 단독 14px") 대체 부분 명시.

### 검증
- `npm run build` **0 에러** · TypeScript 통과(`cardColors` 정리 확인).
- UI 품질게이트 차단룰 0건 → `--pass` 마커 기록(R2·R3 경고는 기존 장식 라벨, 무관).
- 4항목 각각 코드 재대조 완료(①line182 ②line180·247 ③AppShell line254 ④line317·323).

파일: `components/PerformanceInsights.tsx` · `components/layout/AppShell.tsx` · `AGENTS.md` · `docs/00_orchestration/state.md`.

## PHASE O-24 — 실적 분포·히트맵 공종 8색 스트립 코디(캡쳐 마크업)

사용자 캡쳐 마크업 4건을, 시안(`show_widget`)+`AskUserQuestion`으로 해석 1회 확정 후 반영. 의도 = 위쪽 가로 8색 스트립·히트맵 좌측 색선과 **같은 공종색 모티프로 실적 분포 섹션을 하나로 묶기**.

### 지시 → 반영 (1:1 재대조)
| # | 지시(캡쳐) | 반영 | 위치 |
|---|---|---|---|
| ① | 막대차트 좌측 "수직 무늬" 추가 | 각 막대 행 좌측에 공종색 색선(`grid-cols-[3px_121px_1fr_92px]`) → 이후 지시로 **행 전체 높이(`self-stretch`)+행 균등분할(`flex-1`)로 맞닿게** = 히트맵 좌측처럼 **끊김 없는 하나의 수직 일직선**(공종 8색 분절) | `PerformanceInsights.tsx` 막대차트 L209·216·217 |
| ② | 막대차트 하단에 "똑같이 추가" | 막대 리스트 아래 `TradeStrip colors={distColors}` — 상단 헤더 스트립(L203)과 상·하 대칭 | 막대 컨테이너 하단 |
| ③ | 히트맵 하단에 "같은 모양" 추가 | 합계 행 아래 `TradeStrip colors={workTypeColors}` — 히트맵 상단(L239)과 대칭 | 히트맵 블록 하단 |
| ④ | 히트맵 아래 X표시(밀도 범례) 삭제 | 구 밀도 범례("셀 색=적음→많음" 그라데이션 블록) **완전 제거** — 건수는 셀 `title` 툴팁으로 유지 | 히트맵 블록 하단(구 L302-310) |

### 사전 확정 (시안·질의)
- ① 형태 = **각 막대 좌측 색선**(통짜 세로바 아님) → 이후 "실적 히트맵처럼 일직선" 지시로 조각→연속 일직선으로 재조정.
- ④ = **완전 삭제**(유지 대안 반려, "X표시는 삭제해줘").

### 반복 교정 방지 규칙화 (헌법 제5조)
- "수직 무늬"를 픽셀 스펙(틱/세로바→붙이기→`flex-1`)으로만 반복 매만겨 3회 손봄 = 하네스 실패. 사용자 지시 **"팩트보다 지시사항 맥락을 이해하라"** → `memory/intent-context-over-literal-facts.md` 신설(의도 먼저 1줄 정의·구현 메커니즘 전면 나열 금지·불확실 시 의도 수준 확정).

### 검증
- `npm run build` **0 에러** · `graphify update .`(199 nodes) · UI 품질게이트 차단룰 0건 `--pass`(R2 경고는 기존 장식 라벨).

파일: `components/PerformanceInsights.tsx` · `docs/_worklog/2026-07-07_작업정리.md` · `docs/00_orchestration/state.md` · `~/.claude/.../memory/intent-context-over-literal-facts.md`(전역).
