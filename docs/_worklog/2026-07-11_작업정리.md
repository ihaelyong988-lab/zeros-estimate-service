# 2026-07-11 작업정리

## PHASE O-26 — 모바일 실적 상단 밴드 수평 스와이프

### 지시·범위

주인님 캡처 기준으로 휴대폰의 `실적 분포` 제목과 KPI 4종이 두 줄로 감기며 세로폭을 차지하는 문제만 수정한다. 하단 실적 히트맵과 같은 네이티브 가로 이동을 적용하고, 차트·히트맵·세부 실적·하단 탭·데스크톱 레이아웃은 변경하지 않는다.

### 반영

1. `components/PerformanceInsights.tsx` 상단 밴드의 `flex-wrap`을 제거했다.
2. 내부에 `overflow-x-auto no-scrollbar`와 `min-w-full w-max` 행을 두어, 모바일에서 `실적 분포 → 견적 건수 → 검토 비율 → 평균 소요 → 공종 수`가 한 줄로 유지되고 손가락으로 좌우 이동한다.
3. 제목과 KPI 클러스터에는 `shrink-0`·`whitespace-nowrap`를 적용했다. 데스크톱은 `ml-auto`로 기존 KPI 우측 정렬을 유지한다.
4. `AGENTS.md`에 같은 모바일 불변식을 기록해 다음 작업에서 세로 줄바꿈이 되돌아가지 않게 했다.

### 인증문자 점검 (코드 변경 없음)

- 간편 로그인/등록은 이미 `/api/otp/send` → Solapi 어댑터 → `/api/otp/verify` 구조다.
- 실서비스 `otp/status`는 비활성 상태이며, 로컬 Solapi 변수도 비어 있다. Solapi의 발신번호 등록·API 키/시크릿·서버 전용 환경변수 등록과 재배포가 필요하다.
- 현행 Solapi 공식 메시지 API의 `send-many/detail` 요청 형식과 응답 실패 목록을 어댑터에 반영한 뒤 활성화해야 한다. 보안 보강(무작위 코드·재발송/시도 제한·서버 토큰 검증)은 별도 승인 범위다.

### 검증

- `npx eslint components/PerformanceInsights.tsx` 통과.
- `npm run build`는 샌드박스에서 컴파일 후 worker `spawn EPERM`으로 중단됐으나, 권한 확장 재실행에서 TypeScript·정적 페이지 생성까지 0 오류 통과.
- `git diff --check` 통과.
- 전체 `npm run lint`는 기존 파일(`app/page.tsx`, `MyRequestsModal.tsx`, `MyRequestsView.tsx`, `AppShell.tsx`, `estimate-ops`)의 React hook/any 오류로 실패했으며 이번 변경 파일에는 오류가 없다.

### 오류 원장

- Windows 샌드박스가 `apply_patch`의 분할 writable-root 준비를 거부했다. 처방: 패치 내용은 먼저 `git apply --check`로 검증하고, 승인된 최소 범위 `git apply`로만 반영한다.

## PHASE P-1 — 전수 디버깅 감사 + 기본 데이터 보안 계층 구축

### 지시·범위

주인님: "폴더내 웹의 디버깅 검토와 개선 사항을 전수 조사" → "중대한 결함인가?" → "순차적으로 작업을 하고, 기본적 보안을 설정해줘". 4축(버그·보안·성능·UI) 병렬 감사 후, 최우선 중대 결함(C-1)부터 순차 착수.

### 감사 결과 요약

- 빌드 0에러 · ESLint 8건(전부 React19 신규 룰). CRITICAL 3: **C-1 테이블 RLS 전면 개방(anon PII 유출)** · C-2 관리자 삭제 localStorage 미반영 · C-3 CRM 무한 가산. HIGH 6, MEDIUM/LOW 다수(상세 = 대화 로그·`AGENTS.md §13`).

### 이번에 반영한 것 (기본 보안 계층)

1. **서버 데이터 게이트웨이 신설** `app/api/data/route.ts` — service_role + 신원 검증. 관리자=전체 / 고객 세션=본인 건 / 익명=PII 제거 분석 행 / 쓰기=관리자 / 공개 접수=OTP 토큰 검증 후 서버 단건 채번·생성.
2. **service_role 서버 클라이언트** `lib/supabase/supabaseServer.ts`(신규, 브라우저 노출 금지).
3. **브라우저 서비스 우회** `lib/supabase/client.ts` — `SupabaseZerosService.loadTable/persistTable`가 `/api/data` 호출 래퍼로 교체(anon 직접 쿼리·시드 로직 제거), `createEstimate` 오버라이드로 서버 접수. 접수번호 서버 채번(중복 방지).
4. **fail-closed 시크릿** — `OTP_SERVER_SECRET` 랜덤 폴백(위조 불가), `ZEROS_ADMIN_PASSWORD` 미설정 시 로그인 503(`zeros1234!` 폴백 폐기).
5. **RLS 잠금 SQL** — `supabase/supabase-setup.sql §3` anon `for all` 정책 DROP(재생성 안 함).
6. **H-2 동반 수정** — `PhoneVerifyGate`→`RequestWizard.handleVerified`가 `sessionToken`을 `customerAuth`에 저장. 방문 정보를 `createEstimate` 옵션으로 전달(분리 `createSiteVisit` 공개 호출 제거) + `isSmsConfigured()` 공용화.

### 검증

- `npm run build` exit 0 · TypeScript 통과 · `/api/data` 라우트(ƒ) 등록. 변경 7파일 ESLint 0에러. 로컬 `.env.local`에 필수 env 3종 확인.

### 이어서 완결한 후속 (같은 세션 — "작업을 완료하세요" 위임)

7. **C-2 서버 삭제** — `/api/data` `deleteEstimate` op(관리자 전용, 견적+연관 결제/방문/알림 삭제) + 인터페이스/Base(Mock)/Supabase override + `EstimateDetailModal` localStorage 블록 제거. 이제 삭제가 실 DB에 반영(새로고침 후 부활 없음).
8. **C-3 CRM 멱등화** — `updateEstimate`가 매 저장마다 `total_requests/total_won +1` 하던 것 제거 → `syncCustomerOnStatusChange`(수주성공 '전이' 시에만 won 통계, 되돌리면 상쇄). 의뢰 건수는 접수 시에만 증가.
9. **H-5 레이트리밋** — `lib/otp/rateLimit.ts`(인메모리) + verify(번호5/10분·IP30)·send(번호5/시간·IP20) 429. 서버리스 인스턴스별 베스트에포트(완전방어는 Redis 후속).
10. **월별 차트 동적화** — `PerformanceDashboard` 하드코딩 `['2025-12'…'2026-06']` → 현재(KST) 기준 최근 7개월 매번 생성. 이후 달 누락 해소.
11. **KST 날짜 유틸** — `lib/utils/date.ts` 신설. RightSidebar '오늘 발송'·주간, KanbanBoard 접수일(`toISOString` 크래시 가드 포함)에 적용. UTC 하루 밀림 해소.

### 검증(후속 포함)

- 변경 파일 ESLint 0에러 · `npm run build` exit 0 재확인.

### 배포 (마감처리)

- 순서: (1) master 배포(코드) → (2) Supabase `supabase-setup.sql §3` 실행(주인님) → (3) Vercel env `SUPABASE_SERVICE_ROLE_KEY`·`ZEROS_ADMIN_PASSWORD`·`OTP_SERVER_SECRET` 확인. 코드는 이미 anon 미사용이라 배포만으로 화면 안 끊김, SQL 실행 순간 누출 차단.
