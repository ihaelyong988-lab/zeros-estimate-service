# 2026-07-30 작업정리

## 1. 코드베이스 전수 조사 (읽기 전용)

5축 병렬 서브에이전트(보안·데이터정합·아키텍처·성능·컨셉DNA/접근성) + 기계 채점(tsc·ESLint·npm audit·prod-probe)으로 전수 조사. **신규 73건 + 7/24 정찰분 현존 재확인 27건.**

- 결과 문서: `docs/_worklog/2026-07-30_전수조사_개선계획.md`(P0 3 · P1 12 · P2 31 · P3 54, Wave 12기 실행 설계 포함)
- 계획을 독립 검증 에이전트에 태워 **치명적 결함 3건**을 착수 전에 차단: ①무인 자동 배포가 라이브 로그인·접수를 중단시킬 위험 ②홈 히어로 폰트 16px 상향이 O-37 세로 예산과 조문 모순(루트 `overflow-hidden`이라 잘려 보임) ③Wave 커밋이 없어 롤백 단위가 존재하지 않던 문제.
- 주인님 지시로 **실행 범위를 "위중 3건 / 1시간"으로 축소.** 나머지 97건은 계획 문서에 보존만 하고 미실행.

## 2. 위중 결함 3건 조치 (서브에이전트 3기 병렬, 파일 배타 소유)

### ① 인증번호 화면 노출 차단 (P0 보안)
SMS 키 미설정 상태(현 프로덕션)에서 서버가 인증번호를 응답에 실어 보내고 화면에 표시하던 문제. 타인 휴대폰 번호만 알면 무인증 로그인 → 그 사람의 견적 전문·현장주소·도면·견적서 열람이 가능했다.

- `app/api/otp/send/route.ts` — 응답의 `devCode` 필드 제거(`testMode`는 유지). 인증번호는 서버 로그로만.
- `components/forms/PhoneVerifyGate.tsx` · `components/forms/CustomerLoginModal.tsx` · `components/MyRequestsView.tsx` — `testCode` state·setter·표시 블록(이모지 "⚙️ 테스트 모드" 포함) 삭제. 삭제 자리에 단정형 안내 한 문장.
- **부작용(의도됨)**: SMS 키 등록 전까지 마이페이지 로그인은 인증번호를 받을 수 없다. 접수 위저드는 `/api/otp/status`로 게이트를 건너뛰므로 영향 없다. 지금은 아무나 타인 계정에 들어갈 수 있는 상태이므로 막는 것이 맞다.
- `verify` 라우트의 인증 강제(fail-closed)는 **넣지 않았다** — 키 미등록 상태에서 강제하면 접수까지 중단된다.
- 검증: `devCode`·`testCode` grep 0건.

### ② 익명 API 영업정보 유출 차단 (P0 보안)
`stripEstimate`가 PII 9종만 지우는 차단목록 방식이라, 인증 없는 요청에 견적 품목 단가(`line_items`)·견적금액·확정 계약금액·실주사유·담당자 등이 그대로 반환됐다.

- `app/api/data/route.ts` — **허용목록(`PublicEstimate`)으로 전환.** 공개 실적 화면 실사용 필드만 반환: `id`·`created_at`·`work_type`·`site_type`·`estimate_category`·`status`·`estimate_sent_at`.
- 실사용처(`components/PerformanceInsights.tsx`·`lib/calculations.ts`)를 직접 대조해 `estimate_sent_at`(평균 소요일 산출)을 포함시켰다.
- 차단목록은 `Estimate`에 필드가 추가될 때마다 조용히 누출되므로 구조적으로 폐기. 고객 세션·관리자 경로는 불변.

### ③ 방문 이력이 수주 실적을 삭감하던 버그 (P0 데이터 무결성)
`createSiteVisit`/`updateSiteVisit`이 견적의 현재 상태와 무관하게 `현장방문 예정/완료`로 덮어써, `updateEstimate` → `syncCustomerOnStatusChange`가 수주 취소로 판정해 고객의 `total_won −1`·`total_revenue −계약액`을 실제 DB에 기록했다. '취소' 방문도 '예정'으로 매핑됐다.

- `lib/supabase/client.ts` — `VISIT_SYNCABLE_STATUSES` 화이트리스트(`EstimateStatus` 타입 기반) 신설 + `syncEstimateStatusForVisit()`로 동기화 로직 분리.
  - 방문 이후 단계(견적서 송부완료·수주성공·수주실패 등)는 **상태 보존**
  - `visit_status === '취소'`면 상태 미변경
  - 이미 같은 상태면 저장·알림 생략(멱등)
  - 방문 레코드 저장 자체는 그대로 작동
- `syncCustomerOnStatusChange` 본체는 미변경(호출 조건만 차단).

## 3. 검증

| 게이트 | 결과 |
|---|---|
| `npx tsc --noEmit` | 0에러 |
| `npm run build` | 0에러 (12/12 정적 페이지, `/api/data` ƒ 등록 확인) |
| `ui-quality-gate --check` | 차단룰(R1) 0건 → `--pass` 기록. 경고 R2 3건은 기존 `text-gray-light`(이번 변경과 무관) |
| 적대 검증 | 서브에이전트 3기로 반증 시도(주장별 독립 검증) |

**ESLint 6에러는 그대로 남아 있다** — 전부 이번 조치 범위 밖(`page.tsx`·`AppShell.tsx`·`MyRequestsModal.tsx`의 setState-in-effect·ref-during-render·`any`). 축소된 범위를 지키기 위해 손대지 않았다.

## 4. 범위 밖으로 남긴 것

- 나머지 97건 — `docs/_worklog/2026-07-30_전수조사_개선계획.md`에 보존. 착수는 주인님 지시가 있을 때.
- 판정 보류 8건(조문 모순·반응형 전환·홈 팔레트·홈 푸터 등) — 주인님 결정 필요.
- **주인님 액션**: Vercel에 `SOLAPI_API_KEY/SECRET/SENDER`·`ZEROS_ADMIN_PASSWORD` 등록. 이게 되어야 문자 인증(=마이페이지 로그인)과 관리자 로그인이 실제로 작동한다. 절차 `docs/03_qa_deploy/prod-env-setup.md`, 자동화 `scripts/push-env-vercel.ps1`, 확인 `node scripts/prod-probe.mjs`.

---

# 2026-07-30 (2차) — 2시간 강화 작업

Next CVE 패치를 선행 단독 실행한 뒤(npm install이 병렬 편집과 충돌하므로) 서브에이전트 4기 병렬 + 적대 검증 3기.

## 1. 의존성 — Next.js 16.2.7 → 16.2.12

`eslint-config-next`도 동반 승격(정확 핀 유지, `--save-exact`). Next 자체 HIGH 5건(미들웨어/프록시 우회·Server Actions DoS·SSRF·응답 캐시 혼동 2건·내부 Server Function 무인증 노출) **직접 어드바이저리 0건으로 해소**.

**정정**: 착수 시 전제였던 "postcss·sharp도 함께 해소"는 **성립하지 않았다.** next@16.2.12가 여전히 postcss@8.4.31·sharp@0.34.5를 물고 있어 전이 취약점으로 남는다(overrides 없이는 해소 불가, 범위 밖으로 미조치). `npm audit` 총계는 7건으로 동일하나 구성이 바뀌었다 — next의 직접 취약점이 사라지고 전이 사유만 남았다.

## 2. 첫인상·안정성 (`app/error.tsx`·`global-error.tsx` 신규 / `layout.tsx` / `next.config.ts`)

- **에러 바운더리 신설** — 예외 1건에 네이비 백지로 굳던 것을 복구 UI로 대체. `role="alert"`, 터치 44px, motion-reduce 가드, 기존 색 토큰만 사용. Next 16.2 문서 권고에 따라 `reset` 대신 `unstable_retry`(재요청 후 재렌더) 연결.
- **화면 확대 차단 해제** — viewport의 `maximumScale: 1`·`userScalable: false` 제거. 중장년 대상 서비스의 접근성 위반이었다.
- **정적 자산 캐시 헤더** — `/images/:path*`에 `max-age=31536000, immutable`(기존 라이브 실측은 `max-age=0`).
- **보안 헤더** — `X-Frame-Options: DENY`·`Referrer-Policy: strict-origin-when-cross-origin`·`X-Content-Type-Options: nosniff`. CSP 전체 정책은 인라인 스크립트 충돌로 미도입.
- **manifest 중복 선언 제거**(HTML에 2회 출력되던 것).
- **Pretendard 실제 로드 = 미적용.** 저장소·node_modules 어디에도 폰트 파일이 없고, 신규 다운로드·CDN·npm install이 전부 범위 밖이라 담당 기수가 판단상 미적용했다. **올바른 판단이다** — 적용했다면 글자 폭이 바뀌어 O-37/O-38 픽셀 게이트가 깨졌을 것이다. 적용하려면 `public/fonts/`에 Pretendard 서브셋 woff2를 넣거나 `npm i pretendard` 후 **별도 PHASE로 분리해 Playwright 실측을 동반**해야 한다.

## 3. 데이터·인증 정직성 (`api/data/route.ts` / `lib/otp/*` / `lib/supabase/client.ts`)

- 세션 만료가 200+빈배열로 위장되던 것을 **401 + 원인 메시지**로 구분(관리자/고객 경로별).
- `loadTable` 오류 전파 — DB 장애가 전 화면에서 "데이터 0건"으로 보이던 것 차단.
- 관리자 토큰 **TTL 30일 → 8시간** + 비밀번호 해시 바인딩(비밀번호 교체 = 전 토큰 즉시 무효화).
- 레이트리밋 IP를 XFF **마지막 홉** 기준으로(첫 홉은 클라이언트가 위조 가능해 제한이 무력화됐다).
- `unsign` 길이 비교를 `Buffer.byteLength`로 + try/catch(멀티바이트 위조 토큰의 미처리 500 차단).
- 접수번호 채번 **KST 기준**(UTC라 00~09시 접수가 전일자 번호로 발급되던 문제).

## 4. 크래시·거짓 안내 (`components/admin/*`)

- `estimate_category.toUpperCase()` 무가드 접근 2곳 널 가드(데이터가 비면 화면이 죽던 경로).
- **방문 완료 시 거짓 안내 교정** — 상태가 보존되는 건(수주성공 등)에도 "자동 갱신되었습니다"라고 알리던 것을 실제 변경 여부에 따라 분기.
- 취소된 방문이 '출장예정'으로 표시되고 완료 버튼까지 노출되던 것 분기 추가.
- 알림 이력이 `status`를 무시하고 항상 "발송 성공"으로 표시하던 것 교정.

## 5. 고객 자료 커밋 사고 차단 (`.gitignore`)

`견본/`·`견적서 사본/`·`estimate-ops/`·`ZEROS_DB 구축/`·`ZEROS 작성_0606/`·`ZEROS업데이트 0613.txt`·`.codex/`가 **추적도 무시도 안 된 상태**였다 — `git add -A` 한 번이면 고객 견적서가 공개 GitHub에 올라간다. `.gitignore`에 등재했다. `git ls-files` 확인 결과 추적 중인 파일은 0건이라 `git rm --cached`는 불필요했다.

## 6. 적대 검증이 잡은 치명적 회귀 (배포 전 차단)

검증자 2기가 독립적으로 같은 결함을 지적했다: **`adminSessionInvalid` 401 조기 반환이 익명 허용 분기보다 앞에 있어, 관리자 토큰이 남은 브라우저에서 공개 홈·실적이 통째로 비는 회귀.** 클라이언트 `authBody()`가 고객 모드에서도 관리자 토큰을 함께 실어 보내고, 현재 `ZEROS_ADMIN_PASSWORD`가 미설정이라 **모든 관리자 토큰이 무조건 거부**되므로, 과거 관리자 로그인 이력이 있는 브라우저는 즉시 데이터가 0건이 됐을 상황이었다.

조치: ①익명 허용 분기(견적)를 `adminSessionInvalid` 검사보다 **앞으로** 이동 ②`postData`가 401 수신 시 스테일 관리자 토큰을 폐기(없으면 무효 상태가 localStorage에 영구 고착).

**로컬 서버 실측으로 확인**:

| 요청 | 결과 |
|---|---|
| 토큰 없음 + 견적 | 200 |
| 무효 관리자 토큰 + 견적 | **200** (회귀 차단 확인) |
| 무효 관리자 토큰 + 고객 테이블 | 401 (세션 만료 정직 통보) |

## 7. 검증

`npx tsc --noEmit` 0에러 · `npm run build` 0에러 · `ui-quality-gate --check` 차단룰 0 → `--pass` · lint 9건(6에러 3경고, **이전과 동일 — 늘지 않음**).

## 8. 미실행 (사유 기록)

- **위생·툴체인 기수는 안전 분류기에 차단됐다.** 지시에 `.claude/hooks/ui-quality-gate.mjs`의 차단 동작을 약화시키는 내용(새로 걸리는 위반을 경고로 강등, lint를 차단 사유에서 제외)이 포함돼, 사용자가 요청하지 않은 가드 약화로 판정됐다. **타당한 차단이다.** 게이트 훅은 손대지 않았고, 그 기수의 핵심이던 `.gitignore` 항목만 별도로 처리했다(§5). 남은 항목: 게이트 R1 정규식 보강(`error`·`authError` 미탐지 맹점), `noUnusedLocals` 활성화, lint의 마감 게이트 편입.
- `npm audit fix`로 해소 가능한 brace-expansion·js-yaml 2건 — 범위 확대 금지로 미실행.
- Pretendard 실제 로드 — §2 참조.
