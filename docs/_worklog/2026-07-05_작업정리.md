# 2026-07-05 작업정리

## PHASE O-20 — 실적 화면 캡쳐 마크업 5건 전수 (용어 통일·분리선 스트립·히트맵 코너 제목)

### 무엇을·왜
캡쳐 마크업 5건 지시. 핵심 의도 = ①"진단"→"견적" 용어 통일 ②전부 같은 회색 분리선의 비직관성 해소(분리선이 다음 섹션을 상징) ③히트맵 제목 행 제거로 세로폭 확보 → **하단 합계 행이 스크롤 없이 보이게**. 해석 3건은 `AskUserQuestion`으로 착수 전 1회 확정(코너 셀 이동 / 공종 8색 미니 스트립 / 화면 전체 3곳 용어 통일 — 전부 추천안 채택).

### 어디(파일/위치) — `components/PerformanceInsights.tsx`
1. **① KPI 라벨**: "누적 진단 건수" → **"누적 견적 건수"** (L185). 도형(펄스 점·라벨 15px·숫자 30px 오렌지) 유지, 텍스트만 교체.
2. **② 분리선 컬러링**: 소형 컴포넌트 `TradeStrip`(L70, h-[2px]·w-24·8세그먼트) 신설 → 4곳 적용(KPI 밴드 하단 L211 / 분포차트 밑줄 L221 / 히트맵 상단 경계 L253 / 세부실적 밑줄 L332). 각 스트립 색 = 그 섹션 실제 등장 순서(`distColors`/`workTypeColors`/`cardColors`) — 분리선이 미니 범례 역할.
3. **③ 히트맵 제목 코너 셀 내장**: 제목 h3 행 삭제 → `thead` 좌상단 빈 `<th>`에 2줄("공종 × 견적규모" / "실적 히트맵") + 캡션, 상하 중앙·좌측 축 유지, 코너 폭 120→132px(막대차트 라벨 열과 리듬 통일).
4. **④ 캡션**: "(셀 = 진단 건수)" → **"(셀 = 견적 건수)"** (L264, gray-light→gray로 대비 확보).
5. **막대차트 제목**: "공종별 진단 실적 분포" → **"공종별 견적 실적 분포"** (L220, 화면 전체 통일 확정분). "프로젝트 사전진단"(L25)은 견적규모 카테고리 명칭이라 유지.

### 규칙화 — `AGENTS.md`
- §10-A 조문 신설: "실적 화면 용어·히트맵 제목·분리선(2026-07-05 확정, PHASE O-20)" — 견적 용어 고정 / 히트맵 제목=코너 셀 내장(제목 행 금지) / 분리선=공종 8색 미니 스트립.
- 기존 "실적 KPI 밴드·행간" 조문의 "누적 진단 건수" 표기를 "누적 견적 건수"로 동기화.

### 검증법
- `npm run build` **0에러** · `ui-quality-gate --check → --pass`(차단룰 0건, 잔여 R2 경고는 단위 표기 장식용 gray-light) · `graphify update .` 갱신 · `show_widget` after 시안으로 ①~⑤ 번호별 1:1 재대조 · 사용자 재검토 요청에 재대조 보고 후 마감 승인.
- 잔여 해석 여지 2건 보고함: ⓐ파란 견본 스타일(라벨까지 파랑·확대)은 폴백으로 판독해 기존 도형 유지 ⓑ코너 셀 좌우는 좌측 축 유지(완전 중앙정렬 아님) — 사용자가 수정 지시 없이 마감 지시 → 현행 확정.

### 커밋
- 브랜치 `fix/ui-performance-o20` 커밋 `7e667cf` → master 머지 `2ed7a36` 푸시(`d529d7f..2ed7a36`, 호스팅 자동 배포 트리거).

### 오류 원장 (헌법 제8조)
- `AGENTS.md` Edit 시도 시 "File has not been read yet" 1회 거부 — 원인: 세션 내 미독 파일을 곧바로 Edit. 처방: **Edit 전 대상 구간을 Grep/Read로 먼저 확보**(부분 읽기면 충분). 하네스 안전장치가 정상 차단, 데이터 손실 없음.

---

## PHASE O-21 — 첨부파일 보안 잠금 (관리자·본인만 열람, 이력만 공개)

### 배경 / 왜
- 사용자 질의로 데이터 흐름 전수 판독 → 업로드 파일이 **공개 URL**(URL 아는 누구나 열람) + 관리자 비밀번호가 **클라이언트 번들에 하드코딩**돼 있음을 확인.
- 사용자 지시: "아무나 열어보면 안 되지요. 이력만 노출되고, 관리자만 열어보고 다운로드할 수 있도록."

### 무엇을 / 어디
1. **버킷 비공개 SQL** — `supabase/supabase-setup.sql` §5 신설: estimate-files 관련 스토리지 정책 전부 정리 → 익명 **업로드(insert)만** 재허용 → `storage.buckets.public=false`. (사용자가 Supabase SQL Editor에서 1회 실행)
2. **서명 URL 발급 API** — `app/api/files/sign/route.ts` 신규: 관리자 토큰(전체 허용) 또는 고객 세션 토큰(본인 휴대폰 번호로 접수된 건의 파일만 허용) 검증 후 10분짜리 signed URL 발급. service_role 키(`SUPABASE_SERVICE_ROLE_KEY`, 서버 전용) 필요. 레거시 공개 URL도 경로 추출(`refToPath`)로 호환.
3. **관리자 로그인 서버 이전** — `app/api/admin/login/route.ts` 신규(env `ZEROS_ADMIN_PASSWORD`, 미설정 시 기존 기본값) + `components/admin/AdminLogin.tsx`에서 하드코딩 비밀번호 제거, 성공 시 관리자 토큰(30일) 저장. `ShellContext.logoutAdmin`이 토큰도 폐기.
4. **토큰 모듈 확장** — `lib/otp/token.ts`: `createSession/checkSession`(고객 30일)·`createAdminSession/checkAdminSession`(관리자 30일). `/api/otp/verify` 응답에 `sessionToken` 추가, `CustomerAuth.sessionToken` 필드 신설(MyRequestsView·CustomerLoginModal 저장).
5. **업로드 경로 저장** — `lib/supabase/storage.ts`: getPublicUrl 제거, `FileMeta.file_path`(신설, `types/estimate.ts`)에 Storage 경로 저장·`file_url`은 빈 값(레거시 호환 유지).
6. **열람 UI 교체** — `lib/files/secureFile.ts` 신규(`openSecureFile` 헬퍼): 관리자 상세 모달 첨부 링크·"보기"·"발송본 열기", 고객 마이페이지 "견적서 다운로드"를 전부 서명 URL 경유 버튼으로 교체. 견적서 발송 시 `estimate_pdf_url`에 경로 저장.
7. **환경변수** — `.env.local`에 `SUPABASE_SERVICE_ROLE_KEY`·`ZEROS_ADMIN_PASSWORD` 항목 추가(값은 사용자 기입, Vercel에도 등록 필요).

### 검증법
- `npm run build` **0에러**(신규 라우트 `/api/admin/login`·`/api/files/sign` 등록 확인) · `ui-quality-gate --check → --pass`(차단룰 0건) · `graphify update .` 갱신.
- 발효 순서(사용자): ①Vercel에 `SUPABASE_SERVICE_ROLE_KEY` 등록 → ②배포 → ③SQL §5 실행. 잠금 후 관리자는 재로그인 1회 필요(토큰 발급).

### 규칙화
- `AGENTS.md` §12 갱신: 파일 링크 UI는 `<a href={file_url}>` 직결 금지, `openSecureFile()` 경유 고정 + 보안 구조 요약.

### 오류 원장 (헌법 제8조)
- supabase-js 서버 라우트 타입: `ReturnType<typeof createClient>` 파라미터 타이핑 → select 행이 `never` 추론 / 제네릭 불일치 빌드 실패 2회. 처방: **`SupabaseClient` 타입 임포트로 파라미터 선언 + 결과는 `as { data: T }[]` 캐스팅.**

### 커밋
- 브랜치 `fix/secure-file-access` 커밋 `37f7f9e` → master 머지·푸시(호스팅 자동 배포 트리거).

## 진단 — "전체 작업이 앱에서 변화 없음" 보고 원인 격리 (2026-07-05)

### 판정 결과: 배포 정상 · 원인 = 사용자 기기 캐시(설치형 PWA/브라우저)
- git: 로컬 master == origin/master == `e231c27`(O-21 머지까지 전부 푸시 완료) — 미배포 커밋 0건.
- 라이브 검증(신규 원격 판정법): `zerospipe.co.kr` HTML의 `/_next/static/chunks/*.js` 10개 전량 다운로드 → **"누적 견적 건수"·"공종별 견적 실적 분포"(O-20 신규 문구) 존재, "누적 진단 건수"(구 문구) 0건** = O-18~O-20 UI 변경이 라이브 번들에 실재.
- 서비스워커: `public/sw.js`=kill-switch(캐시 전삭제·자가 해제)·`app/layout.tsx` unregister 스크립트 기배포 — 구조 정상. 설치형 PWA는 완전 종료 후 **2회** 실행 필요(1회차=캐시 정리, 2회차=새 화면).
- 처방: 코드 무수정. AGENTS §8에 **원격 판정법**(라이브 청크 마커 grep → 배포/캐시 1회 격리) 조문 신설.

### 함께 커밋
- O-21 잔여 기록: AGENTS §12 오류 원장 ②(sb_secret 키 REST 호출은 apikey 헤더) + state.md 0-A 발효 완료 갱신.

## PHASE O-22 — 실적 상단 밴드 개편(캡쳐 4건 전수): 지배 KPI 폐지·분포 헤더 상단행·코너 제목 단독·표 압축

### 지시(캡쳐 마크업)
- ①X 삭제: 좌측 "누적 견적 건수 101건" 블록 전체 삭제, 우측 클러스터에 "견적 건수 101건"을 보조와 동일 스타일로 편입(도형 샘플 그대로 — KPI 4종 균일).
- ②화살표: "공종별 견적 실적 분포 (건수·비중)" 헤더를 최상단으로 이동(좌=헤더/우=KPI 4종 한 행) → 세로폭 확보.
- ③취소선: 히트맵 코너 셀에서 "공종 × 견적규모"·"(셀 = 견적 건수)" 삭제 → "실적 히트맵"만 12→14px 확대.
- ④실적표 세로폭 축소(셀 py-2.5→1.5, 행 pitch ≈37→30px) → 합계 행 첫 화면 노출. 막대 차트 296→240px로 pitch 동기 유지.

### 파일 / 검증
- `components/PerformanceInsights.tsx`(상단 섹션·코너 th·행 패딩·차트 높이) · `AGENTS.md` §10-A 조문 2건 갱신(지배지표 조문 폐지→상단 밴드 최종 / 코너 문구 재확정).
- ui-quality-gate 차단룰 0건 → `--pass` · `npm run build` 0에러 · show_widget 시안 재대조.
