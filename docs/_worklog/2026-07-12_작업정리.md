# 2026-07-12 작업정리

## PHASE O-28 — 실적 분포·히트맵 공유 세로 그리드 + 폰트 한 단계 업 (캡쳐 녹색선 마크업 4항목)

**무엇을·왜**: 사용자 캡쳐 마크업(녹색 세로선 + "행렬을 직관적으로 맞춰 달라") — 분포 막대 차트와 히트맵이 각각 독립 열 폭을 써서 행렬이 어긋남.

1. **① 라벨 좌측 정렬**: 분포 차트 공종 문구를 히트맵 행 라벨 미러 구조(3px 공종색 `borderLeft` + `pl-3` 좌측 정렬)로 교체 — 구 `text-right` + 별도 색선 `<span>` 폐지.
2. **② 순번 통일**: `distribution` 건수 내림차순 정렬 제거 → 히트맵과 동일한 `WORK_TYPES` 순서 고정. 파생 버그 예방: 막대 최대값을 `distribution[0]` 가정 대신 `Math.max(...values)`로 계산.
3. **③ 세로선 맞춤(공유 그리드)**: 라벨 열 **176px**·건수/합계 열 **104px**을 두 섹션이 공유 — 분포 행 grid `[176px_1fr_104px]`, 히트맵 코너 th `w-[176px] min-w-[176px]`(구 132px), 합계 th `w-[104px]`. 레일 시작 = 셀 시작, 건수 열 = 합계 열.
4. **④ 폰트 한 단계 업(분포+히트맵 전체, AskUserQuestion으로 범위 확정)**: 분포 라벨 12→13·건수 13→14·'건'/% 11→12, 히트맵 표 기본·헤더 12→13px.

- **어디**: `components/PerformanceInsights.tsx` · 규칙화 `AGENTS.md §10-A`("실적 분포·히트맵 = 공유 세로 그리드 + 13px" 조문 신설).
- **검증**: `npm run build` 0에러 · `ui-quality-gate --check→--pass`(차단룰 0) · `show_widget` 반영 후 시안으로 세로선 일치 확인 · graphify 갱신.

## PHASE O-29 — Footer 이동·사업소개 균형·SOP 카피·금액미정 헤드라인·뒤로가기 종료 가드 (다항목 지시 7건)

**무엇을·왜**: 사용자 다항목 지시 전수(①~⑦).

1. **① 사업소개 Footer 삭제 + 여백 균형**: 인라인 `<footer>`(구 `app/page.tsx` L389~423) 제거. Footer 여백을 언더라인 이하 콘텐츠에 배분 — 3단계 타임라인 섹션 `flex-1 justify-center` + 스텝 `gap-7→gap-9`·`py-7→py-8`, 루트 `gap-5 py-5→gap-6 py-6`, CTA `pb-5`.
2. **② Footer를 예상견적 의뢰 탭 하단으로 이동**: `renderBrandFooter()` 공용 렌더로 추출 → `renderRequestTab` 루트를 `flex flex-col min-h-[calc(100vh-128px)]`로 바꾸고 위저드 폭(max-w-3xl) 정렬 + `mt-auto` 하단 앵커로 배치.
3. **③ AI NATIVE 검증 헤드라인 정정**: "도면 한 장이 검증된 예산 검토서가 되기까지" → **"DATA가 도면으로, 검증된 예산안이 되기까지"** (navy + accent span "검증된 예산안" — 컬러 체계 유지).
4. **④ SOP 최소 폰트 1단계 업**: 화면 내 최소 `text-[11px]` 2곳(방법론 칩·io 데이터 변환 라벨) → **12px**.
5. **⑤ 공사규모·금액 미정 헤드라인 추가**: 원인 = `TRADE_KEYWORDS`에 `'unknown'` 키만 누락(다른 견적규모 3종은 존재) → `'unknown': ['규모 산정', '예산 방향', '전문가 상담']` 추가로 상세 화면 상단 헤드라인 밴드 표시.
6. **⑥ 휴대폰 인증 메시지(OTP SMS) 자동화 — 조사 결과(코드 무변경)**: 발송 코드는 Solapi 연동으로 이미 완비(`lib/sms/send.ts`, `isSmsConfigured()` 시 실발송·미설정 시 테스트모드). **로컬 `.env.local`의 `SOLAPI_API_KEY/SECRET/SENDER` 3종이 빈 값 + 라이브 `/api/otp/status` = `enabled:false`** → 잔여 작업은 Solapi 계정 자격증명 발급·등록뿐(사용자 액션, 하단 "다음 할 일" 0-C 갱신).
7. **⑦ 뒤로가기 앱 닫기 확인 팝업 신설**: `AppShell`에 종료 가드 — 모바일 레이아웃 진입 시 최초 히스토리 엔트리를 `zerosExitGuard`로 치환하고 동일 URL을 한 층 push. 스택 바닥 popstate 도달 시 "앱을 닫으시겠습니까?" 팝업(닫기 = `window.close()` 베스트에포트, 실패 시 다음 뒤로가기에서 OS 종료 / 계속 사용 = 가드 재장전). 접근성: `role="dialog"` `aria-modal` · 버튼 `min-h-[48px]` · `motion-reduce` 존중.

- **어디**: `app/page.tsx`(①~⑤) · `components/layout/AppShell.tsx`(⑦) · 규칙화 `AGENTS.md §10-A`("Footer 위치·사업소개 균형·SOP 카피·종료 가드" 조문 신설).
- **검증**: `npm run build` 0에러(1차 파스 에러 — JSX 주석을 `return (` 직후에 배치 → JS 주석으로 이동 수정) · `ui-quality-gate --check→--pass`(차단룰 0, R2·R3 경고는 기존 코드) · graphify 갱신.
- **오류 원장(제8조)**: `return (` 바로 다음 줄의 `{/* */}` JSX 주석은 JSX 요소 밖이라 파스 에러 → 처방: 컨테이너 요소 안쪽 또는 `return` 위 `//` 주석으로 배치.
