# 2026-07-24 작업정리 — 프로젝트 폴더 OneDrive 이탈(용량·빌드 안정화)

## 배경
방문자 관점 앱 점검 착수 중 "폴더 용량이 큰데 문제가 무엇인지" 지시 → 전수 실측 결과, 재생성 가능한 산출물이 OneDrive 동기화에 묶여 있는 구조가 근본 원인으로 드러남.

## 실측 (처리 전)
- 폴더 총량 **1,130MB / 39,192파일**. 이 중 재생성물이 97.5%:
  - `.next` 600MB(그중 `.next/dev` 579MB) · `node_modules` 500MB/32,470파일 · `.git` 15.3MB(loose 1,981, pack 0).
  - 실제 소스(app+components+lib+types) = **0.8MB(전체의 0.07%)**.
- 폴더·파일 속성 = `PINNED`(OneDrive "항상 이 장치에 유지"). dev/build마다 수천 파일이 갈릴 때 OneDrive가 파일 핸들을 잡아 **빌드 지연·EPERM 위험**. (`du -sh .`가 120초 타임아웃 = I/O 부하 증거.)

## A안 실행 (무손실 즉시 회수) — 완료
1. `.next` 삭제 → 600MB 회수(다음 build 시 재생성).
2. `git gc` → loose 1,981개 → pack **8.79MB**, 명령 속도 개선.
- 결과: 폴더 1,130MB → 521.8MB.

## B안 시도 → 폐기 (Turbopack 비호환 실증 2회)
- node_modules를 `C:\dev\zeros-cache`로 junction 이전: OneDrive 동기화 1,130MB→**22MB** 성공했으나 빌드 실패.
  - Turbopack: `Symlink [project]/node_modules is invalid, it points out of the filesystem root`.
- `.next`만 junction 유지: 빌드 실패 — 청크가 실제 경로 기준 node_modules 탐색 → `Cannot find module '@tailwindcss/postcss'`.
- 근거: Next.js 16 docs `turbopack.md` — 루트 밖 파일 미해석. 우회하려면 `turbopack.root`를 공통 상위 `C:\`로 잡아야 하나 그건 드라이브 전체 감시라 폐기.
- **판정: junction 방식은 Next.js 16 Turbopack과 근본 비호환.** junction·임시 캐시(`C:\dev\zeros-cache`) 전량 제거 → 클린 빌드 `✓ Compiled successfully` 복구.

## C안 실행 (근본 처방·사용자 승인) — 새 위치 검증 완료
- 신위치 **`C:\dev\zerospipe.co.kr`** (OneDrive 밖).
- robocopy로 소스 전량 복사(node_modules·.next만 제외, `.git`/`.claude`/docs/public/개인파일 6종 전부 포함).
- 신위치에서 `npm ci`(lockfile 결정적 설치) + `npm run build` → **`✓ Compiled successfully in 18.9s`**, 전 라우트 정상, `.env.local`(17줄) 인식.
- git 무결성: origin 동일, HEAD `902468b` 일치.
- 경로 기반 자동 메모리 이관: `~/.claude/projects/C--dev-zerospipe-co-kr/memory/`(19파일, MEMORY.md 포함).

## 남은 handoff (사용자 액션 — 이 세션에선 불가)
1. **이 Claude Code 세션(구 OneDrive 경로) 종료** → `C:\dev\zerospipe.co.kr`에서 새로 열기.
2. 새 위치에서 `npm run dev`로 실동작 1회 확인.
3. 확인 후 **OneDrive 원본 폴더 삭제**(소스=GitHub, 개인파일=신위치에 복제됨 → 안전). 삭제 시 OneDrive 클라우드에서도 제거됨(정상).

## 미결(별도 스레드)
- 방문자 관점 MECE 10축 정찰 워크플로 결과 → 개선 계획서 수립·승인은 **미완**(용량 이슈로 중단). 코드 동일하므로 신위치에서 재개 가능.

---

## 세션2 (2026-07-24 오후) — 구 원본 삭제 + OneDrive 속 dev 앱 8개 `C:\dev` 이관

### A. 구 ZEROS OneDrive 원본 삭제 완료
- 신위치 재검증: git HEAD `902468b`=origin 3자 일치 · `npm run build` 0 · `npm run dev` HTTP 200(title "ZEROS…") · env 키 8종.
- 구 원본 `OneDrive\바탕 화면\zerospipe.co.kr` 삭제. "폴더 사용 중" 차단 → Restart Manager로 실잠금 0(=탐색기 창) 확인 → **재부팅 후 삭제**.

### B. 활성 앱 5개 이관 (node_modules+.next 보유 = 빌드-동기화 충돌 주범)
방법=robocopy(`/XD node_modules .next`, 미커밋·`.env`·untracked 보존) → npm ci/pnpm install → build 0.

| 앱 | 신위치 | 검증 | 원격 |
|---|---|---|---|
| CanaryLab | `C:\dev\CanaryLab` | npm ci·build 0 | canarylab.git |
| cheonchaekbbang | `C:\dev\cheonchaekbbang` | pnpm install 0(corepack 11.17) | cheonchaekbbang.git |
| pm-prompt-dashboard | `C:\dev\pm-prompt-dashboard` | npm ci·build 0 | pm-prompt-dashboard.git |
| 출장견적서비스0602 | `C:\dev\출장견적서비스0602` | npm ci·build 0 | hanes-estimate-service.git |
| piping-bot | `C:\dev\piping-bot` | 34/34파일·npm ci·build 0 | ⚠ git 없던 유일본 → 로컬 git init(`0976c33`), 원격 push 미완 |

### C. GitHub 백업 있는 dev repo 3개 이관 (비-node → robocopy+git 무결성만)
- 개인용PMIS(personal-pmis.git·`804e4fe`) · 꽁아코스(gongacourse.git·`5b5033d`) · seomdam(seomdam.git·`075766c`) → `C:\dev\`, HEAD 동일.
- **판정: 로컬전용 비-node 자료폴더는 OneDrive 잔류** — PPT추출 464MB·Hermes 155MB·[SE7125]천안 18MB·New project 60MB 등은 빌드 정크 0이라 옮길 이득 없고, 옮기면 클라우드 백업만 소실.

### 남은 handoff (주인님 · 본인 PowerShell)
- 이관 원본 8개 OneDrive 삭제(`Remove-Item -LiteralPath "…" -Recurse -Force`). **piping-bot은 GitHub push 선행**(`gh repo create piping-bot --private --source=. --push`).
- 빈 껍데기 삭제: `문서\New project 2`·`3`·`4`(0MB, `.git`만).
- 앞으로 개발은 `C:\dev\<앱>` 사본에서(OneDrive 원본 아님).
