# 2026-07-04 작업정리

## PHASE O-17 — 견적 검토 캡쳐 지시 전수(FLOW 폰트·히어로 균형) + 공종 실사 이미지 16장 리서치·연결

### 배경 / 지시 (캡쳐 1장 + 후속 질문)
- 캡쳐: ①"견적 작업 FLOW" 박스 내부 폰트 크기 가독성 조정 ②-1 히어로 좌측 eyebrow "● 현재 검토 공종" X 삭제 ②-2 타이틀 아래 키워드 줄 X 삭제 ②-3 좌·우 패널 균형 리디자인(좌=실사 사진 상·하, 우=좌측과 균형 있는 직관적 결과 지표).
- 후속 "리서치해서 실사 이미지 넣었나?" → 실제 이미지 리서치·연결 지시로 수행.

### 반영
1. **FLOW 폰트 위계 상향** (`components/EstimateFlow.tsx`): 탭 13→14px(py-2.5), 단계 제목 16→18px, 설명 13.5→14.5px(+break-keep), 헤드라인 16→17px, 안내문 `text-gray-light` 12.5px→`text-gray` 13px(대비 4.5:1).
2. **히어로 좌측 정리** (`app/page.tsx` `renderReviewDesktop`): eyebrow(닷+"현재 검토 공종")·키워드 줄 삭제 → 공종 타이틀 26px이 최상단 기준선. 미사용 `keywords` 변수 제거.
3. **좌·우 균형 리디자인**: 우측을 상단(제목 "사전 견적 후 결과 지표"+benefit 3줄) · 중앙(−N% 주인공+before→after 그래프, `flex-1 justify-center`) · 하단 앵커(신뢰도·표본 + 절차 캡션, 좌측 사진 하단과 라인 맞춤) 3단 구조로 재배치 — 우측 하단 빈 공간 제거, 양쪽 세로 높이 일치.
4. **공종 실사 이미지 16장 연결**: Pexels 무료 라이선스(상업 이용·출처표기 불요) 이미지를 공종별 [현장 전경, 작업 상세] 2장씩 리서치 → **16장 전수 육안 검증**(부적합 4장 — 인쇄기계·도자기공장·복장불량 용접·주류탱크 문구 — 교체) → `public/images/trades/`(3.0MB, 1200px 압축) 자체 호스팅 → `TRADE_PHOTOS` 데이터 맵(`app/page.tsx`)으로 히어로 슬롯에 `object-cover` 연결 + 좌하단 라벨 칩 + 시그니처색 인셋 링, 미등록 공종은 플레이스홀더 폴백. **자사 현장 사진 확보 시 파일 교체만으로 갱신 가능(경로 유지).**

### 검증
- `ui-quality-gate --check` 차단룰 0건 → `--pass` · `npm run build` 0에러 · `graphify update .` · 이미지 16장 Read 도구로 전수 육안 확인 · `show_widget` 결과 시안.

### 파일
- `components/EstimateFlow.tsx` · `app/page.tsx`(`TRADE_PHOTOS`·`renderReviewDesktop`) · `public/images/trades/*.jpg` 16장(신규)

## PHASE O-18 — 실적 대시보드 KPI 한 줄 압축 + 그래프·히트맵 행간 동기화

### 배경 / 지시 (캡쳐 1장, 마크업 2건)
- ①타원(누적 진단 건수 101건): "폰트 크기 같게 하고, 가로배치해서 상단 여백만큼 전체 페이지를 올리세요. 타원형 도형 내용만"
- ②사각형(막대그래프+히트맵): "상·하 그래프와 표의 행간 간격을 같게 해서 가독성을 높이세요"
- AskUserQuestion 1회 확정(헌법 제3조): ①폰트=보조 KPI와 동일 ②그래프를 히트맵에 맞춤.

### 반영 (`components/PerformanceInsights.tsx`)
1. **KPI 한 줄 가로배치**: "누적 진단 건수" 3줄 스택(라벨 12.5px/숫자 26~36px/밑줄) → 라벨 12px+숫자 23px **한 줄**(보조 3종과 동일 폰트). 밑줄 삭제, 오렌지·펄스 닷 유지. 절약된 높이만큼 페이지 상향. 우측 보조 KPI는 무변경(범위 규율).
2. **행간 동기화**: 막대 차트 컨테이너 `h-[224px]` → `h-[296px]`(8행 × 히트맵 행 pitch ≈37px). 구 "224px 규격(2026-07-03)"은 본 지시로 대체 — 주석·AGENTS §10-A 갱신.
3. **규칙화**: AGENTS.md §10-A에 "실적 KPI 밴드·행간(2026-07-04 확정)" 조문 추가. (같은 커밋에 헌법 제7조 전 폴더 배포분 AGENTS/CLAUDE 갱신 포함.)

### 검증
- `ui-quality-gate --check` 차단룰 0건(R2 경고=기존 장식 단위 라벨) → `--pass` · `npm run build` 0에러 · `show_widget` before→after 시안 1:1 재대조 · `graphify update .`

## PHASE O-19 — 캡쳐 4장 전수: 사업소개·견적검토·실적 KPI·SOP 균형 조정

### 배경 / 지시 (캡쳐 4장, "빠짐없이 작업하고 기록할 것")
- ①사업소개: 헤드라인 한 줄+프레임 맞춤 축소, 절약 여백만큼 3단계 섹션 폰트·여백 확대.
- ②견적검토: FLOW 스트립 폰트=하단 다크밴드와 동기화(상하 균형), 좌측 이미지 패널 호평(무변경), 우측 열=좌측과 대칭(폰트 확대·세로폭 확장·살짝 하향).
- ③실적 KPI: O-18 "보조와 동일 23px" 재지적 → 누적 건수만 확대(AskUserQuestion 확정, 한 줄 유지).
- ④SOP: 6개 카드 간 여백 확대("답답함" 해소).

### 반영
1. **사업소개** (`app/page.tsx` `renderBusinessTab`): 헤드라인 `clamp(30,4.4vw,50)`→`clamp(30,3vw,38)`+`md:whitespace-nowrap`+`<br className="md:hidden">`(데스크톱 한 줄·모바일 2줄 유지) · 3단계 라벨 12→13px·제목 17→19px·설명 14→15.5px·±5% 44→52px·gap-5→7·py-5→7.
2. **견적검토** (`components/EstimateFlow.tsx`·`renderReviewDesktop`): FLOW 제목 17→21px(밴드 21px 동기)·탭 14→15.5px·안내문 13→13.5px / 우측 제목 20→26px(좌측 타이틀 대칭)+pt-2 하향·bullet 13→15px·최적화 라벨 13→15px·그래프 라벨 11.5→13px·바 h-2.5→3·세부 라벨 11.5→13px·숫자 15→17.5px·pt-3→5·절차 캡션 13px·mt-2.5→4. −34.2% 54px 주인공 유지.
3. **실적 KPI** (`components/PerformanceInsights.tsx`): 누적 진단 건수 라벨 12→15px·숫자 23→30px·단위 13→15px(한 줄·오렌지·펄스 닷 유지), 보조 3종 12/23px 무변경. **AGENTS §10-A 조문 수정**: "4종 동일 폰트" 폐기 → "지배지표 축소 금지(15/30)" + 견적검토 상하 균형 조문 신설.
4. **SOP** (`renderSopTab`): 카드 간 gap-2→3.5(8→14px)·카드 px-3.5 py-2.5→px-4 py-3·하단 지표 pt-3→4.

### 검증
- `ui-quality-gate --check` 차단룰 0건(R2 경고=기존 장식) → `--pass` · `npm run build` 0에러 · `show_widget` 시안 재대조 · `graphify update .`

### 파일
- `app/page.tsx`(`renderBusinessTab`·`renderReviewDesktop`·`renderSopTab`) · `components/EstimateFlow.tsx` · `components/PerformanceInsights.tsx` · `AGENTS.md` §10-A
