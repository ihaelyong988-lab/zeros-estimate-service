# 2026-06-21 작업 정리 — 하네스 엔지니어링 & 고객 UI 정밀 정리 (PHASE M)

## 1. 폴더 구조 하네스 정리 (커밋 `e8d337a`)
- 루트 디클러터(전부 rename 100%·이력 보존): `*.pptx`→`docs/assets/`, `supabase-setup.sql`→`supabase/`, `open-browser.bat`·`open_chrome.ps1`→`scripts/`
- docs 위생: 날짜 작업로그를 `docs/_worklog/`로 집결, 빈 `docs/99_scripts/` 제거
- `AGENTS.md` 상단에 **저장소 지도(Repo Map)** 신설 + `README.md`·`docs/README.md` 경로 참조 갱신

## 2. 세션 연속성 하네스 (`.claude/` — git 제외, 로컬 전용)
- `SessionStart` 훅(`.claude/hooks/session-start.mjs`): 새 세션마다 `state.md` 현재단계·다음할일 + 최신 worklog 자동 주입
- `Stop` 훅(`.claude/hooks/stop-graphify.mjs`): 코드(.ts/.tsx) 변경 남으면 세션당 1회 `graphify update .` 안내
- 슬래시커맨드 4종(`.claude/commands/`): `/마감처리` `/이어서` `/시안` `/상태저장`

## 3. 고객화면 UI 정밀 정리 (커밋 `911c0d3`) — 레이아웃 유지·스타일/삭제만
- **좌측 사이드바(A·미니멀 프로)** `components/layout/LeftSidebar.tsx`: 섹션 색막대 3종 제거→액센트 steel 단일화, 항목 굵게→보통(활성만 강조), 활성 화살표 제거. '블로그 위젯' 느낌 탈피.
- **사업소개 히어로** `app/page.tsx`: CTA 2개(무료 견적 의뢰·AI 검증 절차 보기) 섹션 삭제.
- **CAPEX 히어로** `app/page.tsx`: 신뢰칩 도형(알약·배경·그림자·원형아이콘) 제거 → 헤어라인 언더라인(탭 오인 방지).

## 4. 검증 & 배포
- dev 프리뷰(1440px) 실측: 사이드바 헤더 `#9AA3AF`/10.5px/색막대 0개·항목 weight 500, CTA 2개 부재, 칩 배경 none·radius 0·하단 보더만.
- `npm run build` 0에러(컴파일·TS·정적 9/9) → master 머지·푸시(`b3deeff..22c9e94`)로 자동 배포.

## 다음 이어서 할 일
- (기존) Vercel/호스팅 배포 점검·카카오톡 공유, Toss 결제 실서버 연동, AI 도면 스캐너 모델 고도화
- (하네스) 필요 시 보류분 확장: 계층2(매 프롬프트 표준 주입)·계층4(권한 허용목록 확장)
