# 2026-08-08 작업정리 — 최적화 ①단계(삭제)

> 지시: "마네킹 지워, ①단계 진행" · 근거 = `docs/00_orchestration/최적화-계획서.md` §3
> 브랜치 `refactor/stage1-delete` · 기준 `f8bed33` · 수정 5기 + 적대 검증 1기 · 1파도

---

## 1. 결과

**소스 16,635 → 14,657줄 (−1,978줄, −12%) · 파일 68 → 66개**

| 항목 | 처리 |
|---|---|
| **A1 마네킹(Mock 폴백)** | `mock-data.ts` 1,406줄 + `MockZerosService` + `SEED_VERSION` + 삼항 분기 + 채점 테스트 = **1,513줄 삭제.** `.env.example`·`README` 동기 |
| A2 `getCategoryVisuals` 죽은 반환 필드 3종 | 삭제 |
| A3 `MobileSimulator` | 컴포넌트·테스트·컨텍스트 4지점 = 131줄 삭제 |
| A4 `getDynamicMetrics` 죽은 반환 필드 5종 | 삭제 |
| A5 `isSupabaseEnabled` 도달 불가 분기 2곳 | 삭제 |
| A6 `zeros_admin_users` 표면 일체 | 17줄 삭제 (**DB 테이블은 보존**) |
| A9 쓰기 전용 필드 (`next_action`·`last_contact_at`) | 8줄 삭제 |
| A10 저장 카운터 가산 경로 | 13줄 삭제 (**타입 필드는 보존** — 아래 참조) |
| A11 참조 0건 타입 필드 2종 | 삭제 |
| A12 `create-next-app` 잔재 SVG 5개 | 삭제 |
| A13 `hasServiceKey`·`next.config` 중복 항목·`eslint` 제외 보강 | 처리 |
| **A7 `Estimate.payment_status`** | **차단 — 기수가 스스로 멈췄다** |
| A8 SQL §4 중복 블록 | 삭제 (순효과 0 확인) |

## 2. 기수가 스스로 멈춘 것 — 지시보다 코드를 믿었다

**A7(`Estimate.payment_status`·`payment_required` 삭제)을 계획서가 "확실"로 분류했는데 담당 기수가 거부했습니다.**

읽는 곳이 0인 것은 맞았으나, **타입 계약이 자기 소유 밖 3파일에 걸려 있었습니다** — `lib/payments/status.ts:10`(§14-2 보호 모듈의 타입 파생) · `RequestWizard.tsx:585`(살아 있는 쓰기) · `test/fixtures.ts:34-35`. 지웠으면 세 파일에서 tsc가 깨졌습니다.

**A10도 절반만 했습니다.** 저장 카운터의 *가산 경로*는 지웠지만 `types/estimate.ts`의 필드 3종은 남겼습니다. `CustomerList.tsx`가 8지점에서 직접 읽는데, 그 값이 저장값이 아니라 **읽기 시점에 파생값이 실려 오는 전달 통로**였기 때문입니다.

계획서를 그대로 실행했으면 둘 다 사고였습니다. "계획서를 믿지 말고 네가 직접 grep 하라"는 지시가 값을 했습니다.

## 3. 적대 검증이 잡은 것 — FAIL 1 + 잔재 2

### FAIL — `tsconfig.json` `resolveJsonModule` 삭제가 성립하지 않는다

담당 기수는 `npx tsc --noEmit` 0에러로 안전을 주장했으나, **강제 주체가 tsc가 아니라 `next build`** 였습니다. Next.js 16이 이 옵션을 mandatory로 취급해 빌드마다 되돌려 넣으면서 `tsconfig.json` 전체를 재직렬화합니다.

→ 순효과: **축소 0 + 빌드마다 추적 파일이 더러워지는 새 부작용.** 복구했고, 사유를 주석으로 박았습니다. 빌드 후 `git diff`로 재작성 흔적 0을 확인했습니다.

### 잔재 2건 (봉합 완료)

- `test/api/dataRoute.test.ts:72` — 삭제된 `hasServiceKey` export를 `vi.mock`이 계속 반환
- `EstimateDetailModal.tsx:1260` — 화면에 **"로컬 Mock DB에 반영됩니다"** 문구가 남아 있었다. Mock을 지운 지금은 명백한 거짓이고, 사실 삭제 이전에도 이미 거짓이었다(§9)

## 4. 검증 (전부 실측)

| 게이트 | 결과 |
|---|---|
| `npx tsc --noEmit` | **0에러** |
| `npm test` | **471/471** (삭제된 테스트 2개는 죽은 코드를 채점하던 것) |
| `npm run build` | **성공** · 빌드 후 `tsconfig.json` 재작성 **0** |
| `npm run lint` | 에러 **0** |
| `ui-quality-gate --check` | R1 **0** · 베이스라인 대비 **회귀 없음** |
| O-37 홈 1366×768 | 스크롤 **0**(704=704) · 클리핑 **0** |
| O-38 사업소개 412×870 | 스크롤 **0**(736=736) · 카드 차 **0.00px** |
| 콘솔 에러 | **0건** |

## 5. 남은 것

**계획서 ①단계 중 이월 3건** — A7(`payment_status`, 3파일 동시 처리 필요) · `SendResult.sent` · `FileMeta.file_type`. 전부 **여러 파일의 타입 계약이 얽혀 한 기수로 완결 불가**한 것들이라, 다음에 하려면 소유 기수를 묶어야 합니다.

**②단계(중복 제거, 약 700줄)와 ③단계(분해)는 그대로 대기** — 계획서 §4·§5.

---

## 6. 오류 원장 (헌법 제8조)

| # | 무엇이 잘못됐나 | 원인 | 처방 |
|---|---|---|---|
| ① | `resolveJsonModule` 삭제가 빌드마다 파일을 더럽히는 부작용을 냈다 | **검증 도구를 잘못 골랐다** — tsc로만 확인하고 `next build`로 확인하지 않았다. 강제 주체가 다르다 | 설정 파일 삭제는 **그 설정을 강제하는 주체**로 검증한다. `tsconfig`는 `next build` 후 `git status` 청결까지 봐야 한다 |
| ② | 계획서가 A7을 "확실"로 분류했는데 실제로는 3파일 계약이 얽혀 있었다 | 축소 분석이 **읽기 참조만** 세고 **타입 파생 의존**을 세지 않았다 | 필드 삭제 판정에 `type X = Y['field']` 형태의 타입 파생을 반드시 포함한다 |
| ③ | 화면에 "로컬 Mock DB" 거짓 문구가 남아 있었다 | 코드를 지우면서 **그 코드를 설명하는 화면 문구**를 함께 찾지 않았다 | 기능 삭제 시 해당 기능을 언급하는 **사용자 노출 문구**를 함께 grep 한다 |

---

# ② 최적화 2단계 — 중복 제거 (2026-08-08)

> 지시: "②단계 진행" · 근거 `docs/00_orchestration/최적화-계획서.md` §4
> 브랜치 `refactor/stage2-dedupe` · Agent 11개 · Batch 3개

## 1. 결과

**소스 14,657 → 14,345줄 (−312줄).** 파일은 66 → 76개로 늘었다 — 중복을 공용 모듈로 뽑았기 때문이다.
**테스트 471 → 564개 (+93).** ①②단계 합산 **16,635 → 14,345줄 (−2,290, −13.8%)**.

| Batch | Agent | 절감 |
|---|---|---|
| 1 (확실 5건) | 4 | −210줄 |
| 2 (확인필요 7건) | 5 | −131줄 |
| 3 (적대 검증) | 2 | — |

계획서 목표 770줄의 **44%**. 못 채운 이유는 §3 참조 — **채우지 않은 것이 정답이었다.**

## 2. 통합한 것

| ID | 무엇 | 어디로 |
|---|---|---|
| B2 | `BaseZerosService` 상속 계층 제거 | `SupabaseZerosService` 단일 클래스 |
| B4 | 마이페이지 두 파일의 바이트 동일 상수·유틸 | `lib/requests/timeline.ts` |
| B7 | 관리자 오류 배너 7중 복붙 | `components/admin/AlertBanner.tsx` |
| B9 | 모바일 하단 탭 5개 반복 | `lib/ui/mobileTabs.ts` |
| B10 | `scrollMainPanelToTop` 3중 복제 | `lib/ui/scrollMainPanel.ts` |
| B1 | **OTP 인증 3중 복붙** | `lib/otp/` (verifyClient · useOtpVerify · CustomerOtpLoginForm) |
| B5 | a11y 스캔 규칙 이중 구현 | `test/support/sourceScan.ts` + 테이블 주도 |
| B6 | 데이터 적재 74줄 동일 | `lib/requests/useMyRequests.ts` |
| B8 | 의뢰폼 필드 블록 8개 | 공용 `FormField` |
| B11 | 홈 공종 병렬 Record 5종 | `lib/constants/landingTrades.ts` |

## 3. Agent 가 스스로 멈춘 것 — B3

계획서는 마이페이지 두 화면의 JSX 를 합쳐 **100줄**을 줄인다고 적었다.
Agent 가 전수 대조하니 **렌더에 영향 주는 차이가 29개**였다 — 폰트(`12.5px`/`13px`), 여백(`py-10`/`py-16`),
문구("진행중인 건이"/"접수된 건이"), 한쪽에만 있는 견적서 다운로드 버튼.

→ **바이트 동일한 조회실패 배너만** 뽑고 나머지 5블록은 두었다. 합쳤으면 한쪽 화면이 바뀐다.
**계획서 100줄 미회수가 정답이다.**

B2 도 계획서 설명이 틀렸다 — "중복 구현"이 아니라 **도달 불가한 대체 구현**이었다(성격이 아예 다름).
삭제 판정은 통과해 진행했고, 되살아나면 깨지는 회귀 테스트를 신설했다.

## 4. 적대 검증이 잡은 것 — R5 8→7 은 해소가 아니라 은폐였다

**동작 렌즈 6건 전부 PASS.** P2-2 재발(인증 토큰 전달 경로 단절)·`useMyRequests` enabled·§13 불변식·
`clearDataCache` 양쪽 배선·모바일 탭 5개 순서/문구/아이콘 모두 원본 유지 확인.

**렌더 렌즈에서 FAIL 2건.** 오류 UI 2개(`CustomerOtpLoginForm`·`RequestsLoadBanner`)가
`components/` 에서 `lib/` 로 옮겨가며 게이트 `SCAN_DIRS(['app','components'])` 밖으로 나갔다.
그 결과 `MyRequestsView:314` 에서 R5 로 잡히던 `animate-in` 가드 누락이 **검출되지 않게 됐고**,
전수 스윕 R5 가 8→7 로 "줄어든 것처럼" 보였다. **부채가 계기판에서 사라진 것이지 해소된 게 아니다.**

→ 봉합 3건:
1. `CustomerOtpLoginForm.tsx` 에 `motion-reduce:animate-none` 실제 추가 — R5 진짜 해소
2. `ui-quality-gate.mjs` `SCAN_DIRS` 에 **`lib` 추가** — 같은 은폐가 다시 못 생기게. 베이스라인 재스냅샷(12 → **32파일**)
3. 통합 무변경을 채점하던 테스트가 이 의도적 변경으로 깨져 기대값 갱신 + 사유 주석

**은폐가 아님을 기계로 증명했다** — 가드를 일부러 빼면 R5 7→8, 되돌리면 7. `lib` 가 실제로 스캔된다.

## 5. 검증

| 게이트 | 결과 |
|---|---|
| `npx tsc --noEmit` | **0에러** |
| `npm test` | **564/564** |
| `npm run build` | 성공 |
| `npm run lint` | 에러 **0** |
| `ui-quality-gate --check` | R1 **0** · 베이스라인(32파일) 대비 회귀 없음 |
| O-37 홈 1366×768 | 스크롤 **0**(704=704) · 클리핑 **0** |
| O-38 사업소개 412×870 | 스크롤 **0**(736=736) · 카드 차 **0.00px** |

## 6. 오류 원장 (헌법 제8조)

| # | 무엇이 잘못됐나 | 원인 | 처방 |
|---|---|---|---|
| ① | **완료시각을 세 번 밀었다**(09:10 → 09:20 → 09:40 → 09:50) | **동시 실행 상한이 2인 것을 오늘 아침에야 측정.** 그전 계획은 4~5 병렬을 가정한 낙관치였다. 실측 회전당 24분 | AGENTS **§16** 신설 — `회전 수 = ceil(Agent수/2)` 로 소요를 잡는다. "N개 병렬"이라 쓰지 않는다 |
| ② | 파일을 `lib/` 로 옮겨 게이트 사각지대를 만들었다 | 스캔 대상을 **파일 위치**로 정하고 있었다. 리팩터링이 위치를 바꾸면 계기판이 조용히 눈을 감는다 | `SCAN_DIRS` 에 `lib` 추가. **스캔 대상은 위치가 아니라 코드 성격으로** 정한다 |
| ③ | 스캔 검증을 `Select-Object -First 3` 으로 잘라 "안 된다"고 성급히 단정 | 필터가 결과를 잘랐는데 그걸 부재로 읽었다 | 존재 여부 판정은 **총계 변화**로 한다(가드 제거 전후 R5 7↔8) |


---

# ③ 최적화 3단계 — 정찰 후 중단 (2026-08-08)

> 지시: "③단계 진행" → "리스크가 있다니 세밀하게 검토해줘" → **"정찰 진행"** → **"③단계 중단"**
> 읽기 전용 Agent 3개 · 코드 변경 **0건**

## 1. 결론

**화면 분해는 하지 않는다.** 판정·근거 전문은 `docs/00_orchestration/최적화-계획서.md` §5-A.

셋 다 `분해 권장`이 아니라 **`부분 분해`** 또는 **`화면 분해 금지`** 로 나왔다.

## 2. 계획서 숫자가 다섯 번째 틀렸다

| 대상 | 계획서 | 내가 재보고한 값 | **실측** |
|---|---|---|---|
| `app/page.tsx` | 2,298 | 1,727 | **1,852** |
| `EstimateDetailModal` | 1,326 | 1,228 | **1,309** |
| `AppShell` | 862 | 759 | **817** |

내가 `Get-Content | Measure-Object -Line` 으로 잰 값도 `wc -l` 과 달랐다. **줄 수는 도구를 명시해서 재야 한다.**

## 3. 내 위험 분석 2건이 틀렸다

- ❌ **"여백 한 줄만 어긋나도 O-37 이 깨진다"** → React 컴포넌트 추출은 **DOM 노드를 추가하지 않는다.** JSX 를 바이트 동일하게 옮기는 한 구조적으로 발생하지 않는다.
- ❌ **"prettier·eslint --fix 가 className 을 재포맷한다"** → 이 저장소에 **prettier 가 없고** `lint` 는 `--fix` 없는 `eslint` 이며 포맷 룰이 0개다.
- ✅ 실제 위험은 **테스트 공허화**(초록불인데 아무것도 안 지킴)와 **숨은 교차 소비**였다.

**위험을 "그럴듯한 것"으로 짚었고, 정찰이 실측으로 뒤집었다.**

## 4. 정찰이 아니었으면 못 봤을 것

`HOME_STATS`(`app/page.tsx:1496-1501`)가 **"홈(데스크톱)" 주석 아래 정의됐는데 `renderReviewDesktop:1467·1479` 가 소비한다.** 계획서대로 홈을 떼면서 데려갔으면 견적검토 화면이 즉사했다.

같은 유형이 3건이었다(`activeTradeIdx`·`HOME_STATS`·`pendingRequestChannel`). **45분 정찰에서 하나가 더 나왔다는 것은, 남은 게 없다는 보장이 없다는 뜻이다.**

## 5. 다음 할 일은 코드가 아니다

**Vercel 키 등록과 SQL 실행 5분. 지금 고객이 마이페이지에 못 들어가고, 관리자 로그인이 막혀 있다. 이건 코드 문제가 아니라 키가 없어서다.**

절차 = `docs/03_qa_deploy/prod-env-setup.md` · 재개 지점 = `state.md` 0-⭐.

## 6. 오류 원장 (헌법 제8조)

| # | 무엇이 잘못됐나 | 원인 | 처방 |
|---|---|---|---|
| ① | 파일 줄 수를 다섯 번 틀렸다 | 계획서 값을 그대로 옮겨 적거나 `Measure-Object -Line` 으로 쟀는데 `wc -l` 과 다르다 | 줄 수는 **`wc -l` 로 재고 도구를 명시**한다. 계획서 값은 작성 시점 스냅샷이므로 착수 전 항상 재측정 |
| ② | 위험을 실측 없이 "그럴듯한 것"으로 짚었다(여백·prettier) | 도구 설치 여부·React 렌더 원리를 확인하지 않고 일반론으로 말했다 | 위험 주장에도 **근거를 붙인다** — "그 도구가 이 저장소에 있는가"부터 확인 |
| ③ | ③단계를 계획서에 넣을 때 "책임 개수"만 세고 **테스트·교차소비를 안 봤다** | 분해 계획의 판단축이 "파일이 크다" 하나였다 | 분해 판정축 = ①테스트가 그 파일 경로를 붙들고 있는가 ②상태 교차 소비 ③props 배선 수 ④번들·게이트 이득. **크기는 마지막 축이다** |

---

## SQL 실행 검증 (11:00~11:20) — 1차 실행분 롤백 확인 + 자기검증 절 추가

주인님 "SQL 실행했다, 검증해줘" → 라이브 실측 결과 **반영 0건**.

### 실측 (service_role / anon 직접 호출)

| 항목 | 기대 | 실측 | 판정 |
|---|---|---|---|
| 버킷 `file_size_limit` | 52428800 | **null** | ❌ §7 미적용 |
| 버킷 `updated_at` | 실행 시각 | 2026-06-05(생성 시각) | ❌ 갱신 흔적 없음 |
| 접수번호 중복 | 0건 | **0건** (101행 전부 고유) | ✅ §6 실패 원인 아님 |
| 버킷 `public` | false | false | ✅ |
| 익명 업로드 | 허용 | 200 (프로브 후 삭제) | ✅ |
| 익명 파일 읽기 | 차단 | 400 | ✅ |
| 익명 테이블 5종 | 0행 | 전부 0행 (service_role 대조군은 행 있음) | ✅ |

→ 보안 적용분은 전부 살아 있고, **이번 실행분(§6·§7)만 반영되지 않았다.**

### 근본 원인

SQL Editor 는 전체를 **한 트랜잭션**으로 실행 → 중간 한 구문이 실패하면 앞뒤가 통째로 롤백된다.
그런데 파일 마지막이 `update` 라 화면에는 **"Success. No rows returned"** 만 떠서
**전 구문 성공과 통째 롤백이 구분되지 않았다.** 판정을 사람 눈에 맡긴 절차 설계가 원인.

### 처방 (게이트화)

1. `supabase/supabase-setup.sql` **§8 자기검증 절** 추가 — 마지막을 `select` 로 두어 **결과 그리드 5줄이 곧 판정**. 전부 `OK` 여야 완료.
2. `test/supabase/setupSql.test.ts` +3건 — 마지막이 select 인가 · 바꾸는 것 전부를 채점하는가 · 판정 문구가 OK/실패뿐인가. 나중에 지우면 테스트가 막는다.
3. `docs/03_qa_deploy/prod-env-setup.md` §5 3단계를 결과 표 기준으로 교체 + 왜 바꿨는지 명시.
4. `AGENTS.md` §15-9(실행했다는 보고를 상태로 검증) · §15-10(PostgREST RLS = 200+빈배열) 신설.

### 오류 원장

| # | 오류 | 원인 | 처방 |
|---|---|---|---|
| 1 | 익명 테이블이 "열려 있다"고 오판 | PostgREST 는 RLS 차단을 **200 + 빈 배열**로 답하는데 상태코드로 판정 | 행 수 + service_role 대조군으로 판정 (AGENTS §15-10) |
| 2 | 사람 실행 절차에 판정 신호가 없었음 | 파일 마지막이 `update` | §8 자기검증 절 + 테스트 고정 |
| 3 | `vitest --reporter=basic` 실패 | v4 에서 `basic` 리포터 제거됨 | 기본 리포터 사용 |

검증: `tsc` 0 · `npm test` **567/567**(564 → +3).

### 재실행 결과 (11:35) — ✅ 전량 적용 확인

주인님이 §8 자기검증 절이 포함된 파일로 재실행 → 결과 표 **5줄 전부 OK**.
AI 독립 실측(service_role·anon 직접 호출) **10 PASS / 1 FAIL**:

| 항목 | 실측 |
|---|---|
| 업로드 용량 | `file_size_limit = 52428800` ✅ |
| 접수번호 유니크 인덱스 | 존재 ✅ (DB 자체 `pg_indexes` 조회) |
| 버킷 비공개 / MIME 미설정 | ✅ / ✅ |
| 익명 업로드 200 · 익명 읽기 400 | ✅ |
| 익명 테이블 5종 | 전부 0행 (service_role 대조군은 행 있음) ✅ |

**1건 FAIL = 점검 항목 자체의 결함.** `storage.buckets.updated_at` 은 Storage API 경유 변경에서만
갱신되고 SQL `update` 로는 생성 시각(2026-06-05)에 머문다 → 적용 여부의 근거로 쓸 수 없다.
11:10 판정에서 이 필드를 보조 근거로 인용했는데 **유효한 신호가 아니었다**(결정적 근거였던
`file_size_limit = null` 은 옳았고 결론도 옳았다). → AGENTS **§15-11** 신설.

접속 계정 확정: **Supabase = GitHub `ihaelyong988-lab`**(비밀번호 로그인 불가 계정, 소셜 버튼 전용).
안내 문서에 길안내 대신 **클릭 링크**를 넣도록 교정(주인님 지시 "링크를 줘야지").

`prod-probe`: 사이트 200 PASS · SOLAPI WARN · ZEROS_ADMIN_PASSWORD WARN → **남은 것은 Vercel 키 등록뿐.**

---

## 네이버·구글 등록 사전 점검 + 테스트행 정리 (12:20~12:45)

주인님 지시: "네이버,구글 등록 전에 셋업해 두어야 할 일이 있나 확인… 헛다리 작업 계획 세우지 마세요."

### SEO 셋업 — 손댈 것 없음 (라이브 실측)

`robots.txt` 200(`/admin`·`/api/` 차단) · `sitemap.xml` 200 · title/description · og 태그(이미지 포함) ·
favicon 200 · canonical · `/privacy` 200. **익명에게 PII 노출 0** · **테스트 인증번호 노출 없음**
(`/api/otp/send` 응답에 code 미포함, 임의 코드 verify 거부 — 잔여목록 0-★의 P0 "테스트 인증번호
화면노출"은 **이미 해소됨**). 등록 시점에 필요한 것은 **소유확인 메타태그 2개**(구글·네이버)뿐이다.

### 실제로 걸린 것 1건 — 공개 실적이 사실과 달랐다

익명 `/api/data` `op:list` 가 **101건**을 반환했고 그중 **99건이 `est-test-*`**. 공개 실적 화면의
'온라인 접수'가 실제 2건인데 101건으로 표시되고 있었다. (`246건`은 문제 없음 — 라벨이
"누적 검증(30년 실적)"으로 모집단을 갈라 둠, `lib/constants/trust.ts`.)

**주인님 결정: 삭제하고 실적 화면은 "준비 중"으로.**

### 조치

1. **전체 백업** → `.backups/full-backup-2026-08-08T1229.json`(108KB, gitignore 등록). PII 포함이라 저장소에 커밋하지 않는다.
2. **삭제 필터 사전 검증** — `id=like.est-test-*` 가 정확히 99건만 잡고 실접수 0건 매칭 확인 후 실행.
3. **삭제 99건** → 남은 2건(`ZR-20260615-001`·`ZR-20260607-001`). 라이브 익명 API 수신도 2건으로 일치.
4. **표본 부족 시 지표 비공개** — `PERFORMANCE_MIN_SAMPLE = 10` + `isPublishable` 를
   `lib/performance/insights.ts`(집계 단일 소스)에 두고, 화면은 판정만 따른다.
   테스트 5건 신설(모집단 밖 100건이 있어도 공개 안 됨 포함).
5. **'실적 공개 준비 중' 화면** — 좌측 단일 축·본문 16px·`text-gray`·이모지 없음·설명 1문장.

**왜 임계값이 필요한가**: 테스트행을 지우면 표본이 2건이 된다. 1건이 50%가 되는 분포·평균은
실적이 아니라 오해다. "몇 건부터 지표로 말할 수 있는가"를 화면이 아니라 집계에 두고 테스트로 고정했다.

### 검증

Playwright DOM 실측(1366×768, `?tab=performance`): 준비중 제목·본문 **있음** / 실적 분포·히트맵·
공종별 세부 실적 **전부 없음** / 건수 표시 **없음**. tsc 0 · `npm test` **572/572**(567 → +5) ·
게이트 차단룰 0 · 베이스라인 회귀 없음.

### 남긴 것 (삭제하지 않음 — 범위 밖, 주인님 판단 필요)

`zeros_customers` 의 `test161@example.com`(010-1161-1127, 2026-07-02 생성)은 실접수 2건 어디에도
대응하지 않는 **고아 행**으로 보인다. 같은 번호를 참조하는 `zeros_notification_logs` 1행도 동일.
**공개 화면에 뜨지 않으므로**(익명 테이블 차단) 급하지 않다. 임의 삭제하지 않았다.

### 오류 원장

| # | 오류 | 원인 | 처방 |
|---|---|---|---|
| 1 | Glob `{app/robots*,app/sitemap*,…}` 가 "없음" 반환 | 중괄호 다중 패턴이 이 도구에서 매칭 실패 — 실제로는 둘 다 존재 | 존재 여부는 `ls`/디렉터리 나열로 교차 확인 |
| 2 | `/api/data` 첫 호출 400 | `op:'loadTable'` 로 추측 — 실제 계약은 `op:'list'` | 라우트에서 op 이름을 먼저 확인 |
| 3 | Playwright MCP 스크린샷 파일 회수 불가 | 출력 경로가 프로젝트·TEMP 어디에도 없음 | 시각 증거 대신 **DOM 실측**으로 판정(더 강한 증거) |

---

## 잔여작업 계획 전량 재검증 + 단일화 (12:55~13:35 · PLAN B 승인)

주인님 지시: "잔여 작업 계획를 철저히 검토해서 기록해 놓으세요."
헌법 제11조대로 읽기 전용 정찰 → PLAN A/B 보고 → **PLAN B 승인** → 착수. Agent 0기(주인님 지침).

### 결과 — 26건 중 22건이 이미 해소돼 있었다

| | 2026-08-07 문서 | **2026-08-08 실측** |
|---|---|---|
| 코드 잔여 | 26건 | **6건** |
| 주인님 결정 대기 | 3건 | **0건** |
| 소요 | 2시간 50분 · 15기 · 4파도 | **2시간 10분 · 2기 · 1회전** |

**잔여 6건**: C6(본문 대비 104곳/15파일, M) · C5(motion-reduce, 게이트 R5 7건) ·
C4(focus-visible 10곳/4파일, 전부 관리자 화면) · N2(VisitList alert 1곳) ·
D3(단건 쓰기, L, 별도 차수) · SMS 발송 경로(M, 별도 차수).

**해소 22건 근거(발췌)**: D1 게이트 123→**383줄·룰 7종·4모드·베이스라인 동작** · D2 `mock-data.ts`
**파일 자체 없음** · C2/결정② `041B33` **grep 0곳** · B5 CTA `무료 견적 검토 신청` **1종 ×3** ·
D6 `setInterval` **0곳** · N6 `deletePayment` op 존재 · N4 `lib/a11y/rovingTabindex.ts` 존재 ·
N1 `saveError`+`AlertBanner` · N3 `deriveCustomerType(industry)` · C8 `buildShellUrl(selectedMenu…)`+pushState ·
B9 4건 전부(`isValidEmail`·`validateVisitDate`·`clearDraft`·`serializeRequestDraft`, 보호 테스트 46건).

### 왜 하루 만에 낡았나 (근본 원인)

08-07 이후 **커밋 23개**가 잔여를 처리했는데 ①같은 목록이 **문서 3곳에 복제**돼 있었고
②처리 기록이 worklog에만 남고 **목록에 역반영되지 않았다.** 낡은 목록으로 착수했다면 이미 끝난
일을 다시 하고(D1·B4), 없는 줄 수(`app/page.tsx` 2,166 → 실제 1,852)로 소요를 잘못 잡았을 것이다.

### 산출물

- **`docs/00_orchestration/잔여작업-마스터.md` 신설** — 잔여의 단일 기준. 잔여 6건 + 해소 22건(근거 포함)
  + 주인님 액션 + 착수 안 하기로 한 것 + **§5 낡음 재발 방지 규칙** + PRE-FLIGHT 템플릿.
- 구 문서 3곳에 **⛔ 이관됨 헤더**(지우지 않고 배경 기록으로 보존) — `잔여작업-실행준비.md` ·
  `개선-작업계획서.md §8` · 결함대장 부록 B.
- `state.md` **0-📋** 신설(0-◎·0-☆는 ⛔ 낡음 표시, 0-★★ 완료 처리).
- **AGENTS §15-12** 신설 — 잔여 목록 단일화 + 처리 즉시 역반영 + 착수 전 실측 재확인.
- memory `backlog-goes-stale-without-writeback`.

### 오류 원장

| # | 오류 | 원인 | 처방 |
|---|---|---|---|
| 1 | bash `chk` 헬퍼에서 `alert(` 등 괄호·따옴표가 섞인 인자가 `syntax error` | `$( )` 안의 중첩 따옴표를 bash가 재해석 | 다건 정규식 집계는 **node 스크립트 한 번**으로. bash 인라인 grep 조합 금지 |
| 2 | C5 미가드를 137건으로 과다 계상 | `transition-`까지 셌다 — 짧은 전환은 `prefers-reduced-motion` 대상이 아니다 | 기준은 **게이트 R5(7건)**, 광의 스캔은 참고치로만 |

### 재재검토 (13:10~13:25) — 주인님 지적으로 판정 기준 자체를 고침

> "다시한번 재검토해, 할때마다 끊임없이 잔여 작업이 있다고 그러면 뭐가 잘못된것 아닌가?"

**지적이 옳았다.** 숫자는 수렴 중이었으나(39 → 26 → 6) 남긴 6건을 **영향**으로 다시 재니:

| 항목 | 위치 실측 | 고객 영향 |
|---|---|---|
| C4 포커스 10곳 | **10/10 전부 `components/admin/`** | 0 |
| N2 alert 1곳 | 관리자 `VisitList` | 0 |
| C6 옅은 글씨 104곳 | **관리자 92 / 고객 12**(고객분 5곳은 실적=준비 중이라 렌더 안 됨) | 실질 ~7곳 |
| C5 애니메이션 7건 | 관리자 4 / 고객 3 | 3곳 |
| D3 동시편집 소실 | **관리자 2명 동시 편집 시 발동 — 실제 관리자 1명** | **0 · 발생 불가** |
| SMS | 결함 아님, 기능 | — |

**고객이 실제로 겪는 것 = 옅은 글씨 ~10곳, 30분.** 코드 작업은 사실상 끝났다.

**근본 원인**: 게이트·린트 경고를 그대로 잔여작업으로 올렸다 — 기계가 트집 잡을 수 있는 것과
사람이 손해 보는 것을 구분하지 않았다. **2026-08-06에 주인님이 같은 지적(MECE 관점)을 했는데
근본을 안 고쳐 재발했다(헌법 제5조 위반).** 규칙만 쓰고 관문을 안 만들면 반복된다.

**처방(관문화)**: 잔여작업-마스터 **§5-A** 신설 — 올리기 전 `누가 · 언제 · 무엇을 잃는가` 한 줄
필수, 못 적으면 목록에 못 올림. 등급 A(고객이 지금)/B(주인님만)/**C(발생 불가 → 목록에서 내림)**.
`AGENTS §15-13` · memory `backlog-needs-who-suffers-gate`.

**오류 원장**: 영향 분류 스크립트가 Windows 경로 구분자(`\`)를 못 잡아 "관리자 0곳"으로 오판 →
경로 판별은 `path.sep` 양쪽(`[\/]`)을 모두 넣는다.

---

## 고객 화면 옅은 글씨 10곳 수정 (15:08~15:30)

주인님 지시: "고객 옅은 글씨 10곳만 고쳐."

`text-gray-light`(#9AA3AF, 흰 배경 **2.6:1**) → `text-gray`(#5B6573, **5.82:1**). §10 본문 4.5:1 충족.

| 파일 | 곳 | 무엇 |
|---|---|---|
| `components/PerformanceInsights.tsx` | 5 | 적재 중 문구 · KPI 단위 · '건' · '평균 검토' · '대표 현장' |
| `components/forms/RequestWizard.tsx` | 3 | 미도달 단계 라벨 · '접수 번호' 라벨 · 업종 미선택 안내 |
| `components/forms/PhoneVerifyGate.tsx` | 2 | '번호 다시 입력하기' 버튼 · 개인정보 안내 문구 |
| | **10** | |

### 손대지 않은 2곳 (판정 근거)

- `app/page.tsx:309` — 구분 기호 `|`, `aria-hidden="true"`. **글자가 아니라 장식**이다.
- `components/layout/RightSidebar.tsx:283` — 패널 접기 **아이콘 버튼**. `rightSidebarSource.test.ts` 가
  이미 "비본문(장식) — 그대로 둔다"로 **개수를 고정**해 두었다. 주인님 지시도 "옅은 **글씨**"였다.
  판정을 뒤집으려면 그 테스트를 먼저 고쳐야 한다.

### 검증

`test/ui/customerFacingContrast.test.ts` 신설(§14-1 실패 테스트 선행 — 4건 실패 확인 후 수정).
tsc **0** · `npm test` **576/576** · 게이트 R2 **105 → 95**(정확히 10건 감소) · 베이스라인 회귀 없음.
**Playwright 라이브 실측**: 고객 4개 탭(request·performance·business·account) 전부
`#9AA3AF` 렌더 **0곳** · `#5B6573` 렌더 46곳.

### 오류 원장

| # | 오류 | 원인 | 처방 |
|---|---|---|---|
| 1 | `TS1005` — RequestWizard 915 | JSX **속성 목록 안**에 `{/* */}` 주석을 넣었다(문법 불가) | 주석은 여는 태그 **위**로 |
| 2 | RightSidebar 일괄 치환이 기존 테스트를 깼다 | 그 파일에 "장식이라 그대로 둔다"는 **선행 판정 테스트**가 있는 걸 확인 안 하고 sed 로 전면 치환 | 착수 전 **대상 파일의 기존 테스트를 먼저 읽는다** |
| 3 | 대비 측정이 두 번 틀림(3.55 → 3.3) | ①정규식으로 `oklab()` 파싱 ②처방이라며 쓴 `canvas.fillStyle` 도 `oklab()` 미지원 → **검정으로 조용히 폴백** | **배경 합성을 하지 말고 글자색 자체로 판정**(색 문자열 비교는 파싱이 없다). AGENTS §15-3 보강 |

### 애니메이션 3곳 (16:08~16:30) — 가드는 달았고, 실측으로 "안 돌던 것"이 드러났다

주인님 지시: "애니메이션 3곳도 고쳐."

`MyRequestsView:45·75`(fade-in) · `AppShell:780`(slide-in-from-right) 에 `motion-reduce:animate-none` 추가.
게이트 R5 **7 → 4**(남은 4건은 관리자 화면 = 등급 B). `a11ySourceRules.test.ts` PINNED 에 두 파일 추가해 0건 고정.
tsc 0 · `npm test` **578/578**.

#### ⚠ 실측으로 드러난 것 — 이 3곳은 애초에 애니메이션이 없었다

두 갈래 독립 증거:
1. `package.json` 에 **`tailwindcss-animate` 없음** · `tailwind.config` 없음(v4 CSS-first).
   `animate-in`·`fade-in`·`slide-in-*` 은 **코어 클래스가 아니다.**
2. Playwright `emulateMedia({reducedMotion:'no-preference'})` 상태에서도 해당 요소
   `getComputedStyle().animationName === 'none'` · 스타일시트 `animate-in` 규칙 **0건** · `@keyframes enter` **0건**.

→ **고객 피해는 0이었다.** 내가 이 항목을 등급 A(고객이 지금 겪음)로 올린 것이 틀렸다.

**가드는 그대로 둔다** — 플러그인을 나중에 넣으면 그 순간 진짜 애니메이션이 되고, 그때 가드가 없으면
조용히 조문 위반이 된다. 지우는 쪽보다 막아 두는 쪽이 싸다.

#### 규칙 보강 — §5-A 관문의 두 번째 조건

**"소스에 문자열이 있다 ≠ 화면에서 동작한다."** 등급 A로 올리기 전에 **라이브에서 그 현상이 실제로
재현되는지** 확인한다. 게이트는 소스 문자열만 보므로 이 판별을 대신하지 못한다.

#### 오류 원장

| # | 오류 | 원인 | 처방 |
|---|---|---|---|
| 1 | C5를 등급 A로 잘못 분류 | 게이트가 잡았다는 이유로 "고객이 겪는다"고 단정 — 동작 여부 미확인 | §5-A 관문에 **"실제로 렌더되는가"** 조건 추가 |
| 2 | 첫 대비 실측이 무의미(`CSS: ` 빈 값) | 라이브 HTML 에서 CSS 링크 grep 실패인데 결과 0건을 그대로 읽음 | **대상이 0개면 판정이 아니라 측정 실패다** — 분모를 먼저 확인(§15-3) |
| 3 | 첫 애니메이션 측정이 양쪽 다 `none` | 1200ms 대기 후 읽어 선택자·타이밍이 뒤섞임 | 선택자로 요소를 먼저 찍고 클래스·계산값을 함께 출력 |

### B등급 재판정 (16:20~16:30) — 관문을 B에도 적용하니 2건이 또 틀렸다

주인님 "B는 어떻게?" → 등급 A에서 배운 관문(**실제로 겪는가 · 실제로 렌더되는가**)을 B에도 적용했다.

| 항목 | 문서 기재 | **재판정** |
|---|---|---|
| N2 방문완료 실패 무음 | 잔존 (alert 1곳) | **✅ 이미 해소** — `VisitList.tsx:91` catch 는 `setActionError` 배너. 남은 `alert(`(83행)은 **성공 알림**이다. 문자열만 세고 결함을 안 봤다 |
| C5 관리자 4건 | 잔존 | **1건만 진짜** — `app/admin/page.tsx:18` `animate-pulse`(코어). 나머지 3건(`CustomerList:365`·`TossPaymentModal:175·214`)은 `animate-in` = **죽은 클래스** |
| C4 포커스 10곳 | 잔존 | 실재 — 관리자 화면 키보드 사용 시 |
| C6 관리자 92곳 | 잔존 | 실재 — 관리자 화면 가독성 |

**관리자 화면은 지금 쓸 수 없다** — `/admin` 200 이지만 로그인 API **503**(`ZEROS_ADMIN_PASSWORD` 미등록).
즉 B 전체가 **주인님이 아직 쓰지 못하는 화면**의 결함이다.

**권고**: 키 등록 → 실사용 → **불편한 것만** 고친다. 지금 미리 고치면 오늘 세 번 반복한 실수
("아무도 안 겪는 것을 고치기")를 또 하는 것이다.

**오류 원장**: 결함 개수를 셀 때 **문자열(`alert(`)이 아니라 그 문자열이 하는 일**을 본다.
성공 경로의 alert 을 실패 무음으로 계산했다 — §15-3(측정 도구 의심)의 변형이다.

---

## 라이브 실사용 전수 검증 (16:30~17:00) — 목록이 아니라 "작동하는가"

주인님: "2일동안 계속 고친다고 했는데… 다시 검토해, 철저히 확인해."
**게이트 경고만 세고 고객 여정을 끝까지 돌려본 적이 없었다.** 라이브에서 실제로 돌렸다.

### 화면 7개 전수 (라이브)

홈·사업소개·견적검토·실적·예상견적 의뢰·AI NATIVE·마이페이지 **전부 정상 렌더**.
**콘솔 에러 0 · 실패 요청(4xx/5xx) 0 · 가로 스크롤 0 · 스플래시 정지 0.**
(실적은 글자수 78 = 의도된 '준비 중' 상태)

### 고객 여정 끝까지 — 실제 접수 성공

채널 선택 → STEP1(지역·공사 종류 10종·현장 유형 7종) → STEP2(예산·성함·연락처·이메일)
→ STEP3(참조 사항·개인정보 동의) → **제출 → `ZR-20260808-001` 발급 · `/api/data` 200 · DB 반영 확인.**
검증 후 접수·고객·알림 3건 정리 완료(남은 접수 2건 = 실접수 그대로).

### ⚠ 이 과정에서 발견한 실제 결함 — 완료 화면 조사 오류

```
접수하신 자료{channel === 'visit' ? '·예약' : ''}은 이력관리에 저장되었습니다.
```
조사 `은` 이 조건문 **밖**에 있어 받침 유무가 채널마다 달라진다.
- 예약방문: "자료·예약**은**" → 우연히 맞음
- **간편 견적: "자료은"** → **틀림. 그리고 이쪽이 더 흔한 경로다.**

**어떤 게이트도 이걸 못 잡는다.** lint·tsc·대비·접근성 룰은 문법을 보지 않고,
소스 스캔도 `자료{...}은` 처럼 보간이 끼면 앞 글자를 못 읽는다. **라이브에서 끝까지 돌려야만 보인다.**

처방: 조사를 분기 안으로(`'·예약은'` / `'는'`) + `test/forms/koreanParticles.test.ts` 신설
— ①이 줄을 문자열째 고정 ②고객 화면 리터럴 문장을 **받침 규칙으로 기계 채점**
(`X은` 에서 X 받침 없으면 실패. 반대 방향은 동사 어미 "있는·먹는"과 구분 안 돼 넣지 않았다).

검증: tsc 0 · `npm test` **583/583**.

### 오류 원장

| # | 오류 | 원인 | 처방 |
|---|---|---|---|
| 1 | 2일간 "고쳤다"면서 고객 여정을 한 번도 안 돌렸다 | 게이트·결함목록이 곧 품질이라고 착각 | **마감 판정에 "고객 여정 1회 완주"를 넣는다.** 게이트 초록은 작동의 증거가 아니다 |
| 2 | 공사 종류 칩 클릭 30초 타임아웃 | 칩이 `role="radio"` 인데 `getByRole('button')` 으로 찾음 | 접근성 role 을 먼저 확인하고 선택자를 만든다 |
| 3 | 조사 스캐너 첫 실행이 자기 주석을 잡음 | 결함을 설명하는 주석이 그 결함 문구를 인용 | 스캐너는 주석을 걷어내고 채점 |
