# 2026-07-16 작업정리 — PHASE O-32 (예상견적 의뢰 채널 화면 레이아웃 정돈)

## 배경
2026-07-15 O-31에서 예상견적 의뢰 탭 채널 선택 2카드 아래에 견적 절차 방법론(좌 Crisp-DM 7단계 / 우 KDD 6단계)을 신설. 이번엔 그 화면의 레이아웃 정돈 지시(캡쳐 + 손글씨) 및 후속 검토 지시를 반영.

## 반영 내용

### 1) 채널 2카드 좌우로 넓게 + 독립 패널 (캡쳐 지시 ①)
- `components/forms/RequestWizard.tsx` 루트 래퍼를 `channelView`일 때만 **공용 박스(border/shadow) 제거** + `max-w-3xl lg:max-w-4xl mx-auto`.
- 채널 그리드 `p-6 md:p-8` 패딩 제거, `gap-6 md:gap-8`로 좌우 분리.
- 각 카드 열을 **독립 패널**(`rounded-custom border border-border bg-bg p-5 md:p-6`)로, 내부 CTA 버튼의 자체 테두리·패딩 제거(이중선 방지).

### 2) 절차 설명 한 줄로 깔끔하게 (캡쳐 지시 ③)
- `MethodologyList` `<li>` = `text-[13.5px] leading-snug`, `<ol>` gap-1.5.
- 폭 확대만으로는 3-Pane 중앙 패널(우측 검토 패널 탓 협소)에서 긴 단계가 2줄로 넘어감을 브라우저 실측으로 확인 → **각 단계 설명을 핵심 키워드(≈11~14자)로 압축**(신뢰구간·교차검증 등 방법론 용어 유지). 데스크톱·모바일 모두 전 단계 한 줄 확인.

### 3) 하단 Footer — 브랜드·핵심영역 삭제, © 바만 (캡쳐 지시 ②)
- `app/page.tsx` `renderBrandFooter()`에서 ZEROS 워드마크·"최적합 견적 검증" 태그라인·설명문·"핵심 영역" 3줄 삭제 → **© 저작권 바 단독**(저작권·개인정보처리방침·문의하기, `border-t-2 border-navy pt-4`).

### 4) Footer 하단 앵커·폭 통일 (후속 검토 지시)
- Footer 오버라인선이 좌측 사이드바 하단 레벨보다 213px 위로 떠 있던 오류 발견 → `renderRequestTab` footer 래퍼를 `mt-auto pt-6`(하단 앵커 복원)로 교정, 오버라인 810으로 하강(좌측 하단 860과 차이 50px 이내).
- 채널 컨테이너·footer 폭을 **AI Native(SOP) 탭 `renderSopTab`과 동일한 `max-w-3xl lg:max-w-4xl`로 통일**(구 max-w-5xl). 실측상 두 탭 패널 폭 완전 일치(우측 패널 열림 시 815px, 숨김 시 896px).

### 5) 우측 '예상 견적범위' 패널 — 의뢰 탭도 기본 숨김 (후속 지시)
- `components/layout/AppShell.tsx` 자동 숨김 useEffect 조건에 **`activeTab === 'request'` 추가**(기존 review·business·performance만 포함해 의뢰 탭에서 직전 상태가 남아 패널이 열린 채 보이던 문제). 엣지 탭 클릭 시 펼침은 유지(브라우저 실측 확인).

## 검증
- `npm run build` 0 에러 · `ui-quality-gate --pass`(차단룰 0) · `graphify update .`.
- 브라우저 실측(1440 데스크톱·375 모바일): 독립 패널·© 단독 footer·절차 한 줄·footer 하단 정렬·폭 SOP 일치·우측 패널 기본 숨김/탭 펼침 모두 확인.

## 파일
- `components/forms/RequestWizard.tsx` · `app/page.tsx` · `components/layout/AppShell.tsx` · `AGENTS.md`(§Footer·§O-31 채널카드·§우측패널 규칙 갱신).

## 규칙 영구화(AGENTS.md)
- Footer = © 바 단독·`mt-auto`·`max-w-4xl`(SOP 동기), 브랜드/핵심영역 재추가 금지.
- 채널 컨테이너 = `max-w-3xl lg:max-w-4xl`(SOP 동일 폭), 방법론 설명 = 핵심 키워드 압축(길게 되돌리지 말 것).
- 우측 예상 견적범위 패널 자동 숨김 조건에 request 포함(빼지 말 것).

---

# PHASE O-33 (같은 날 후속) — 좌측 메뉴 라벨 접미어 삭제 + 종료 가드 작동 검증

## 1) 메뉴 라벨 X표시 문구만 삭제 (캡쳐 지시 — "딱 X표시 문구만", 이유: 필요 이상 설명·오해 소지)
- 대상 5문구: `CAPEX개선,증설 ~~검토~~` · `배관 SPOOL Module ~~검토~~` · `SKID 제작설치 ~~검토~~` · `Structure제작,설치 ~~검토~~` · `공사규모·금액 ~~미정~~`.
- 반영 위치(전체 웹앱): 단일 소스 `lib/constants/menu.ts` `MENU_DISPLAY_NAMES`(사이드바·우측 패널·상세 제목 공용) + 하드코딩 4곳 — `LeftSidebar.tsx`(로컬 label)·`AppShell.tsx`(모바일 드로어)·`app/page.tsx`(홈카드 title/desc)·`PerformanceInsights.tsx`(실적 라벨). 구 문구 grep 잔존 0건.
- **범위 엄수**: X 없는 '공장증설 검토'·'온라인 간편검토' 유지. **데이터 키**(`'CAPEX 개·증설 검토'` WorkType/menu value)는 DB 저장 키라 불변 — 표시 문자열만 수정.
- 검증: `npm run build` 0에러 · `ui-quality-gate --pass`. (헤드리스 프리뷰 스플래시 고착 → §8 규약대로 코드+빌드 검증 대체.)

## 2) 모바일 '앱 닫기 팝업'(종료 가드) 정확 작동 검증 (마감 전 지시)
- Playwright(375×812)로 전 시나리오 실측: ①진입 시 가드 적재(`zerosExitGuard` 치환+`zerosShell` 한 층) ②뒤로가기 → "앱을 닫으시겠습니까?" 하단 시트(닫기/계속 사용 48px) 표시 ③'계속 사용' → 팝업 닫힘+가드 재적재+앱 유지 ④재차 뒤로가기 → 팝업 재표시 ⑤'닫기' → `window.close()` 베스트에포트(비스크립트 탭 차단 = 설계 주석대로). **전 항목 통과, 코드 수정 불필요.**
- 교훈(오류 원장): 헤드리스 Browser pane이 rAF 스로틀로 스플래시 고착 시 → **Playwright MCP로 대체 검증**(rAF 정상 작동, waitForSelector+run_code로 시나리오 주행 가능).

## 파일
- `lib/constants/menu.ts` · `components/layout/LeftSidebar.tsx` · `components/layout/AppShell.tsx` · `app/page.tsx` · `components/PerformanceInsights.tsx` · `AGENTS.md`(§10-A 라벨 규칙 신설).

---

# PHASE O-34 (같은 날 후속) — 웹 기본 규칙 검토 → 프로덕션 env 프로브 도입 + 등록 가이드

## 1) 검토 결과 (지시: 홈탭 첫화면 · 종료 팝업 · 휴대폰 인증 정상 여부)
- **홈탭 = 첫 화면: 정상.** URL 무파라미터 진입 시 `activeTab/mobileActiveTab='home'`(`AppShell.tsx` applyUrlState) + PWA `manifest.json` `start_url:"/"`.
- **홈까지 뒤로가기 → 닫기 팝업: 정상.** 종료 가드(스택 바닥 `zerosExitGuard` 치환+한 층 push, 탭 이동마다 pushState → 역순 복귀 후 바닥에서 팝업). O-33 Playwright 전 시나리오 실측 통과 재확인. 유의: 일반 브라우저 탭에선 `window.close()` 차단 가능(설계된 폴백 = 다음 뒤로가기에서 OS 종료), 가드는 모바일 레이아웃(<1024px) 전용(의도).
- **휴대폰 인증 문자: 미작동(비활성) — 라이브 실측.** `GET zerospipe.co.kr/api/otp/status` → `{"enabled":false}` = SOLAPI 3종이 Vercel·로컬 `.env.local` 모두 빈 값. 게이트 자동 생략(테스트 코드 고객 노출 방지 설계는 정상), 고객 무인증 접수 상태.
- **추가 발견: 프로덕션 관리자 로그인 불가.** `POST /api/admin/login` 오답 프로브 → **503** = `ZEROS_ADMIN_PASSWORD` Vercel 미설정(`login/route.ts:26` fail-closed). 보안 누출은 없으나 기능 정지 상태.

## 2) 보완 실행 (P2 — 재발 방지 기계화)
- **`scripts/prod-probe.mjs` 신설**: 라이브 3종 기계 판정 — ①사이트 200 ②`/api/otp/status` enabled ③admin/login 오답 401(=env 있음)/503(=미설정). 기본 보고만(exit 0), `--strict`면 WARN/FAIL 시 exit 1. 실행 검증: PASS 1·WARN 2(현 상태 정확 판정).
- **마감처리 9단계 편입**: `.claude/commands/마감처리.md`에 "배포 후 프로브(필수)" 추가 — WARN은 차단 아님·보고 필수(키 미등록을 "정상"으로 보고 금지).
- **`AGENTS.md §13` 조문 추가**: 실측 결과 + "키 등록 전 OTP·관리자 로그인 정상 전제 금지" + 프로브 의무.
- **주인님 등록 가이드**: `docs/03_qa_deploy/prod-env-setup.md` — Solapi 가입→발신번호 사전등록(심사 1~2일)→키 발급→Vercel Environment Variables 등록→Redeploy→프로브 3/3 PASS 검증. 키 입력은 보안 원칙상 사람 전용(AI 대행·채팅 전달 금지 명시).
- memory `prod-env-gaps-2026-07-16` 기록(키 등록 전 "인증 정상" 전제 금지).

## 검증
- `npm run build` 0에러 · `graphify update .` · 프로브 실측 1회(현 상태 정확 판정 확인). UI(.tsx) 변경 없음 → ui-quality-gate 해당 없음.

## 파일
- 신규: `scripts/prod-probe.mjs` · `docs/03_qa_deploy/prod-env-setup.md` / 수정: `.claude/commands/마감처리.md` · `AGENTS.md`(§13) · `docs/00_orchestration/state.md`(0-C 갱신).

## 남은 주인님 액션 (보안상 대행 불가)
- Vercel에 `SOLAPI_API_KEY/SECRET/SENDER`·`ZEROS_ADMIN_PASSWORD` 등록(+`OTP_SERVER_SECRET` 확인) → Redeploy → `node scripts/prod-probe.mjs` 3/3 PASS 확인.

---

# PHASE O-35 (같은 날 후속) — AGENTS.md 지침 현행화 (md 지침 기록 정리)

## 지시·확정
- 지시 "md 지침 기록 정리" → AskUserQuestion 1회 확정(제3조): **AGENTS.md 현행화**(현행 규칙만 본문, 구 조문·개정 이력은 아카이브 이동, 손실 0).

## 작업
- **§10-A 전면 재편**: 시간순 누적 17개 불릿 → **화면별 그룹**(공통/실적/견적검토/사업소개/의뢰/SOP)으로 재배열, 각 조문 = 최종 확정값만(개정 서사 제거). 상호 모순 조문 병합: 실적 헤더 문구("공종별 견적 실적 분포…"→"실적 분포" 단독), 히트맵 코너 제목(14px→23px), 세부 카드 상단보더(철회 반영) 등.
- **§10 기본 조문 현행화**: 사업소개 히어로 헤드라인 `clamp ~50px`→현행 `clamp(30px,3vw,38px)` 한 줄, Footer 삭제 반영 · 실적 KPI 오렌지 펄스→무채색(2026-07-05 폐지) 반영.
- **누락 조문 편입**: 사업소개 O-30 구조(2단 모델·성장 축·신뢰 지표→CTA, 3단계 타임라인 폐지) — state.md에만 있고 AGENTS에 없던 것을 조문화(제2조).
- **§7 제목 개칭**("오늘 확정된 구조"→"확정 화면 구조 — 견적검토") + 우측 패널 확장 각주. **섹션 번호 §0~13 불변**(외부 참조 보존).
- **아카이브 신설**: `docs/00_orchestration/agents-archive.md` — ①개정 연대기 표(폐지 조문→최종값 18행) ②재작성 조문 원문 전문 스냅샷(§7 제목·§10 2불릿·§10-A 전문). 무손실 기준점 = git `e32c6c9`.
- **AGENTS §10-A 헤더에 아카이브 참조 규칙**: 구 패턴 복원 전 아카이브에서 폐지 여부 확인 의무.

## 검증
- 1:1 재대조(제7조): 원문 §10-A 17불릿 + §10 2불릿 + §7 → 전부 현행 조문 배치 또는 아카이브 이동 확인(누락 0). 상호참조 오류 1건(§7 각주 "공통 ⑦") 자체 발견·교정. `npm run build` 0에러(md-only). UI 변경 없음 → ui-quality-gate 해당 없음.

## 파일
- 수정: `AGENTS.md`(§7·§10·§10-A) / 신규: `docs/00_orchestration/agents-archive.md`.

## 규칙
- 앞으로 §10-A 개정 시: 본문 = 최종값 갱신, 폐지된 구 값 = 아카이브 연대기에 1행 추가(본문에 개정 서사 누적 금지).
