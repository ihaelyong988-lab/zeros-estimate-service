# 2026-07-16 작업정리 — PHASE O-32 (예상견적 의뢰 채널 화면 레이아웃 정돈)

## 배경
2026-07-15 O-31에서 예상견적 의뢰 탭 채널 선택 2카드 아래에 견적 절차 방법론(좌 Crisp-DM 7단계 / 우 KDD 6단계)을 신설. 이번엔 그 화면의 레이아웃 정돈 지시(캡쳐 + 손글씨) 및 후속 검토 지시를 반영.

## 반영 내용

### 1) 채널 2카드 좌우로 넓게 + 독립 패널 (캡쳐 지시 ①)
- `components/forms/RequestWizard.tsx` 루트 래퍼를 `channelView`일 때만 **공용 박스(border/shadow) 제거** + `max-w-3xl lg:max-w-4xl mx-auto`.
- 채널 그리드 `p-6 md:p-8` 패딩 제거, `gap-6 md:gap-8`로 좌우 분리.
- 각 카드 열을 **독립 패널**(`rounded-custom border border-border bg-bg p-5 md:p-6`)로, 내부 CTA 버튼의 자체 테두리·패딩 제거(이중선 방지).

### 2) 절차 설명 한 줄로 깔끔하게 (캡쳐 지시 ③)
- `MethodologyList` `<li>` = `text-[13.5px] leading-snug`, `<ol>` gap-1.5.
- 폭 확대만으로는 3-Pane 중앙 패널(우측 검토 패널 탓 협소)에서 긴 단계가 2줄로 넘어감을 브라우저 실측으로 확인 → **각 단계 설명을 핵심 키워드(≈11~14자)로 압축**(신뢰구간·교차검증 등 방법론 용어 유지). 데스크톱·모바일 모두 전 단계 한 줄 확인.

### 3) 하단 Footer — 브랜드·핵심영역 삭제, © 바만 (캡쳐 지시 ②)
- `app/page.tsx` `renderBrandFooter()`에서 ZEROS 워드마크·"최적합 견적 검증" 태그라인·설명문·"핵심 영역" 3줄 삭제 → **© 저작권 바 단독**(저작권·개인정보처리방침·문의하기, `border-t-2 border-navy pt-4`).

### 4) Footer 하단 앵커·폭 통일 (후속 검토 지시)
- Footer 오버라인선이 좌측 사이드바 하단 레벨보다 213px 위로 떠 있던 오류 발견 → `renderRequestTab` footer 래퍼를 `mt-auto pt-6`(하단 앵커 복원)로 교정, 오버라인 810으로 하강(좌측 하단 860과 차이 50px 이내).
- 채널 컨테이너·footer 폭을 **AI Native(SOP) 탭 `renderSopTab`과 동일한 `max-w-3xl lg:max-w-4xl`로 통일**(구 max-w-5xl). 실측상 두 탭 패널 폭 완전 일치(우측 패널 열림 시 815px, 숨김 시 896px).

### 5) 우측 '예상 견적범위' 패널 — 의뢰 탭도 기본 숨김 (후속 지시)
- `components/layout/AppShell.tsx` 자동 숨김 useEffect 조건에 **`activeTab === 'request'` 추가**(기존 review·business·performance만 포함해 의뢰 탭에서 직전 상태가 남아 패널이 열린 채 보이던 문제). 엣지 탭 클릭 시 펼침은 유지(브라우저 실측 확인).

## 검증
- `npm run build` 0 에러 · `ui-quality-gate --pass`(차단룰 0) · `graphify update .`.
- 브라우저 실측(1440 데스크톱·375 모바일): 독립 패널·© 단독 footer·절차 한 줄·footer 하단 정렬·폭 SOP 일치·우측 패널 기본 숨김/탭 펼침 모두 확인.

## 파일
- `components/forms/RequestWizard.tsx` · `app/page.tsx` · `components/layout/AppShell.tsx` · `AGENTS.md`(§Footer·§O-31 채널카드·§우측패널 규칙 갱신).

## 규칙 영구화(AGENTS.md)
- Footer = © 바 단독·`mt-auto`·`max-w-4xl`(SOP 동기), 브랜드/핵심영역 재추가 금지.
- 채널 컨테이너 = `max-w-3xl lg:max-w-4xl`(SOP 동일 폭), 방법론 설명 = 핵심 키워드 압축(길게 되돌리지 말 것).
- 우측 예상 견적범위 패널 자동 숨김 조건에 request 포함(빼지 말 것).
