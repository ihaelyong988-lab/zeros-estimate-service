# 2026-07-15 작업정리

## PHASE O-31 — 예상견적 의뢰 탭: 채널 카드 하단 견적 절차 방법론(Crisp-DM / KDD) 신설

### 배경·판독
- 사용자 캡쳐 2장 지시. **이미지 2(사업 소개 + 파란 지시문)** 는 PHASE O-30에서 이미 반영·배포된 지시로 확정 — 라이브 원격 판정법(청크 10개 grep: 신규 마커 "이렇게 제공합니다"/"여기서, 여기로" 존재 · 구 3단계 마커 0건)으로 검증. **작업 대상 아님**(구화면 보이면 캐시 → 시크릿/Ctrl+Shift+R).
- **이미지 1(예상견적 의뢰하기)** = 신규 지시: 채널 선택 2카드 아래에 각각 회색 구분선 + 견적 절차 방법론 목록 신설. 검은 굵은 테두리 = 마크업 프레임(영역 지정)으로 해석, 화면에 미렌더.
- `AskUserQuestion` 3건 확정: ①우측 제목 = 채널명에 맞게 교정("무료 견적 신청 - 견적 절차 KDD 방법론") ②제목 앞 `#` 제거(장식 최소화) ③시안 생략, 바로 구현·마감처리.

### 반영 (`components/forms/RequestWizard.tsx`)
1. **데이터 분리**: `CHANNEL_METHODOLOGY` 맵 신설 — visit=Crisp-DM 7단계(작업 이해→데이터 이해→데이터 준비→AI모델링→평가→전개→고객 제공) / quick=KDD 6단계(데이터→전처리→변환→마이닝→평가→고객 제공), 각 단계 = 용어+한 줄 설명(캡쳐 문구 그대로).
2. **공용 렌더 `MethodologyList`**: 구분 바(`h-[3px] bg-border/80`) + 제목 15px font-black navy + `<ol>` 번호 목록(용어 14px font-black navy `tabular-nums` · 설명 gray font-semibold, leading-relaxed). **카드 `<button>` 바깥** 비인터랙티브 영역(거대 클릭영역·중첩 인터랙션 방지).
3. **채널 선택 그리드 재구성**: 각 셀을 `div.flex.flex-col`로 감싸 `카드 버튼(변경 없음) → MethodologyList` 세로 묶음, `gap-x-6 gap-y-6`. 모바일은 기존 `grid-cols-1` 자연 스택.

### 검증
- `ui-quality-gate --check` → 차단룰 0(R2 경고는 기존 장식 요소 잔존, 신규 코드 무관) → `--pass` 마커 기록.
- `npm run build` 0에러 (12/12 정적 페이지 생성).
- 배포 후 라이브 청크 grep으로 "견적 절차 Crisp-DM 방법론" 마커 확인 예정.

### 규칙화 (헌법 제1조)
- `AGENTS.md` §10-A에 "의뢰 탭 채널 카드 하단 = 채널별 견적 절차 방법론" 조문 추가(visit=Crisp-DM 7 / quick=KDD 6, 데이터 맵 분리, 제목 채널명 동기·`#` 없음).
