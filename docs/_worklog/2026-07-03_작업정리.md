# 2026-07-03 작업정리

## PHASE O-15 — 견적 검토(데스크톱) 히어로 재설계 (나열 → Benefit 단일 주인공)

### 배경 / 지시
캡쳐 주석 4항목 전수:
- ① (좌 상단) "견적 가능 공종의 **세분화 + 이미지 전달** 패널"
- ② (좌 이미지 영역) "직관적 **공종 실사 이미지**로 방문자 이해를 돕기"
- ③ (우 상단) "**견적 결과 지표** 패널 · 고객 **benefit 지표 그래프** 표현"
- ④ (우 중앙·핵심) "히어로섹션 **디자인·폰트·컬러가 너무 비직관적이고 나열적** → 최고의 개선 방향 검토"

### 진단
`renderReviewDesktop()` 히어로가 우측에 `작업과정 01·02·03` 리스트 + `결과지표(−34.2%·막대2·신뢰도·표본)`가 **균등 무게로 5개 나열** → 주인공(위계) 부재. 핵심 benefit(−34.2%)이 하단에 묻힘. 좌측 키워드도 `|` 나열로 평평.

### 시안 → 확정
`ui-ux-pro-max`(Trust & Authority·`--design-system` landing/chart 조회) 선호출 → `show_widget`로 현재안 + A안(Benefit 단일 주인공)·B안(Before→After 대비) 비교 → `AskUserQuestion`으로 **A안 + 하단 밴드도 다듬기** 확정.

### 반영 (A안) — `app/page.tsx` `renderReviewDesktop()`
1. **좌측(①②)**: 공종 타이틀 22→26px 승격(밑줄 박스 제거). 키워드 `|` 나열 → `키워드 · 키워드` **한 줄 절제**(13px gray). 이미지 자리 점선박스 → 좌하단 **크게 확보한 실사 이미지 영역**(inset 링, 플레이스홀더 — 실제 이미지 파일 대기).
2. **우측(③④)**: `작업과정 01·02·03` 3줄 리스트 삭제 → **−{bubbleRate}% 54px 대형 주인공**으로 승격 + `검토 전 100 → ZEROS 최적합 {optimizedIndex}` before→after 바 그래프. 신뢰지표(신뢰도·표본) 인라인 한 줄. 작업과정은 하단 **한 줄 절차 캡션**(자료 정합 → DB 교차대조 → AI·PM 검증)으로 압축.
3. **색 단일 강조**: 공종 시그니처색(`sigHex`, PHASE N 기능 유지) 하나만 — 대형 지표·바·최적합 라벨에.
4. **하단 밴드**: 박스 그리드(`grid gap-px bg-white/10`) → **얇은 헤어라인(`w-px h-8 bg-white/15`) 구분 인라인 지표**로 정리(여백·헤어라인 철학). 헤드라인 22→21px, 라벨 대비 정리.
5. 그리드 비율 `1.5fr:1fr` → `1fr:1fr`(benefit 패널 균형).

### 검증
- `npm run build` **0에러** (Compiled successfully).
- UI 품질 게이트 `--check`/`--pass`: **차단룰(R1) 0건 → 통과**. 남은 R2 경고 2곳(343·398행)은 히어로 밖 기존 코드·장식 `|` 구분자.
- 이번 편집으로 히어로의 기존 `text-gray-light` 2곳을 `text-gray`로 개선.

### 파일
- `app/page.tsx` (`renderReviewDesktop` 히어로 좌/우 컬럼 + 하단 밴드)
